# Verl examples 参数特性报告 2026-06-27

## 扫描对象

- 仓库：`/Users/Zhuanz/work/github/verl`
- 分支/提交：`codex/verl-case-formal-20260618-a2ak225-sync8192-r1-20260618-a2ak225-async16384-r1-20260618-a2ak225-async-r4-20260618-005703-a2ak225-20260617-222232-a3ax180-main` / `049a3295`
- 工作区 dirty：`True`
- 扫描根目录：`examples`
- 扫描 examples 文件数：`94`
- 参数特性数：`773`

## 参数总览

| 分类 | 参数数 |
|---|---:|
| 效率 | 530 |
| 算法 | 123 |
| 配置 | 120 |

## Top 参数特性

| 参数 | 分类 | 中文解释 | 常见值 | 性能影响 | 精度影响 | 示例数 |
|---|---|---|---|---|---|---:|
| data.train_files | 配置 | 指定训练/验证数据来源，影响任务分布、评测口径和数据加载路径。 | "$TRAIN_DATA_PATH", "$TRAIN_FILE", "$train_files", "${DATA_PATH}" | 通常不直接影响计算性能；保存、评测或日志频率可能影响端到端耗时。 | 通常不直接影响精度，除非通过性能瓶颈、数据口径或训练稳定性间接影响。 | 94 |
| trainer.experiment_name | 配置 | 训练运行的实验名，用于 wandb/swanlab/mlflow/trackio 等日志后端标识单次 run，并参与默认 checkpoint 路径 `checkpoints/${trainer.project_name}/${trainer.experiment_name}`。 | "${EXPERIMENT_NAME}", "${exp_name}", "${experiment_name}", "ppo_clip_multi_rs" | 机制推断：仅作为日志与 checkpoint 路径标识，通常不改变训练吞吐；名称冲突或不规范主要影响归档、检索和恢复定位。 | 机制推断：不参与模型前向、反向或奖励计算，通常不直接影响精度；主要影响实验对比和结果追踪口径。 | 93 |
| trainer.logger | 配置 | 训练指标日志后端列表；Verl `Tracking` 支持 console、wandb、swanlab、mlflow、tensorboard、trackio、file 等后端，examples 常用 `["console","wandb"]`。 | '["console", "wandb"]', '["console","tensorboard","file"]', '["console","tensorboard"]', '["console","wandb","file"]' | 机制推断：console 开销较小；启用 wandb/mlflow/tensorboard/trackio/file 会增加序列化、磁盘或网络写入，日志量大时可能拉长端到端训练时间。 | 机制推断：只改变指标记录位置，不改变训练目标或梯度；主要影响可观测性、复现实验排查和模型选择依据。 | 93 |
| trainer.project_name | 配置 | 训练项目名，用于 wandb/swanlab/mlflow/trackio 等日志系统归组实验；也参与默认 checkpoint 路径 `checkpoints/${trainer.project_name}/${trainer.experiment_name}`。 | "${PROJECT_NAME}", "${project_name}", "rollout_corr_multi_rs_example", "rollout_corr_rloo_example" | 机制推断：仅作为日志项目与 checkpoint 路径标识，通常不改变计算性能；命名冲突主要影响归档、权限或远端 tracking 查询。 | 机制推断：不进入模型训练计算，通常不直接影响精度；会影响跨 run 聚合、对比和最佳 checkpoint 选择的管理口径。 | 93 |
| data.val_files | 配置 | 指定训练/验证数据来源，影响任务分布、评测口径和数据加载路径。 | "$TEST_DATA_PATH", "$TEST_FILE", "$test_files", "$val_files" | 通常不直接影响计算性能；保存、评测或日志频率可能影响端到端耗时。 | 通常不直接影响精度，除非通过性能瓶颈、数据口径或训练稳定性间接影响。 | 91 |
| trainer.total_epochs | 算法 | 总训练 epoch 数；Verl trainer 按该值遍历训练 dataloader，未显式设置 `total_training_steps` 时训练步数通常由 dataloader 长度乘以 `trainer.total_epochs` 得到。 | 1, 10, 1000, 15 | 机制推断：epoch 越多，总训练步数、rollout/反向/验证/保存次数通常越多，端到端耗时和资源消耗近似随训练步数增加。 | 机制推断：增加训练预算可能改善收敛和任务指标，但过多 epoch 也可能带来过拟合、奖励过优化或 RL 训练不稳定。 | 89 |
| data.train_batch_size | 效率 | 控制训练全局 batch，影响样本吞吐、梯度稳定性和单步耗时。 | $train_batch_size, 0, 1024, 128 | 机制推断：增大通常提高有效吞吐或样本量，但会增加显存和单步时间。 | 机制推断：影响优化动态、稳定性和收敛速度。 | 86 |
| data.truncation | 效率 | 控制超过 `data.max_prompt_length` 的输入如何处理：从左/右截断，或在严格校验时直接报错。 | 'error', 'left', 'right', error | 文档说明：截断可让长尾 prompt 在固定长度预算内继续训练，`error` 更适合严格校验但会暴露并中断超长样本；真正的算力开销主要由 `max_prompt_length` 与实际 token 数决定。 | 文档说明：左/右截断会丢弃部分上下文；官方 best practices 提到若训练日志出现较大 `clip_ratio` 和指标变差，应增大 `data.max_prompt_length` 或清洗数据。 | 85 |
| trainer.save_freq | 配置 | 按 iteration 控制 actor/critic checkpoint 保存频率；正数表示每隔对应步数保存，`-1`/非正值在 PPO trainer 中表示不按频率保存。 | $save_freq, ${SAVE_FREQ}, -1, 10 | 机制推断：保存越频繁，checkpoint 序列化、分片写盘和远端同步 I/O 越多，端到端训练时间和存储占用会增加；关闭保存可减少 I/O 但降低容错。 | 机制推断：不改变优化目标或梯度，通常不直接影响精度；会影响故障恢复、回滚和选择历史最优 checkpoint 的能力。 | 83 |
| trainer.test_freq | 配置 | 按 iteration 控制验证/测试频率；正数表示每隔对应训练步运行验证，`-1`/非正值通常表示不按频率触发验证，SFT 中还可用 `after_each_epoch` 表示每个 epoch 后验证。 | ${TEST_FREQ}, -1, 10, 20 | 机制推断：验证越频繁，生成/打分和指标汇总越多，会占用训练资源并拉长 wall-clock 时间；频率过低则减少监控开销。 | 机制推断：不直接参与参数更新，通常不改变最终梯度；更频繁验证有助于及时发现退化和选择 checkpoint，但本身不是精度提升机制。 | 83 |
| actor_rollout_ref.model.path | 配置 | 指定模型权重或模型 ID，是模型规模、结构、显存占用和任务能力的来源。 | "$MODEL_PATH", "$STUDENT_MODEL", "${MODEL_PATH}", "${RAY_DATA_HOME | 通常不直接影响计算性能；保存、评测或日志频率可能影响端到端耗时。 | 通常不直接影响精度，除非通过性能瓶颈、数据口径或训练稳定性间接影响。 | 80 |
| actor_rollout_ref.rollout.name | 配置 | 选择 rollout 推理后端，例如 vLLM、SGLang 或 TensorRT-LLM，决定生成吞吐、兼容性和 NPU 迁移风险。 | "vllm", $ENGINE, $rollout_name, sglang | 机制推断：不同推理后端会改变生成吞吐、显存管理、启动开销和 NPU 兼容性。 | 通常不直接影响精度，除非通过性能瓶颈、数据口径或训练稳定性间接影响。 | 80 |
| actor_rollout_ref.actor.ppo_mini_batch_size | 效率 | 控制 PPO/反向传播分块大小，是显存占用和 step 时间的核心旋钮。 | $ppo_mini_batch_size, 1024, 128, 16 | 机制推断：增大通常提高有效吞吐或样本量，但会增加显存和单步时间。 | 机制推断：影响优化动态、稳定性和收敛速度。 | 79 |
| data.max_prompt_length | 效率 | 控制 prompt 最大长度，增大可覆盖更长输入，但会增加激活、注意力和数据处理成本。 | $((1024 * 1)), $((1024 * 2)), $max_prompt_length, 1024 | 机制推断：长度越大，显存、KV cache 和单步/生成耗时通常上升。 | 机制推断：改变有效上下文、输出空间或样本保留策略，可能影响任务准确率。 | 79 |
| data.max_response_length | 效率 | 控制 response 最大长度，增大可给模型更多输出空间，但会显著增加 rollout KV cache 和耗时。 | $((1024 * 2)), $((1024 * 20)), $((1024 * 255)), $((1024 * 8)) | 机制推断：长度越大，显存、KV cache 和单步/生成耗时通常上升。 | 机制推断：改变有效上下文、输出空间或样本保留策略，可能影响任务准确率。 | 79 |
| trainer.nnodes | 效率 | 控制训练任务使用的节点数，通常与 Ray 多节点集群和每节点 GPU/NPU 数共同决定全局可用设备数。 | "${NNODES}", $NODES_NUM, $NUM_NODES, $nnodes | 文档说明：官方多节点文档用 `trainer.nnodes` 配合 `trainer.n_gpus_per_node` 提交多节点任务；节点数增加可扩大并行资源，但会带来 Ray 调度、跨节点通信和网络拓扑开销。 | 机制推断：资源规模本身不改变算法目标；只有当同步、全局 batch、随机种子或失败重试改变训练轨迹时才会间接影响结果。 | 79 |
| actor_rollout_ref.actor.optim.lr | 算法 | Actor 策略网络优化器学习率，决定每次 PPO/GRPO 更新时参数移动幅度。 | $actor_lr, 1.0e-05, 1e-5, 1e-6 | 机制推断：不显著改变单步计算量，但会影响达到目标指标所需 step 数；过高导致震荡或回滚时会浪费训练时间，过低会延长收敛。 | 文档说明：官方 best practices 建议从 `1e-5` 或 `1e-6` 附近开始；学习率过大易破坏策略稳定性，过小可能收敛慢或欠优化。 | 79 |
| actor_rollout_ref.rollout.gpu_memory_utilization | 效率 | 控制 rollout engine 可使用的显存比例，影响 KV cache 容量、吞吐和 OOM 风险。 | "${ROLLOUT_GPU_MEM_UTIL}", $gpu_memory_utilization, 0.25, 0.4 | 机制推断：主要改变显存、通信、计算图或调度开销之间的取舍。 | 通常不直接影响精度，除非通过性能瓶颈、数据口径或训练稳定性间接影响。 | 78 |
| actor_rollout_ref.rollout.tensor_model_parallel_size | 效率 | 控制张量并行切分度，降低单卡权重/KV 压力，但会增加层内通信。 | "${ROLLOUT_TP}", $GEN_TP, $infer_tp, ${rollout_tp | 机制推断：主要改变显存、通信、计算图或调度开销之间的取舍。 | 通常不直接影响精度，除非通过性能瓶颈、数据口径或训练稳定性间接影响。 | 78 |
| trainer.n_gpus_per_node | 效率 | 控制每个节点参与训练的 GPU/NPU 数，和 `trainer.nnodes` 一起决定 Ray 任务可调度设备总量。 | "${NGPUS_PER_NODE}", "${NPUS_PER_NODE}", $((8)), $NODE_GPU_NUM | 文档说明：官方多节点示例直接设置 `trainer.n_gpus_per_node` 与 `trainer.nnodes`；每节点设备数越多，可提升并行吞吐或容纳更大模型，但也可能增加节点内通信和显存分片协调成本。 | 机制推断：设备数本身不改变损失函数；若同时改变全局 batch、并行策略或数值归约顺序，结果可能有间接差异。 | 78 |
| algorithm.adv_estimator | 算法 | 选择优势估计器，用来把 reward/价值信号转换为策略梯度中的 advantage；常见值包括 `gae`、`grpo`、`rloo`、`reinforce_plus_plus` 等。 | $adv_estimator, gae, gdpo, gpg | 机制推断：估计器通常不主导单步算力，但会改变是否需要 value/baseline、分组统计或额外归一化，从而影响训练吞吐和显存占用。 | 文档说明：PPO README 列出多个支持的 estimator，best practices 指出 DAPO/GRPO 使用 `grpo`；不同 estimator 改变 bias/variance 与优化目标，直接影响稳定性和最终指标。 | 78 |
| actor_rollout_ref.rollout.n | 算法 | 控制每个 prompt 生成多少条响应，增加探索和训练信号，但 rollout 成本近似线性上升。 | "${N_SAMPLES}", $n_resp_per_prompt, 1, 16 | 机制推断：增大通常提高有效吞吐或样本量，但会增加显存和单步时间。 | 通常不直接影响精度，除非通过性能瓶颈、数据口径或训练稳定性间接影响。 | 77 |
| algorithm.use_kl_in_reward | 算法 | 是否把相对参考模型的 KL 惩罚加入 reward，用 reward 侧约束策略不要偏离参考策略太远。 | $use_kl_in_reward, False, True | 机制推断：启用后需要 KL 相关计算和记录，通常小于 rollout/训练主耗时；若引入参考 log-prob 计算路径，会增加少量前向开销。 | 文档说明：best practices 写明 PPO 常用 `True`，GRPO/DAPO 常用 `False`；启用后强化对参考模型的约束，可降低漂移和 reward hacking，但可能压制探索。 | 72 |
| actor_rollout_ref.actor.use_kl_loss | 算法 | 是否在 actor loss 中加入 KL loss，用 loss 侧正则把当前策略约束在参考策略附近。 | $use_kl_loss, False, True | 机制推断：主要增加 loss 组合和 KL 统计开销；若所需参考 log-prob 已在流程中计算，额外成本较小。 | 文档说明：PPO README 说明该选项用于 KL divergence control，且使用 actor KL loss 时通常不再在 reward 中加 KL；best practices 写明 GRPO 常用 `True`，PPO/DAPO 取值不同。 | 70 |
| data.filter_overlong_prompts | 效率 | 控制是否在数据阶段过滤超过长度上限的 prompt；关闭时通常交给 `data.truncation` 的截断或报错策略处理。 | False, True | 机制推断：开启过滤可减少超长样本进入 tokenization/rollout/训练路径，降低 OOM 或异常风险；但也可能减少有效样本量。 | 机制推断：过滤会改变训练数据分布，尤其会移除长上下文样本；关闭后若依赖截断，则可能保留样本但丢失部分上下文。 | 69 |
| NNODES | 效率 | 示例脚本中的节点数环境变量，通常传入 `trainer.nnodes` 或启动脚本，用于声明多节点训练规模。 | 1, 12, 16, 2 | 文档说明：官方多节点文档用节点数启动 Ray head/worker 并设置 `trainer.nnodes`；增加节点可扩大总设备数，但跨节点通信、Ray 调度和网络带宽会成为性能变量。 | 机制推断：作为资源变量不直接改变模型目标；只有间接改变全局 batch、并行划分或重试行为时才可能影响结果。 | 67 |
| actor_rollout_ref.actor.entropy_coeff | 算法 | Actor loss 中的熵正则系数，用来鼓励策略保持一定随机性和探索能力。 | $entropy_coeff, 0, 0.0 | 机制推断：熵项只增加很小的 loss 统计/归约开销，通常不影响主要吞吐；真正影响端到端耗时的是它对收敛速度和探索轨迹的间接作用。 | 机制推断：较大熵系数会增强探索和输出多样性，但可能降低收敛确定性；过小则更快收敛到当前高 reward 模式，探索不足风险更高。 | 65 |
| actor_rollout_ref.actor.use_dynamic_bsz | 效率 | 为 actor 更新启用动态 batch size，使每次前向/反向尽量处理相近 token 数，而不是固定样本数。 | $use_dynamic_bsz, False, True | 文档说明：官方 perf tuning 说明动态 batch 能显著提升训练效率并降低显存占用；启用后重点调 `ppo_max_token_len_per_gpu`，不再主要调 micro batch。 | 机制推断：目标函数不变，通常不直接影响精度；但 token 上限过小会导致 batch 切分过细或无法覆盖长序列，间接影响稳定性与吞吐。 | 64 |
| actor_rollout_ref.model.use_remove_padding | 效率 | 启用 remove padding/sequence packing，在支持的模型上去掉 padding token 的无效计算。 | $use_remove_padding, False, True | 文档说明：官方 perf tuning 建议启用 `use_remove_padding=True` 做 sequence packing；对 llama、mistral、gemma1、qwen 等模型可减少 padding 计算并提升有效吞吐。 | 机制推断：理论上不改变非 padding token 的训练目标；但模型/后端不支持或与其他特性不兼容时，可能导致数值或运行稳定性问题。 | 64 |
| EXPERIMENT_NAME | 配置 | 示例脚本中的实验名环境变量，通常传入 `trainer.experiment_name`，用于日志、W&B run、trace 和 checkpoint 子目录命名。 | GRPO-Qwen3_5-27B, GRPO-Qwen3_5-35B, dapo_mimo_7b_mtp_fully_async, deepseek_v3_671b_vllm_megatron | 文档说明：quickstart/checkpoint 文档把 checkpoint 路径组织为 `checkpoints/${trainer.project_name}/${trainer.experiment_name}`；该命名参数通常不影响计算性能。 | 文档说明：实验名仅用于追踪和产物归档，不直接改变训练数据、模型或优化目标。 | 63 |
| MODEL_PATH | 配置 | 指定模型权重或模型 ID，是模型规模、结构、显存占用和任务能力的来源。 | "${RAY_DATA_HOME, "${WORK_DIR, "Qwen/Qwen2.5-7B", ByteDance-Seed/Seed-OSS-36B-Base | 通常不直接影响计算性能；保存、评测或日志频率可能影响端到端耗时。 | 通常不直接影响精度，除非通过性能瓶颈、数据口径或训练稳定性间接影响。 | 63 |
| PROJECT_NAME | 配置 | 示例脚本中的项目名环境变量，通常传入 `trainer.project_name`，作为日志、W&B project、trace 和 checkpoint 根目录的一部分。 | GRPO-Qwen3_5, gsm8k-sft, mtp, multiturn-sft | 文档说明：quickstart/checkpoint 文档把 checkpoint 路径组织为 `checkpoints/${trainer.project_name}/${trainer.experiment_name}`；项目名本身不改变训练计算。 | 文档说明：项目名用于追踪和归档，不直接影响模型参数更新或评测结果。 | 63 |
| actor_rollout_ref.actor.ppo_max_token_len_per_gpu | 效率 | 动态 batch 模式下，actor 执行 `update_policy` 前向和反向时单 GPU 可处理的最大 token 数。 | $(((max_prompt_length + max_response_length) * 1)), $(((max_prompt_length + max_response_length) * 10 / 10)), $(((max_prompt_length + max_response_length) * 2)), $(((max_prompt_length + max_response_length) * 8)) | 文档说明：官方 perf tuning 建议该值至少为 `2 * (max_prompt_length + max_response_length)`，并尝试增大以获得更高吞吐；过大增加显存/OOM 风险。 | 机制推断：不改变 loss 定义；若过小导致有效 batch 切得过碎、长样本处理受限或训练频繁退避，可能间接影响稳定性。 | 62 |
| actor_rollout_ref.rollout.log_prob_use_dynamic_bsz | 效率 | 在 old policy/rollout 侧计算 log-prob 时启用动态 batch，让前向按 token 预算而不是固定样本数分批。 | False, False", True | 文档说明：best practices 推荐为 old policy log-prob 启用动态 batching；perf tuning 说明动态 batch 可提升效率并降低显存。 | 机制推断：只改变 log-prob 前向分批方式，不改变概率定义；显存不足或 token 上限不合理时才可能间接影响训练稳定性。 | 61 |
| ROLLOUT_GPU_MEM_UTIL | 效率 | 示例脚本中的 rollout 显存利用率环境变量，通常传入 `actor_rollout_ref.rollout.gpu_memory_utilization` 控制推理后端显存预算。 | 0.25, 0.4, 0.5, 0.6 | 文档说明：best practices 建议在不 OOM 的前提下尽量提高该值，常见 0.8-0.9；perf tuning 说明不同推理后端定义不同，过高可能 OOM，0.5-0.7 常是吞吐和稳定性的折中。 | 机制推断：显存比例不直接改变采样分布；但 OOM、回退批大小或 KV cache 容量不足会间接影响可完成的 rollout 和训练稳定性。 | 60 |
| actor_rollout_ref.rollout.log_prob_max_token_len_per_gpu | 效率 | 动态 batch 模式下，rollout/old policy 计算 log-prob 前向时单 GPU 允许处理的最大 token 数。 | $(((max_prompt_length + max_response_length) * 1)), $(((max_prompt_length + max_response_length) * 2)), $(((max_prompt_length + max_response_length) * 3)), $(((max_prompt_length + max_response_length) / sp_size)) | 文档说明：best practices 要求至少设为 `max_prompt_length + max_response_length` 的倍数以避免截断；perf tuning 将其定义为 `compute_log_prob` 前向的最大 token 数。 | 机制推断：不改变 log-prob 数学定义；设置过低会导致分批过碎或无法覆盖长序列，间接影响训练效率和稳定性。 | 60 |
| MAX_PROMPT_LENGTH | 效率 | 控制 prompt 最大长度，增大可覆盖更长输入，但会增加激活、注意力和数据处理成本。 | $((1024 * 2)), 1024, 2048, 4096 | 机制推断：长度越大，显存、KV cache 和单步/生成耗时通常上升。 | 机制推断：改变有效上下文、输出空间或样本保留策略，可能影响任务准确率。 | 58 |
| MAX_RESPONSE_LENGTH | 效率 | 控制 response 最大长度，增大可给模型更多输出空间，但会显著增加 rollout KV cache 和耗时。 | $((1024 * 20)), 1024, 2048, 4096 | 机制推断：长度越大，显存、KV cache 和单步/生成耗时通常上升。 | 机制推断：改变有效上下文、输出空间或样本保留策略，可能影响任务准确率。 | 58 |
| PPO_MINI_BATCH_SIZE | 效率 | 控制 PPO/反向传播分块大小，是显存占用和 step 时间的核心旋钮。 | 1024, 128, 16, 256 | 机制推断：增大通常提高有效吞吐或样本量，但会增加显存和单步时间。 | 机制推断：影响优化动态、稳定性和收敛速度。 | 58 |
| ROLLOUT_TP | 效率 | 示例脚本中的 rollout tensor parallel size，通常传入 `actor_rollout_ref.rollout.tensor_model_parallel_size` 控制推理后端张量并行度。 | 1, 16, 2, 32 | 文档说明：best practices 建议逐步增大 TP 来扩展 KV cache 容量，同时关注通信成本，尤其 TP > 8 后通信开销更明显。 | 机制推断：并行切分理论上不改变模型输出；不同后端/并行度的数值归约和失败重试可能带来很小的间接差异。 | 57 |
| TRAIN_BATCH_SIZE | 效率 | 控制训练全局 batch，影响样本吞吐、梯度稳定性和单步耗时。 | 1024, 128, 16, 192 | 机制推断：增大通常提高有效吞吐或样本量，但会增加显存和单步时间。 | 机制推断：影响优化动态、稳定性和收敛速度。 | 57 |
| ACTOR_LR | 算法 | 示例脚本中的 actor 学习率环境变量，通常传入 `actor_rollout_ref.actor.optim.lr`。 | 1.0e-05, 1e-5, 1e-6, 3e-6 | 机制推断：不改变单步算子规模，但影响收敛所需 step 数；过大造成不稳定会浪费训练时间，过小会拖慢达到目标指标的速度。 | 文档说明：官方 best practices 建议从 `1e-5` 或 `1e-6` 附近开始；该值直接影响优化稳定性、探索后策略更新幅度和最终效果。 | 57 |
| actor_rollout_ref.ref.log_prob_max_token_len_per_gpu | 效率 | 动态 batch 模式下，reference policy 计算参考 log-prob 前向时单 GPU 允许处理的最大 token 数。 | $(((max_prompt_length + max_response_length) * 1)), $(((max_prompt_length + max_response_length) * 2)), $(((max_prompt_length + max_response_length) * 3)), $(((max_prompt_length + max_response_length) / sp_size)) | 文档说明：perf tuning 将 `actor_rollout_ref.ref.log_prob_max_token_len_per_gpu` 定义为 `compute_ref_log_prob` 前向的最大 token 数；值越大可减少分批并提升吞吐，但显存占用更高。 | 机制推断：不改变 reference log-prob 定义；若过小导致长序列处理受限或频繁切分，只会通过效率和稳定性间接影响结果。 | 56 |
| TOTAL_EPOCHS | 算法 | 示例脚本中的总训练 epoch 数环境变量，通常传入 `trainer.total_epochs` 控制训练循环轮数。 | 1, 10, 1000, 15 | 文档说明：quickstart 和多节点示例直接设置 `trainer.total_epochs`；增大该值通常近似线性增加端到端训练时长和 checkpoint/评测机会。 | 机制推断：训练轮数增加通常给模型更多优化机会，但过多可能过拟合、策略漂移或放大 reward hacking；过少可能欠训练。 | 56 |
| actor_rollout_ref.ref.log_prob_use_dynamic_bsz | 效率 | 在 reference policy 计算参考 log-prob 时启用动态 batch，让分批按 token 预算适配不同序列长度。 | False, True | 文档说明：best practices 推荐为 reference policy 启用动态 batching；perf tuning 说明动态 batch 能提升效率并降低显存压力。 | 机制推断：只改变参考模型前向的批处理组织，不改变 KL/log-prob 定义；仅在 token 上限或显存配置不当时产生间接影响。 | 55 |
| actor_rollout_ref.actor.kl_loss_coef | 算法 | Actor KL loss 的权重系数，控制当前策略向参考策略回拉的强度。 | $kl_loss_coef, 0.0, 0.001, 0.01 | 机制推断：系数本身不改变主计算量；但较大约束可能改变收敛速度和所需训练 step，间接影响端到端耗时。 | 文档说明：PPO README 给出默认 0.001，best practices 建议从约 0.001 开始；更大值能抑制 reward hacking/策略漂移，但会降低探索。 | 54 |
| SAVE_FREQ | 配置 | 示例脚本中的 checkpoint 保存频率环境变量，通常传入 `trainer.save_freq`；`-1` 常表示不按步保存。 | -1, 10, 100, 20 | 文档说明：quickstart/多节点示例设置 `trainer.save_freq`，checkpoint 文档说明会写入 project/experiment 目录；频繁保存会增加 I/O 和端到端耗时，`-1` 可减少保存开销。 | 机制推断：保存频率不改变训练目标；但影响可恢复点密度，故障后从较旧 checkpoint 恢复可能间接改变训练轨迹。 | 54 |
| TEST_FREQ | 配置 | 示例脚本中的验证/测试频率环境变量，通常传入 `trainer.test_freq` 控制每隔多少 step 计算验证指标。 | -1, 10, 20, 5 | 文档说明：quickstart 指出 `val/test_score/openai/gsm8k` 会按 `trainer.test_freq` 计算；更频繁验证会增加评测耗时，`-1` 常用于关闭周期性验证。 | 机制推断：评测频率不直接改变训练更新；但会影响早停/模型选择/问题排查的粒度，从而间接影响最终选用 checkpoint。 | 54 |
| ROLLOUT_N | 算法 | 示例脚本中的每 prompt 采样响应数环境变量，通常传入 `actor_rollout_ref.rollout.n`。 | 1, 16, 2, 4 | 文档说明：GRPO README 写明总 trajectories = `train_batch_size * rollout.n`，best practices 给出 GRPO/DAPO 常见较大取值；增大 `n` 会近似线性增加生成、打分和 log-prob 计算量。 | 文档说明：`rollout.n` 是每个 prompt 的生成数，GRPO 要求至少 2；更多样本增强组内比较和探索信号，但也可能增加 off-policy drift 与训练成本。 | 52 |
| NGPUS_PER_NODE | 效率 | 示例脚本中的每节点 GPU 数环境变量，通常传入 `trainer.n_gpus_per_node`。 | ${GPUS_PER_NODE:-8, 2, 4, 8 | 文档说明：PPO/GRPO README 和多节点文档都展示通过 `NGPUS_PER_NODE`/`trainer.n_gpus_per_node` 调整资源；增大可提升并行容量，但受节点内互联、显存和并行策略约束。 | 机制推断：资源变量不直接改变目标函数；若同步 batch、并行切分或数值归约路径随之变化，可能产生间接差异。 | 50 |
| trainer.balance_batch | 效率 | 控制 trainer 是否对 batch 做负载均衡分配，以减少不同 rank 之间因样本长度或分组导致的工作量不均。 | False, True | 文档说明：PrefixGrouper README 写明 `balance_batch=True` 可获得更好的 load distribution，并要求 `batch_size % (world_size * rollout.n) == 0`；开启后可减少慢 rank 拖尾，但需满足整除约束。 | 机制推断：负载均衡不改变样本内容或 loss 定义；若分组约束处理不当导致样本排列/uid 分布变化，才可能间接影响训练统计。 | 48 |
| actor_rollout_ref.actor.kl_loss_type | 算法 | 选择 actor KL loss 的计算形式，例如 `kl`/`abs`/`mse`/`low_var_kl`/`full`，决定如何估计当前策略与参考策略的 KL 差异。 | $kl_loss_type, low_var_kl | 机制推断：不同 KL 公式的额外计算通常小于模型前后向主耗时；若选择更完整的 KL 形式，可能增加少量张量计算和内存读写。 | 文档说明：PPO README 列出 `kl(k1)`、`abs`、`mse(k2)`、`low_var_kl(k3)`、`full` 等类型，并说明其用于计算 actor 与 reference policy 的 KL；不同估计方式会影响约束强度、方差和训练稳定性。 | 48 |
| PPO_MAX_TOKEN_LEN_PER_GPU | 效率 | 文档说明：对应 actor/ref/critic 动态 batch 的每 GPU token 上限，限制一次 forward/backward 或 log-prob 计算可处理的 token 数，常按 `max_prompt_length + max_response_length` 的倍数设置以避免 OOM 并提升吞吐。 | $((MAX_PROMPT_LENGTH + MAX_RESPONSE_LENGTH)), $((max_prompt_length + max_response_length)), 10240, 12288 | 文档说明：这是本地 per-GPU 性能参数；调大可提高动态 batch 吞吐，但会增加激活/显存峰值，长序列训练时需要下调以避免 OOM。 | 机制推断：通常不改变优化目标；若设置过小导致样本被截断、频繁 OOM 或有效 batch 下降，可能间接影响训练稳定性。 | 47 |
| actor_rollout_ref.model.enable_gradient_checkpointing | 效率 | 用重算换激活显存，适合长序列或大模型，但会增加计算时间。 | $enable_gradient_checkpointing, True | 机制推断：主要改变显存、通信、计算图或调度开销之间的取舍。 | 通常不直接影响精度，除非通过性能瓶颈、数据口径或训练稳定性间接影响。 | 46 |
| ENTROPY_COEFF | 算法 | 文档说明：对应 `actor_rollout_ref.actor.entropy_coeff`，是 PPO/RL actor loss 中熵正则项的权重，用来调节策略随机性和探索程度。 | 0, 0.0 | 机制推断：非零时需要计算并记录策略熵，主要增加少量 loss 计算和日志开销，相比 rollout/训练主体通常不是吞吐瓶颈。 | 文档说明：增大熵正则会鼓励更随机的策略和更多探索；过大可能削弱对高奖励输出的收敛，过小则探索不足。 | 46 |
| trainer.critic_warmup | 算法 | 文档说明：Critic 预热步数；Ray trainer 文档/源码语义是在全局步数达到该阈值后才执行 actor 更新，常见示例设为 0 表示不单独预热 critic。 | $critic_warmup, 0 | 机制推断：大于 0 会在预热阶段增加只训练 critic 的迭代时间，但可能让后续 actor 更新使用更稳定的 value baseline。 | 机制推断：PPO/GAE 依赖 critic 估值，适当预热可能降低优势估计噪声；GRPO 等无 critic 流程通常设 0，影响较小。 | 45 |
| actor_rollout_ref.actor.fsdp_config.param_offload | 效率 | 把参数、梯度或优化器状态卸载到 CPU/主机侧，降低 HBM 压力但可能拖慢训练。 | False, True | 机制推断：主要改变显存、通信、计算图或调度开销之间的取舍。 | 通常不直接影响精度，除非通过性能瓶颈、数据口径或训练稳定性间接影响。 | 43 |
| actor_rollout_ref.actor.fsdp_config.optimizer_offload | 效率 | 把参数、梯度或优化器状态卸载到 CPU/主机侧，降低 HBM 压力但可能拖慢训练。 | False, True | 机制推断：主要改变显存、通信、计算图或调度开销之间的取舍。 | 通常不直接影响精度，除非通过性能瓶颈、数据口径或训练稳定性间接影响。 | 42 |
| actor_rollout_ref.ref.fsdp_config.param_offload | 效率 | 把参数、梯度或优化器状态卸载到 CPU/主机侧，降低 HBM 压力但可能拖慢训练。 | False, True | 机制推断：主要改变显存、通信、计算图或调度开销之间的取舍。 | 通常不直接影响精度，除非通过性能瓶颈、数据口径或训练稳定性间接影响。 | 40 |
| actor_rollout_ref.rollout.log_prob_micro_batch_size_per_gpu | 效率 | 控制 PPO/反向传播分块大小，是显存占用和 step 时间的核心旋钮。 | 1, 10, 16, 2 | 机制推断：增大通常提高有效吞吐或样本量，但会增加显存和单步时间。 | 机制推断：影响优化动态、稳定性和收敛速度。 | 39 |
| actor_rollout_ref.actor.ppo_micro_batch_size_per_gpu | 效率 | 控制 PPO/反向传播分块大小，是显存占用和 step 时间的核心旋钮。 | $ppo_micro_batch_size_per_gpu", 1, 10, 16 | 机制推断：增大通常提高有效吞吐或样本量，但会增加显存和单步时间。 | 机制推断：影响优化动态、稳定性和收敛速度。 | 37 |
| trainer.val_before_train | 配置 | 文档说明：控制训练开始前是否先跑一次验证，用于获得 baseline checkpoint/初始指标；examples 中常按是否需要起始评测设为 True 或 False。 | False, True | 文档说明：设为 True 会在第一个训练 step 前额外执行一次验证，增加启动时间和一次评测的 rollout/推理开销。 | 机制推断：不改变训练目标；但初始验证能暴露数据、reward 或采样配置问题，间接降低误跑风险。 | 36 |
| KL_LOSS_COEF | 算法 | 文档说明：对应 `actor_rollout_ref.actor.kl_loss_coef`，是在 actor loss 中加入 KL loss 时使用的权重，用于约束当前策略不要过度偏离参考策略。 | 0.001, 0.01 | 机制推断：主要增加 KL loss 聚合/反传中的少量计算；真正的额外开销通常来自是否需要 reference log-prob，而不是系数本身。 | 文档说明：较大系数会抑制 reward hacking、降低策略漂移但减少探索；过小可能让策略更快偏离参考模型，稳定性风险上升。 | 32 |
| actor_rollout_ref.ref.log_prob_micro_batch_size_per_gpu | 效率 | 控制 PPO/反向传播分块大小，是显存占用和 step 时间的核心旋钮。 | 1, 16, 2, 20 | 机制推断：增大通常提高有效吞吐或样本量，但会增加显存和单步时间。 | 机制推断：影响优化动态、稳定性和收敛速度。 | 31 |
| TRAIN_FILE | 配置 | 指定训练/验证数据来源，影响任务分布、评测口径和数据加载路径。 | "${RAY_DATA_HOME, "${WORK_DIR, "data/train.parquet", $HOME/data/dapo-math-17k/train.parquet | 通常不直接影响计算性能；保存、评测或日志频率可能影响端到端耗时。 | 通常不直接影响精度，除非通过性能瓶颈、数据口径或训练稳定性间接影响。 | 27 |
| actor_rollout_ref.actor.megatron.pipeline_model_parallel_size | 效率 | 控制流水并行切分度，降低单卡层数和激活压力，但会引入 pipeline bubble。 | $PP, 1, 12, 16 | 机制推断：主要改变显存、通信、计算图或调度开销之间的取舍。 | 通常不直接影响精度，除非通过性能瓶颈、数据口径或训练稳定性间接影响。 | 26 |
| actor_rollout_ref.actor.megatron.tensor_model_parallel_size | 效率 | 控制张量并行切分度，降低单卡权重/KV 压力，但会增加层内通信。 | $TP, 1, 2, 4 | 机制推断：主要改变显存、通信、计算图或调度开销之间的取舍。 | 通常不直接影响精度，除非通过性能瓶颈、数据口径或训练稳定性间接影响。 | 26 |
| trainer.default_local_dir | 配置 | 文档说明：本地 checkpoint 保存根目录；官方 checkpoint 文档说明它通常作为 `checkpoints/${trainer.project_name}/${trainer.experiment_name}` 前缀，下面按 global step 保存 actor/critic 分片、优化器和额外状态。 | "${CKPTS_DIR}", "${CKPT_HOME}", "${RAY_DATA_HOME, "${ckpts_home}" | 机制推断：目录本身不影响计算吞吐，但落在慢盘/网络盘会放大 save/load checkpoint 的 I/O 时间；路径不可写会导致保存失败。 | 机制推断：不直接影响精度；但可靠的 checkpoint 路径影响故障恢复和继续训练的一致性。 | 25 |
| actor_rollout_ref.rollout.enable_chunked_prefill | 效率 | 文档说明：vLLM rollout 的 chunked prefill 开关，将长 prompt 的 prefill 拆块调度；官方 Best Practices 标注为 vLLM 专用，通常与 `max_num_batched_tokens` 一起调。 | False, True | 文档说明：启用可提升 GPU 利用率，尤其是长上下文/大 batch rollout；但 batch token 上限过小或后端不匹配时可能带来额外调度开销。 | 机制推断：只改变 prefill 调度方式，不改变采样分布；若引发 OOM 或后端兼容问题才会间接影响训练/评测完成度。 | 24 |
| actor_rollout_ref.rollout.enforce_eager | 效率 | 文档说明：vLLM rollout 是否强制 eager 执行；设为 False 时可使用 CUDA graph，设为 True 时禁用 CUDA graph，通常用于显存紧张或图捕获不稳定场景。 | False, True | 文档说明：`enforce_eager=False` 允许 CUDA graph 提速但会占用额外显存；`True` 可缓解显存压力但通常牺牲部分推理吞吐。 | 机制推断：不改变模型权重或采样参数；但图模式/显存配置导致的 OOM、回退或 batch 变小会间接影响运行稳定性。 | 23 |
| actor_rollout_ref.ref.megatron.pipeline_model_parallel_size | 效率 | 控制流水并行切分度，降低单卡层数和激活压力，但会引入 pipeline bubble。 | ${actor_pp, 1, 12, 16 | 机制推断：主要改变显存、通信、计算图或调度开销之间的取舍。 | 通常不直接影响精度，除非通过性能瓶颈、数据口径或训练稳定性间接影响。 | 21 |
| actor_rollout_ref.ref.megatron.tensor_model_parallel_size | 效率 | 控制张量并行切分度，降低单卡权重/KV 压力，但会增加层内通信。 | ${actor_tp, 1, 2, 4 | 机制推断：主要改变显存、通信、计算图或调度开销之间的取舍。 | 通常不直接影响精度，除非通过性能瓶颈、数据口径或训练稳定性间接影响。 | 21 |
| TEST_FILE | 配置 | 指定训练/验证数据来源，影响任务分布、评测口径和数据加载路径。 | "${RAY_DATA_HOME, "${WORK_DIR, "data/test.parquet", $HOME/data/geo3k/test.parquet | 通常不直接影响计算性能；保存、评测或日志频率可能影响端到端耗时。 | 通常不直接影响精度，除非通过性能瓶颈、数据口径或训练稳定性间接影响。 | 21 |
| actor_rollout_ref.actor.megatron.optimizer_offload | 效率 | 把参数、梯度或优化器状态卸载到 CPU/主机侧，降低 HBM 压力但可能拖慢训练。 | False, True | 机制推断：主要改变显存、通信、计算图或调度开销之间的取舍。 | 通常不直接影响精度，除非通过性能瓶颈、数据口径或训练稳定性间接影响。 | 19 |
| actor_rollout_ref.actor.megatron.param_offload | 效率 | 把参数、梯度或优化器状态卸载到 CPU/主机侧，降低 HBM 压力但可能拖慢训练。 | False, True | 机制推断：主要改变显存、通信、计算图或调度开销之间的取舍。 | 通常不直接影响精度，除非通过性能瓶颈、数据口径或训练稳定性间接影响。 | 19 |
| actor_rollout_ref.actor.megatron.use_mbridge | 效率 | 文档说明：Megatron 训练后端的 mbridge 格式转换开关；官方 Best Practices 说明 Megatron 训练模型需要启用 mbridge，当前必须为 True。 | True | 机制推断：主要影响模型初始化、权重格式转换与 checkpoint/HF 互转流程；训练 step 吞吐通常由并行配置和 offload 主导。 | 机制推断：不应改变数值目标；若格式转换版本不匹配或权重映射错误，会造成加载失败或数值不一致风险。 | 19 |
| algorithm.kl_ctrl.kl_coef | 算法 | 文档说明：in-reward KL penalty 的初始/固定系数，属于 `algorithm.kl_ctrl`，用于把策略相对参考模型的 KL 惩罚加入 reward，而不是 actor loss。 | $kl_coef, 0.0, 0.001 | 机制推断：系数本身几乎不影响吞吐；若启用 in-reward KL，需要计算 reference/old policy log-prob，相关 forward 才是主要开销。 | 文档说明：调大表示更强惩罚策略偏离，通常提升稳定性但压缩探索和奖励优化空间；调小则相反。 | 19 |
| ACTOR_PP | 效率 | 机制推断：用户侧环境变量，通常映射到 `actor_rollout_ref.actor.megatron.pipeline_model_parallel_size` 和 ref 侧同名配置，表示 actor Megatron pipeline parallel 的切分段数。 | 1, 12, 2, 3 | 文档说明：Megatron PP/TP/EP/ETP/CP 需要按显存、网络拓扑和通信成本平衡；PP 增大可降低单卡层数/显存，但会引入 pipeline bubble 和跨阶段通信。 | 机制推断：并行切分不改变目标函数；错误切分或与 ref/critic 不一致可能导致加载失败、数值差异或训练中断。 | 18 |
| ACTOR_TP | 效率 | 机制推断：用户侧环境变量，通常映射到 `actor_rollout_ref.actor.megatron.tensor_model_parallel_size` 和 ref 侧同名配置，表示 actor Megatron tensor parallel 的切分度。 | 2, 4, 8 | 文档说明：TP 增大可降低每卡参数/激活压力，但会增加张量并行通信；官方建议与 PP/EP/ETP/CP 和硬件拓扑一起平衡。 | 机制推断：并行切分不改变优化目标；若与 checkpoint、ref 或 rollout 并行配置不匹配，可能造成加载失败或数值一致性风险。 | 18 |
| actor_rollout_ref.actor.megatron.grad_offload | 效率 | 把参数、梯度或优化器状态卸载到 CPU/主机侧，降低 HBM 压力但可能拖慢训练。 | False, True | 机制推断：主要改变显存、通信、计算图或调度开销之间的取舍。 | 通常不直接影响精度，除非通过性能瓶颈、数据口径或训练稳定性间接影响。 | 18 |

## 输出文件

- `process/example-parameter-features.json`：扫描过程与全量结构化结果。
- `process/example-parameter-features.md`：本报告。
- `lists/*.md`：每个参数的同名解释文件。
- `verl-example-parameters.xlsx`：最终 Excel 汇总。
