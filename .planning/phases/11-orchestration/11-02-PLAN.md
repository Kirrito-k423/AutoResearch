---
phase: 11-orchestration
plan: 02
status: completed
requirements:
  - ORCH-RUN-01
  - ORCH-RUN-02
  - ORCH-LOG-01
---

# Phase 11 Plan 02: `autoresearch run smoke`

## Scope

新增 `autoresearch run smoke --server X`，执行 collect -> report。

## Tasks

1. 新增 `autoresearch/orchestrator/smoke.py`
   - 调 `run_collect`
   - 成功后调 `run_render`
   - 失败时写 `failed_step`、`diagnosis`、step details

2. CLI 接线
   - `autoresearch run smoke --server X --lib verl --timeout 60 --pushgateway-url ... [--open]`
   - 支持 `--run-id`、`--workdir`、`--config`

3. Tests
   - collect 成功 + report 成功
   - collect 失败时 report skipped
   - report 失败时 failed_step=report
   - CLI stdout 单 JSON

4. Real UAT
   - 在 A2-AK-225 上跑 `--lib verl`
   - 验证 report.html 生成

## Verification

```bash
uv run pytest tests/test_orchestrator_smoke.py -q
uv run autoresearch run smoke --server A2-AK-225 --lib verl --timeout 60
```
