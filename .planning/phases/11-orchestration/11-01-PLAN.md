---
phase: 11-orchestration
plan: 01
status: completed
requirements:
  - ORCH-CHECK-01
  - ORCH-CHECK-02
  - ORCH-LOG-01
---

# Phase 11 Plan 01: `autoresearch check all`

## Scope

新增 `autoresearch check all`，输出统一 step report。

## Tasks

1. 新增 `autoresearch/orchestrator/models.py`
   - 定义 step payload 结构
   - 统一 `ok`、`status`、`exit_code`、`diagnosis`

2. 新增 `autoresearch/orchestrator/checks.py`
   - 实现 `run_check_all(...)`
   - 顺序执行 config/services/hw/net/reach/stack readiness steps
   - data-collection/report 作为 readiness placeholders，明确不执行完整实验

3. CLI 接线
   - `autoresearch check all --server X --config ... --stack-lib verl --remote-proxy-port 17892`
   - stdout 唯一 JSON
   - stderr progress stages: `orch.check.start`、`orch.check.step`、`orch.check.result`

4. Tests
   - happy path
   - downstream failure produces `failed_step`
   - CLI outputs valid single JSON

## Verification

```bash
uv run pytest tests/test_orchestrator_check.py -q
uv run autoresearch check all --server A2-AK-225 --stack-lib verl
```
