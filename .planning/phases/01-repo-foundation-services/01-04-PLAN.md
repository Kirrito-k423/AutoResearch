---
phase: 01-repo-foundation-services
plan: 04
type: execute
wave: 1
depends_on: []
source: gap_closure
gap_source: .planning/phases/01-repo-foundation-services/01-UAT.md
files_modified:
  - autoresearch/services/start.py
  - autoresearch/services/stop.py
  - tests/test_start_stop.py
autonomous: true
requirements:
  - SVC-CHK-DEPS-01
  - SVC-CHK-START-01
  - SVC-CHK-STOP-01
user_setup: []
must_haves:
  truths:
    - "autoresearch services start 缺 docker 时, --lang en 返英文错误 ('docker' + 英文 token)"
    - "autoresearch services start 缺 docker 时, 默认中文错误仍然正确 (regression)"
    - "autoresearch services stop 缺 docker 时, --lang en 返英文错误"
    - "autoresearch services stop 缺 docker 时, 默认中文错误仍然正确 (regression)"
    - "现有 11 个 phase 1 单测全部仍然 PASS"
  artifacts:
    - path: autoresearch/services/start.py
      must_exist: true
      must_contain: "def _check_docker(lang: str)"
    - path: autoresearch/services/stop.py
      must_exist: true
      must_contain: "def _check_docker(lang: str)"
  key_links:
    - from: autoresearch/services/start.py
      to: autoresearch/cli.py
      via: "cli.services_start 把 --lang 传给 run_start (already wired)"

gap_diagnosis:
  test: 10
  severity: major
  reported: "PATH=/usr/bin:/bin .venv/bin/python -m autoresearch services start --lang en → 仍输出中文『错误：找不到 `docker` 命令。请先安装 Docker Desktop（macOS）。』; --lang en 没切到错误文案"
  root_cause: "autoresearch/services/start.py:11 _check_docker() 不接 lang, 写死中文; autoresearch/services/stop.py:11 同样问题; start.py:48 except FileNotFoundError 也写死中文"
  fix_scope: "2 文件, 4 处修改: start.py + stop.py 各 1 处 _check_docker 签名 + 各 1 处字符串条件分支"
---

<objective>
Fix UAT phase 1 Test 10 issue: `autoresearch services start --lang en` 和 `autoresearch services stop --lang en` 在缺 docker 时没切英文错误文案. 跟 01-03 SUMMARY 声明的"全 3 子命令统一 --lang en 切英文"保持一致.

Root cause: `autoresearch/services/{start,stop}.py` 的 `_check_docker()` 函数不接 `lang` 参数, 写死中文错误消息; `_run_compose_up/down` 里的 `except FileNotFoundError` 同样写死中文. cli 层的 `--lang` 已经接好 (`cli.py:52` 和 `cli.py:68`), 问题在 service 层没消费.

Out of scope: 跑真 docker / 修其它子命令 (status 已经正确支持 --lang). 不动 cli.py, 不动 11 个现有单测.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-repo-foundation-services/01-03-SUMMARY.md (line: "default zh, --lang en 切英文 — 全 3 子命令统一")
@.planning/phases/01-repo-foundation-services/01-UAT.md (Test 10 issue + Gaps section)
@.planning/phases/01-repo-foundation-services/01-04-SUMMARY.md (this plan)
@autoresearch/services/start.py
@autoresearch/services/stop.py
@autoresearch/cli.py
@tests/test_start_stop.py
</context>

<tasks>

<task type="auto">
  <name>Task 4.1: 修 autoresearch/services/start.py (2 处)</name>
  <files>
    - autoresearch/services/start.py
  </files>
  <read_first>
    - autoresearch/services/start.py
  </read_first>
  <action>
两处修改:

1. `_check_docker()` 签名加 lang 参数, 函数体按 lang 返中/英:

```python
def _check_docker(lang: str) -> str | None:
    """检查 docker CLI 是否可用; 返回错误消息 (按 lang 切中/英) 或 None."""
    if shutil.which("docker") is None:
        return (
            "错误：找不到 `docker` 命令。请先安装 Docker Desktop（macOS）。"
            if lang == "zh"
            else "Error: `docker` command not found. Please install Docker Desktop (macOS)."
        )
    return None
```

2. `run_start(*, lang: str)` 里调用点改为 `err = _check_docker(lang)`.

3. `_run_compose_up(svc, lang)` 里 `except FileNotFoundError` 改条件分支:

```python
except FileNotFoundError:
    msg = "错误：找不到 docker 命令。" if lang == "zh" else "Error: docker command not found."
    print(msg, file=sys.stderr)
    return False
```
  </action>
  <verify>
```bash
cd /Users/Zhuanz/Documents/autoresearch
test -f autoresearch/services/start.py
grep -q "def _check_docker(lang: str)" autoresearch/services/start.py
grep -q "Error: \`docker\` command not found" autoresearch/services/start.py
.venv/bin/python -c "from autoresearch.services.start import _check_docker; print(_check_docker('zh')); print(_check_docker('en'))"
```
  </verify>
  <acceptance_criteria>
    - start.py _check_docker 签名接 lang
    - 中/英两条字符串都在
    - 现有 11 个 phase 1 单测仍然 PASS
  </acceptance_criteria>
  <done>start.py 修复</done>
</task>

<task type="auto">
  <name>Task 4.2: 修 autoresearch/services/stop.py (2 处, 跟 start.py 对称)</name>
  <files>
    - autoresearch/services/stop.py
  </files>
  <read_first>
    - autoresearch/services/stop.py
  </read_first>
  <action>
与 Task 4.1 对称:

1. `_check_docker()` 签名加 lang, 字符串条件分支:

```python
def _check_docker(lang: str) -> str | None:
    """检查 docker CLI 是否可用; 返回错误消息 (按 lang 切中/英) 或 None."""
    if shutil.which("docker") is None:
        return (
            "错误：找不到 `docker` 命令。"
            if lang == "zh"
            else "Error: `docker` command not found."
        )
    return None
```

2. `run_stop(*, lang: str)` 调用点改为 `err = _check_docker(lang)`.

3. `_run_compose_down` 里 `except FileNotFoundError` 改条件分支:

```python
except FileNotFoundError:
    msg = "错误：找不到 docker 命令。" if lang == "zh" else "Error: docker command not found."
    print(msg, file=sys.stderr)
    return False
```
  </action>
  <verify>
```bash
cd /Users/Zhuanz/Documents/autoresearch
test -f autoresearch/services/stop.py
grep -q "def _check_docker(lang: str)" autoresearch/services/stop.py
grep -q "Error: \`docker\` command not found" autoresearch/services/stop.py
.venv/bin/python -c "from autoresearch.services.stop import _check_docker; print(_check_docker('zh')); print(_check_docker('en'))"
```
  </verify>
  <acceptance_criteria>
    - stop.py _check_docker 签名接 lang
    - 中/英两条字符串都在
  </acceptance_criteria>
  <done>stop.py 修复</done>
</task>

<task type="auto">
  <name>Task 4.3: 加 2 个单测 (start/stop --lang en 切英文错误)</name>
  <files>
    - tests/test_start_stop.py
  </files>
  <read_first>
    - tests/test_start_stop.py (现有 11 测试模式参考)
    - autoresearch/services/start.py
    - autoresearch/services/stop.py
  </read_first>
  <action>
在 tests/test_start_stop.py 末尾追加 2 个测试, 沿用 phase 1 的 CliRunner 模式:

```python
def test_start_missing_docker_lang_en_returns_english_error():
    """D-04: --lang en 切英文错误 (start)."""
    runner = CliRunner()
    # 屏蔽 docker 模拟"没装"
    with runner.isolated_filesystem():
        result = runner.invoke(
            main, ["services", "start", "--lang", "en"],
            env={"PATH": "/usr/bin:/bin", "HOME": "/tmp"},
        )
    assert result.exit_code == 2
    combined = (result.output or "") + (result.stderr or "")
    # 英文 token 验证: 不能含中文字符 (粗略)
    assert "Error" in combined or "docker" in combined
    # 反向断言: 默认中文 "错误：" 字符串不在
    assert "错误" not in combined


def test_stop_missing_docker_lang_en_returns_english_error():
    """D-04: --lang en 切英文错误 (stop)."""
    runner = CliRunner()
    with runner.isolated_filesystem():
        result = runner.invoke(
            main, ["services", "stop", "--lang", "en"],
            env={"PATH": "/usr/bin:/bin", "HOME": "/tmp"},
        )
    assert result.exit_code == 2
    combined = (result.output or "") + (result.stderr or "")
    assert "Error" in combined or "docker" in combined
    assert "错误" not in combined
```

注意:
- CliRunner 8.2+ 接受 `env=` 关键字参数
- `env={"PATH": "/usr/bin:/bin"}` 屏蔽 docker (前提 docker 不在 /usr/bin 或 /bin)
- "错误" 不在 combined 是关键断言 (确认真的切了英文)
  </action>
  <verify>
```bash
cd /Users/Zhuanz/Documents/autoresearch
test -f tests/test_start_stop.py
grep -q "test_start_missing_docker_lang_en_returns_english_error" tests/test_start_stop.py
grep -q "test_stop_missing_docker_lang_en_returns_english_error" tests/test_start_stop.py
.venv/bin/python -m pytest tests/test_start_stop.py -v 2>&1 | tail -8
```
  </verify>
  <acceptance_criteria>
    - 2 新测试存在 + PASS
    - 现有 11 测试仍 PASS (regression)
  </acceptance_criteria>
  <done>2 测试就位, 现有 11 仍 PASS</done>
</task>

<task type="auto">
  <name>Task 4.4: git add + commit (1 atomic commit, 修 start/stop + 加 2 测试)</name>
  <files>
    - autoresearch/services/start.py
    - autoresearch/services/stop.py
    - tests/test_start_stop.py
    - .planning/phases/01-repo-foundation-services/01-04-SUMMARY.md
  </files>
  <read_first>
    - .gitignore
  </read_first>
  <action>
1. 跑全套 pytest 确认 regression 0
2. 写 01-04-SUMMARY.md (沿用 01-01 格式, 加 deviation 段说明)
3. commit

```bash
cd /Users/Zhuanz/Documents/autoresearch
.venv/bin/python -m pytest tests/ 2>&1 | tail -3  # 期望 81 passed (11 phase 1 + 28 phase 2-3 + 8 ping + 2 new)
# 写 01-04-SUMMARY.md (DI-01..02: lang 修复 + 测试模式)
git add autoresearch/services/start.py autoresearch/services/stop.py tests/test_start_stop.py .planning/phases/01-repo-foundation-services/01-04-SUMMARY.md
git status
git commit -m "fix(01): services start/stop --lang en 切英文错误文案 (D-04)

- start.py _check_docker(lang) + except FileNotFoundError 条件分支
- stop.py 同上 (对称)
- 2 测试: test_start/stop_missing_docker_lang_en_returns_english_error
- Closes 01-UAT Test 10 issue (major)

跟 01-03 SUMMARY 声明的'全 3 子命令统一 --lang en 切英文'保持一致.
Fix scope: 2 文件 + 4 处修改 + 2 测试 = 1 atomic commit."
```
  </action>
  <verify>
```bash
cd /Users/Zhuanz/Documents/autoresearch
git log -1 --format="%h %s" | grep -q "fix(01): services start/stop"
git show --stat HEAD | grep -E "start.py|stop.py|test_start_stop" | wc -l
.venv/bin/python -m pytest tests/ 2>&1 | tail -3
```
  </verify>
  <acceptance_criteria>
    - 1 atomic commit
    - 81 tests PASS (43 phase 1+2 之前 + 28 phase 2-03 + 8 ping + 2 new)
    - UAT Test 10 issue 关闭
  </acceptance_criteria>
  <done>phase 1 gap closure commit 完成</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] start.py + stop.py _check_docker 接 lang 参数
- [ ] 2 测试 PASS (start/stop --lang en 切英文)
- [ ] 现有 79 tests 仍 PASS (regression 0)
- [ ] 累计 81 tests PASS
- [ ] UAT Test 10 issue 关闭
</verification>

<success_criteria>
- All 4 tasks completed
- `autoresearch services start --lang en` 缺 docker 返英文 (跟 01-03 SUMMARY 声明对齐)
- `autoresearch services stop --lang en` 缺 docker 返英文 (同上)
- 累计 81 pytest PASS
- UAT.md Test 10 result 从 issue → pass (后续可重跑 gsd-verify-work 1 二次验收)
</success_criteria>

<output>
After completion, create `.planning/phases/01-repo-foundation-services/01-04-SUMMARY.md`
</output>
