---
phase: 01-repo-foundation-services
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - services/README.md
  - services/archon/README.md
  - services/wandb/compose.yml
  - services/wandb/README.md
  - services/prometheus/compose.yml
  - services/prometheus/prometheus.yml
  - services/prometheus/README.md
  - services/grafana/compose.yml
  - services/grafana/datasources.yml
  - services/grafana/README.md
  - .env.example
autonomous: true
requirements:
  - SVC-01
  - SVC-02
  - SVC-03
  - SVC-04
user_setup:
  - "Install Archon CLI (brew install archon) — Archon 不在我们的 compose 里, 用户需单独装并运行 archon serve"
  - "前置条件: https://archon.diy/ 查看安装文档"
must_haves:
  truths:
    - "services/README.md 存在，描述 4 服务概览和端口"
    - "services/archon/README.md 存在，指向 archon.diy + `archon serve`"
    - "services/wandb/compose.yml 存在，端口 8080，image wandb/local:0.17.5"
    - "services/prometheus/compose.yml 存在，端口 9090，挂载 prometheus.yml，self-scrape"
    - "services/grafana/compose.yml 存在，端口 3000，挂载 datasources.yml，prom + wandb 两个 datasource"
    - ".env.example 存在，列 4 个服务的端口变量（PORT_WANDB / PORT_PROMETHEUS / PORT_GRAFANA / PORT_ARCHON）"
    - "`docker compose -f services/wandb/compose.yml config -q` 验证通过"
    - "`docker compose -f services/prometheus/compose.yml config -q` 验证通过"
    - "`docker compose -f services/grafana/compose.yml config -q` 验证通过"
  artifacts:
    - path: services/README.md
      must_exist: true
    - path: services/archon/README.md
      must_exist: true
    - path: services/wandb/compose.yml
      must_exist: true
      must_contain: "wandb/local"
    - path: services/prometheus/compose.yml
      must_exist: true
      must_contain: "prom/prometheus"
    - path: services/prometheus/prometheus.yml
      must_exist: true
      must_contain: "self-scrape"
    - path: services/grafana/compose.yml
      must_exist: true
      must_contain: "grafana/grafana"
    - path: services/grafana/datasources.yml
      must_exist: true
      must_contain: "host.docker.internal"
    - path: .env.example
      must_exist: true
  key_links:
    - from: services/grafana/datasources.yml
      to: services/prometheus
      via: "http://prometheus:9090"
    - from: services/grafana/datasources.yml
      to: services/wandb
      via: "http://host.docker.internal:8080"
    - from: services/prometheus/prometheus.yml
      to: prometheus container
      via: "self-scrape localhost:9090"
---

<objective>
Build the local services stack: 4 isolated `docker-compose.yml` files (wandb / prometheus / grafana / archon-stub) + per-service README + .env.example. Archon 走 README 指向 archon.diy，**不**在我们的 compose 里。满足 SVC-01..04。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-repo-foundation-services/01-CONTEXT.md
@.planning/phases/01-repo-foundation-services/01-RESEARCH.md
@.planning/REQUIREMENTS.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 2.1: 写 services/README.md (4 服务总览)</name>
  <files>services/README.md</files>
  <read_first>
    - .planning/phases/01-repo-foundation-services/01-CONTEXT.md (D-03a, D-03c, D-05)
    - .planning/phases/01-repo-foundation-services/01-RESEARCH.md
  </read_first>
  <action>
创建 `services/README.md`，包含 4 服务总览 + 启动顺序 + 端口矩阵 + 已知约束。完整内容如下：

```markdown
# AutoResearch 本地服务栈

> 4 个独立 Docker Compose 服务；Archon 单独管理（不在我们的 compose 里）。

## 服务总览

| 服务 | 端口 | 镜像 | compose 文件 | 启动方式 |
|------|------|------|--------------|----------|
| **Archon** | 8088 | (本地 CLI) | — | `archon serve`（用户自管） |
| **wandb (local)** | 8080 | `wandb/local:0.17.5` | `wandb/compose.yml` | autoresearch services start |
| **Prometheus** | 9090 | `prom/prometheus:v2.51.0` | `prometheus/compose.yml` | autoresearch services start |
| **Grafana** | 3000 | `grafana/grafana:10.4.0` | `grafana/compose.yml` | autoresearch services start |

## 启动顺序

1. **前置**：先装 `archon` CLI（见 [archon/README.md](archon/README.md)），再 `archon serve &`
2. 启动 3 个 docker 服务：
   ```bash
   autoresearch services start
   # 或手动：
   docker compose -f services/wandb/compose.yml up -d
   docker compose -f services/prometheus/compose.yml up -d
   docker compose -f services/grafana/compose.yml up -d
   ```
3. 验证全绿：
   ```bash
   autoresearch services status
   ```

## 端口冲突处理

如果 8080/9090/3000 任一被占，编辑 `.env`（基于 `.env.example`）改端口变量，compose 用 `${VAR}` 引用。

## 已知约束
- Archon 不在 compose 里（D-05 锁定）
- 端口 wandb 8080 / Prom 9090 / Grafana 3000 / Archon 8088 **固定**（D-03c）
- `host.docker.internal` 是 Mac/Windows Docker Desktop 默认；Linux 需要 `extra_hosts`
- 本地优先：所有数据落本机 Mac 容器 volume，远程 NPU 服务器不涉及
```

  </action>
  <verify>
```bash
cd /Users/Zhuanz/Documents/autoresearch
test -f services/README.md
grep -q "wandb" services/README.md
grep -q "Prometheus" services/README.md
grep -q "Grafana" services/README.md
grep -q "Archon" services/README.md
grep -q "8088" services/README.md
```
  </verify>
  <acceptance_criteria>
    - `services/README.md` 文件存在
    - 表格列出 4 个服务及端口
    - 提到 Archon 由 `archon serve` 管理
  </acceptance_criteria>
  <done>services/README.md 含 4 服务总览 + 端口矩阵</done>
</task>

<task type="auto">
  <name>Task 2.2: 写 services/archon/README.md (D-05 — Archon 不在 compose)</name>
  <files>services/archon/README.md</files>
  <read_first>
    - .planning/phases/01-repo-foundation-services/01-CONTEXT.md (D-05a, D-05c, D-05d)
  </read_first>
  <action>
创建 `services/archon/README.md`。**关键**：`services/archon/` 目录**不**包含 `compose.yml`；只有 README 指向外部安装。完整内容：

```markdown
# Archon (本目录为占位说明)

> **Archon 不在 autoresearch 的 docker-compose 里**（D-05 锁定决策）。
> 本目录存在仅为标记服务位置 + 文档化外部安装步骤。

## 为什么不在 compose 里

Archon 是 MIT 工作流编排器（[coleam00/Archon](https://github.com/coleam00/Archon)），有自己的 CLI 工具链。
把它塞进我们的 compose 会导致：
- 与我们 8 skill 边界混乱（Archon 期望自家工作流 YAML）
- 镜像版本/升级节奏与我们的需求脱节
- 跨机器迁移时 Archon 配置丢失风险

## 安装 Archon（前置条件）

Archon 不在我们的 `docker-compose.yml` 里，由用户自行安装：

```bash
# macOS
brew install archon

# 或从源码
git clone https://github.com/coleam00/Archon.git
cd Archon
pip install -e .
```

详见官方安装指南：https://archon.diy/

## 启动 Archon

```bash
archon serve &     # 默认监听 :8088
# 或前台运行（看 log 方便调试）
archon serve
```

启动后验证：
```bash
curl http://localhost:8088/healthz
# 期望返回 200 OK
```

## autoresearch 与 Archon 的关系

- `autoresearch services status` **会**检查 Archon 的 `/healthz`（端口 8088）
- `autoresearch services start` **不会**启 Archon；只会起 wandb / prometheus / grafana 3 个
- Archon 安装是进入 Phase 1 之前的**前置条件**（README.md quickstart 写明）

## 故障排查

| 现象 | 原因 | 修复 |
|------|------|------|
| `archon: command not found` | 未安装 | 按上面"安装 Archon"步骤操作 |
| `/healthz` 404 | Archon 启动失败 | 看 `archon serve` 的 stderr；常见是端口 8088 被占 |
| 端口 8088 被占 | 其他应用占 | `lsof -i :8088` 找占端口进程；或改 Archon 端口 + `services/_common.py` 同步 |
```

  </action>
  <verify>
```bash
cd /Users/Zhuanz/Documents/autoresearch
test -f services/archon/README.md
# 关键：此目录**不**应有 compose.yml
test ! -f services/archon/compose.yml && echo "OK: archon has no compose.yml (D-05)"
grep -q "Archon 不在" services/archon/README.md
grep -q "archon.diy\|archon serve" services/archon/README.md
```
  </verify>
  <acceptance_criteria>
    - `services/archon/README.md` 文件存在
    - `services/archon/compose.yml` **不**存在（D-05 锁定）
    - README 含 "Archon 不在" 字样
    - README 含 `archon serve` 命令
  </acceptance_criteria>
  <done>services/archon/ 只有 README，Archon 走外部 install</done>
</task>

<task type="auto">
  <name>Task 2.3: 写 services/wandb/compose.yml + README</name>
  <files>
    - services/wandb/compose.yml
    - services/wandb/README.md
  </files>
  <read_first>
    - .planning/phases/01-repo-foundation-services/01-CONTEXT.md (D-03c, D-03a)
    - .planning/phases/01-repo-foundation-services/01-RESEARCH.md (Standard Stack: wandb)
  </read_first>
  <action>
**Task 2.3a**: 创建 `services/wandb/compose.yml`：

```yaml
# services/wandb/compose.yml
# 用途: 本地 wandb 服务（offline 模式可被 autoresearch collect 消费）
# 端口: 8080 (固定, D-03c)

services:
  wandb:
    image: wandb/local:0.17.5
    container_name: ar-wandb
    ports:
      - "${PORT_WANDB:-8080}:8080"
    environment:
      - WANDB_BASE_URL=http://localhost:${PORT_WANDB:-8080}
    volumes:
      - wandb-data:/vol
    restart: unless-stopped

volumes:
  wandb-data:
    name: ar-wandb-data
```

**Task 2.3b**: 创建 `services/wandb/README.md`：

```markdown
# wandb (本地)

> 本地模式 wandb；M1 阶段只起服务，actual 数据采集在 Phase 9 (data-collection)。

## 启动

```bash
docker compose -f services/wandb/compose.yml up -d
# 或
autoresearch services start
```

## 验证

```bash
curl http://localhost:8080/healthz
# 期望 200 OK 或 404（wandb local 镜像可能不暴露 /healthz，需要查 README 调整）
```

> **注意**：wandb/local 镜像的 health 端点路径**可能**不是 `/healthz`。
> 如果 `/healthz` 404，请用以下命令探活：
> ```bash
> curl -I http://localhost:8080/
> ```
> 然后在 `autoresearch/services/_common.py` 把 `WANDB_HEALTH_URL` 常量改正确。
> （这是 RESEARCH.md Open Question 1 的解答路径；先按 `/healthz` 走，遇到再修。）

## 端口

默认 8080（D-03c 锁定）；可通过 `.env` 里 `PORT_WANDB=8081` 覆盖。

## 数据持久化

`wandb-data` named volume，docker volume inspect ar-wandb-data 查看位置。
```

  </action>
  <verify>
```bash
cd /Users/Zhuanz/Documents/autoresearch
test -f services/wandb/compose.yml
test -f services/wandb/README.md
grep -q "wandb/local:0.17.5" services/wandb/compose.yml
grep -q "8080" services/wandb/compose.yml
grep -q "wandb-data" services/wandb/compose.yml
# YAML 语法验证
docker compose -f services/wandb/compose.yml config -q && echo "wandb compose.yml: YAML OK"
```
  </verify>
  <acceptance_criteria>
    - `services/wandb/compose.yml` 存在，含 `wandb/local:0.17.5` 镜像
    - 端口映射 `${PORT_WANDB:-8080}:8080`
    - 命名 volume `wandb-data` 用于数据持久化
    - `docker compose config -q` 验证 YAML 语法通过
    - `services/wandb/README.md` 存在
  </acceptance_criteria>
  <done>wandb 服务 compose + README 完成</done>
</task>

<task type="auto">
  <name>Task 2.4: 写 services/prometheus/{compose.yml, prometheus.yml, README.md}</name>
  <files>
    - services/prometheus/compose.yml
    - services/prometheus/prometheus.yml
    - services/prometheus/README.md
  </files>
  <read_first>
    - .planning/phases/01-repo-foundation-services/01-CONTEXT.md (D-03d)
    - .planning/phases/01-repo-foundation-services/01-RESEARCH.md (Pattern 3)
  </read_first>
  <action>
**Task 2.4a**: 创建 `services/prometheus/prometheus.yml`（D-03d 自监控配置）：

```yaml
# services/prometheus/prometheus.yml
# 用途: Prometheus 自监控 scrape config (D-03d)
# 容器内: localhost:9090 是 Prometheus 自己

global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: prometheus
    # self-scrape: 容器内 localhost 指容器自己
    static_configs:
      - targets: ["localhost:9090"]
        labels:
          instance: prometheus-self

  # M1 阶段只自监控；Phase 7 (REACH) 起加远程 pushgateway
```

**Task 2.4b**: 创建 `services/prometheus/compose.yml`：

```yaml
# services/prometheus/compose.yml
# 用途: 本地 Prometheus（D-03 自监控；SVC-03）
# 端口: 9090 (固定, D-03c)

services:
  prometheus:
    image: prom/prometheus:v2.51.0
    container_name: ar-prometheus
    ports:
      - "${PORT_PROMETHEUS:-9090}:9090"
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus-data:/prometheus
    command:
      - --config.file=/etc/prometheus/prometheus.yml
      - --storage.tsdb.path=/prometheus
      - --web.console.libraries=/usr/share/prometheus/console_libraries
      - --web.console.templates=/usr/share/prometheus/consoles
    restart: unless-stopped

volumes:
  prometheus-data:
    name: ar-prometheus-data
```

**Task 2.4c**: 创建 `services/prometheus/README.md`：

```markdown
# Prometheus

> 本地 Prometheus；M1 阶段只跑自监控，Phase 7+ 接 pushgateway 收远程 NPU 资源数据。

## 启动

```bash
docker compose -f services/prometheus/compose.yml up -d
# 或
autoresearch services start
```

## 验证

```bash
# 自监控端点
curl http://localhost:9090/-/healthy
# 期望: "Prometheus is Healthy."

# 自监控 scrape 状态（Web UI）
open http://localhost:9090/targets
# 期望: "prometheus" job 显示 UP

# 查 self-scrape 指标
curl 'http://localhost:9090/api/v1/query?query=up{job="prometheus"}'
# 期望: vector 含 1 个 value=1
```

## 自监控

`prometheus.yml` 里 `prometheus` job 抓取 Prometheus 自己的 `/metrics` 端点（localhost:9090）。
- 容器内 localhost 是容器自己（关键点，**不**是 `prometheus:9090`）
- 验证：Web UI Status > Targets，`prometheus` job 应显示 UP

## 数据持久化

`prometheus-data` named volume；`docker volume inspect ar-prometheus-data`。

## 端口

默认 9090（D-03c）；可通过 `.env` 里 `PORT_PROMETHEUS=9091` 覆盖。
```

  </action>
  <verify>
```bash
cd /Users/Zhuanz/Documents/autoresearch
test -f services/prometheus/compose.yml
test -f services/prometheus/prometheus.yml
test -f services/prometheus/README.md
grep -q "prom/prometheus:v2.51.0" services/prometheus/compose.yml
grep -q "9090" services/prometheus/compose.yml
grep -q "prometheus.yml" services/prometheus/compose.yml
grep -q "self-scrape\|self scrape\|localhost:9090" services/prometheus/prometheus.yml
grep -q "scrape_configs" services/prometheus/prometheus.yml
# YAML 验证
docker compose -f services/prometheus/compose.yml config -q && echo "prometheus compose.yml: YAML OK"
```
  </verify>
  <acceptance_criteria>
    - `services/prometheus/prometheus.yml` 存在，含 `scrape_configs` + 自监控 `localhost:9090`
    - `services/prometheus/compose.yml` 存在，含 `prom/prometheus:v2.51.0` 镜像
    - 端口映射 `${PORT_PROMETHEUS:-9090}:9090`
    - prometheus.yml 通过 volume 挂载到 `/etc/prometheus/prometheus.yml`
    - `docker compose config -q` 验证通过
  </acceptance_criteria>
  <done>Prometheus 服务（compose + config + README）完成</done>
</task>

<task type="auto">
  <name>Task 2.5: 写 services/grafana/{compose.yml, datasources.yml, README.md}</name>
  <files>
    - services/grafana/compose.yml
    - services/grafana/datasources.yml
    - services/grafana/README.md
  </files>
  <read_first>
    - .planning/phases/01-repo-foundation-services/01-CONTEXT.md (D-03e)
    - .planning/phases/01-repo-foundation-services/01-RESEARCH.md (Pattern 4 + Pitfall 1)
  </read_first>
  <action>
**Task 2.5a**: 创建 `services/grafana/datasources.yml`（D-03e 双 datasource provisioning）：

```yaml
# services/grafana/datasources.yml
# 用途: Grafana provisioning datasources（D-03e：Prometheus + 本地 wandb）
# 关键: 用 host.docker.internal 连 Mac 上的 wandb（RESEARCH Pitfall 1）

apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
    jsonData:
      timeInterval: "15s"

  - name: wandb
    type: grafana-simple-json-datasource
    access: proxy
    # 容器内 localhost 是容器自己；用 host.docker.internal 指 Mac 主机
    url: http://host.docker.internal:${PORT_WANDB:-8080}
    editable: false
```

**Task 2.5b**: 创建 `services/grafana/compose.yml`：

```yaml
# services/grafana/compose.yml
# 用途: 本地 Grafana（D-03e 预置 datasources；SVC-04）
# 端口: 3000 (固定, D-03c)

services:
  grafana:
    image: grafana/grafana:10.4.0
    container_name: ar-grafana
    ports:
      - "${PORT_GRAFANA:-3000}:3000"
    volumes:
      - ./datasources.yml:/etc/grafana/provisioning/datasources/datasources.yml:ro
      - grafana-data:/var/lib/grafana
    environment:
      - GF_SECURITY_ADMIN_USER=admin
      - GF_SECURITY_ADMIN_PASSWORD=admin
      - GF_USERS_ALLOW_SIGN_UP=false
    restart: unless-stopped

volumes:
  grafana-data:
    name: ar-grafana-data
```

**Task 2.5c**: 创建 `services/grafana/README.md`：

```markdown
# Grafana

> 本地 Grafana；M1 阶段预置 Prometheus + wandb 两个 datasource。

## 启动

```bash
docker compose -f services/grafana/compose.yml up -d
# 或
autoresearch services start
```

## 验证

```bash
# 健康检查
curl http://localhost:3000/api/health
# 期望: {"database":"ok",...}

# Web UI
open http://localhost:3000
# 默认 admin / admin（首次登录会要求改密码；可跳过）

# Datasource 验证
open http://localhost:3000/datasources
# 期望: 看到 Prometheus 和 wandb 两个；Prometheus 标 "default"
```

## Datasources

启动时通过 `datasources.yml` 自动 provisioning：
- **Prometheus** (default)：`http://prometheus:9090`（容器名服务发现）
- **wandb**：`http://host.docker.internal:8080`（Mac 主机网络）

## 关键点（RESEARCH Pitfall 1）

`host.docker.internal` 是 Docker Desktop (Mac/Windows) 提供的特殊 DNS，
让容器内能访问 Mac 主机服务。在 Linux 上需加：
```yaml
extra_hosts:
  - "host.docker.internal:host-gateway"
```
M1 阶段只面向 Mac（PROJECT.md 锁 Mac 单机），暂不处理 Linux。

## 端口

默认 3000（D-03c）；可通过 `.env` 里 `PORT_GRAFANA=3001` 覆盖。
```

  </action>
  <verify>
```bash
cd /Users/Zhuanz/Documents/autoresearch
test -f services/grafana/compose.yml
test -f services/grafana/datasources.yml
test -f services/grafana/README.md
grep -q "grafana/grafana:10.4.0" services/grafana/compose.yml
grep -q "3000" services/grafana/compose.yml
grep -q "host.docker.internal" services/grafana/datasources.yml
grep -q "http://prometheus:9090" services/grafana/datasources.yml
# YAML 验证
docker compose -f services/grafana/compose.yml config -q && echo "grafana compose.yml: YAML OK"
```
  </verify>
  <acceptance_criteria>
    - `services/grafana/datasources.yml` 存在，含 `host.docker.internal` 和 `http://prometheus:9090`
    - `services/grafana/compose.yml` 存在，含 `grafana/grafana:10.4.0`
    - 端口映射 `${PORT_GRAFANA:-3000}:3000`
    - datasources.yml 通过 volume 挂载到 provisioning 目录
    - `docker compose config -q` 验证通过
  </acceptance_criteria>
  <done>Grafana 服务（compose + datasources + README）完成</done>
</task>

<task type="auto">
  <name>Task 2.6: 写 .env.example (端口变量模板)</name>
  <files>.env.example</files>
  <read_first>
    - .planning/phases/01-repo-foundation-services/01-CONTEXT.md (D-03c)
  </read_first>
  <action>
创建 `.env.example`（**注意是 `.env.example`，不是 `.env`** — `.env` 在 .gitignore 里）：

```bash
# .env.example
# 用途: 服务端口变量模板；复制为 .env 后修改覆盖默认值
# 真实 .env 已被 .gitignore 排除；本文件可被 git 追踪

# === Archon (D-05：不在 compose，由 `archon serve` 启动) ===
PORT_ARCHON=8088

# === wandb (本地模式) ===
PORT_WANDB=8080

# === Prometheus ===
PORT_PROMETHEUS=9090

# === Grafana ===
PORT_GRAFANA=3000
```

每段加注释说明用途；不改默认端口值（D-03c 锁定），仅展示变量化结构。
  </action>
  <verify>
```bash
cd /Users/Zhuanz/Documents/autoresearch
test -f .env.example
grep -q "PORT_WANDB" .env.example
grep -q "PORT_PROMETHEUS" .env.example
grep -q "PORT_GRAFANA" .env.example
grep -q "PORT_ARCHON" .env.example
# 反向：真实 .env 不应被 git tracked
! git ls-files | grep -q "^\.env$" && echo "OK: .env not tracked (it's in .gitignore)"
```
  </verify>
  <acceptance_criteria>
    - `.env.example` 文件存在
    - 含 4 个端口变量
    - `.env`（真实文件）**不**在 git tracking 中
  </acceptance_criteria>
  <done>.env.example 模板完成</done>
</task>

<task type="auto">
  <name>Task 2.7: git add + commit (服务栈)</name>
  <files>
    - services/README.md
    - services/archon/README.md
    - services/wandb/compose.yml
    - services/wandb/README.md
    - services/prometheus/compose.yml
    - services/prometheus/prometheus.yml
    - services/prometheus/README.md
    - services/grafana/compose.yml
    - services/grafana/datasources.yml
    - services/grafana/README.md
    - .env.example
  </files>
  <read_first>
    - .gitignore
  </read_first>
  <action>
把 services/ 与 .env.example 加入 git 并提交：

```bash
cd /Users/Zhuanz/Documents/autoresearch
git add services/ .env.example
git status   # 确认 services/ 下 9 个文件 + .env.example 被 staged

git commit -m "feat(01): add local services stack (wandb/prometheus/grafana/archon-stub)

- services/README.md: 4 服务总览 + 端口矩阵 + 启动顺序
- services/archon/README.md: D-05 — Archon 走外部 install，无 compose.yml
- services/wandb/compose.yml: wandb/local:0.17.5，端口 8080
- services/prometheus/{compose.yml,prometheus.yml}: self-scrape，端口 9090
- services/grafana/{compose.yml,datasources.yml}: 预置 prometheus + wandb，端口 3000
- .env.example: 4 端口变量模板

Closes SVC-02, SVC-03, SVC-04
Refs SVC-01 (Archon 由 archon CLI 单独管理)"
```

注意：
- 验证 `git status` 没有意外 staged 文件（比如 .env 实际文件）
- commit message 提到 Closes SVC-02/03/04 + Refs SVC-01（说明 Archon 不在 compose）
  </action>
  <verify>
```bash
cd /Users/Zhuanz/Documents/autoresearch
git log -1 --format="%h %s" | grep -q "feat(01): add local services stack"
git show --stat HEAD | grep -E "services/|\.env" | wc -l   # 至少 10 行
```
  </verify>
  <acceptance_criteria>
    - 10+ 文件被 commit
    - commit message 以 "feat(01):" 开头
  </acceptance_criteria>
  <done>服务栈 commit 完成</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] 4 个服务目录（archon/wandb/prometheus/grafana）齐全
- [ ] `services/archon/compose.yml` **不**存在（D-05 锁定）
- [ ] 3 个 compose.yml 全部 `docker compose config -q` 验证通过
- [ ] Prometheus self-scrape 配置存在
- [ ] Grafana datasources 包含 prometheus + host.docker.internal
- [ ] `.env.example` 含 4 端口变量
- [ ] 真实 `.env` 不在 git tracking
</verification>

<success_criteria>
- All 7 tasks completed
- All verification checks pass
- SVC-02, SVC-03, SVC-04 满足
- SVC-01 修订为「Archon 由 `archon serve` 控制」（D-06）
- 3 个 compose.yml 全部 `docker compose config -q` 验证通过
- Archon 不在 compose 里（D-05 锁定），README 指引外部 install
</success_criteria>

<output>
After completion, create `.planning/phases/01-repo-foundation-services/01-02-SUMMARY.md`
</output>
