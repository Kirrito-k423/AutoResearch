---
phase: 01-repo-foundation-services
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - autoresearch/__init__.py
  - autoresearch/__main__.py
  - autoresearch/cli.py
  - autoresearch/services/__init__.py
  - autoresearch/services/_common.py
  - autoresearch/services/status.py
  - autoresearch/services/start.py
  - autoresearch/services/stop.py
  - tests/__init__.py
  - tests/test_cli.py
  - tests/test_status.py
  - tests/test_start_stop.py
  - uv.lock
autonomous: true
requirements:
  - SVC-CHK-STAT-01
  - SVC-CHK-STAT-02
  - SVC-CHK-STAT-03
  - SVC-CHK-START-01
  - SVC-CHK-STOP-01
  - SVC-CHK-DEPS-01
user_setup:
  - "Install uv (curl -LsSf https://astral.sh/uv/install.sh | sh) — M1 阶段 Python 包管理工具"
  - "Install Docker Desktop for Mac — services start 依赖 docker CLI"
must_haves:
  truths:
    - "`uv sync` 安装依赖后，`autoresearch --version` 输出 0.1.0"
    - "`autoresearch services status` 输出 4 行 healthz 状态表（人读）"
    - "`autoresearch services status --json` 输出唯一 JSON 对象含 services[].{name,url,healthy,latency_ms} + summary.{total,healthy,unhealthy}"
    - "`autoresearch services start` 串行调 3 个 compose up -d；Archon 提示由 `archon serve` 管理"
    - "`autoresearch services stop` 调 3 个 compose down"
    - "缺 docker 时 status/start/stop 给可读中文错误"
    - "`pytest tests/` 至少 3 个 happy path 单测通过"
    - "`autoresearch --help` 显示 services 子命令组"
  artifacts:
    - path: pyproject.toml
      must_exist: true
      must_contain: "click"
    - path: autoresearch/__init__.py
      must_exist: true
    - path: autoresearch/__main__.py
      must_exist: true
    - path: autoresearch/cli.py
      must_exist: true
      must_contain: "@click.group"
    - path: autoresearch/services/_common.py
      must_exist: true
      must_contain: "ThreadPoolExecutor"
    - path: autoresearch/services/status.py
      must_exist: true
    - path: autoresearch/services/start.py
      must_exist: true
      must_contain: "subprocess.run"
    - path: autoresearch/services/stop.py
      must_exist: true
    - path: tests/test_cli.py
      must_exist: true
    - path: tests/test_status.py
      must_exist: true
    - path: tests/test_start_stop.py
      must_exist: true
  key_links:
    - from: pyproject.toml
      to: autoresearch.cli:main
      via: "[project.scripts]"
    - from: autoresearch/cli.py
      to: autoresearch.services.{status,start,stop}
      via: "click group 子命令"
    - from: autoresearch/services/_common.py
      to: requests
      via: "ThreadPoolExecutor + requests.get"
    - from: autoresearch/services/start.py
      to: services/*/compose.yml
      via: "subprocess.run([docker, compose, -f, path, up, -d])"
---

<objective>
Build the `autoresearch` Python CLI: click 树状子命令 + `services {status, start, stop}` + 并发 healthz 探测 + docker compose 串行调度 + uv 锁包管理 + 3 个 pytest 单测。满足 SVC-CHK-STAT-01..03 / SVC-CHK-START-01 / SVC-CHK-STOP-01 / SVC-CHK-DEPS-01。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-repo-foundation-services/01-CONTEXT.md
@.planning/phases/01-repo-foundation-services/01-RESEARCH.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-repo-foundation-services/01-01-PLAN.md
@.planning/phases/01-repo-foundation-services/01-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 3.1: 初始化 pyproject.toml + uv lockfile</name>
  <files>
    - pyproject.toml
    - uv.lock
  </files>
  <read_first>
    - .planning/PROJECT.md
    - .planning/phases/01-repo-foundation-services/01-RESEARCH.md (Example 4)
  </read_first>
  <action>
**Task 3.1a**: 用 uv 初始化 Python 包：

```bash
cd /Users/Zhuanz/Documents/autoresearch
# 假设 uv 已装（user_setup 项；如未装先 curl -LsSf https://astral.sh/uv/install.sh | sh）
uv init --package autoresearch
# 这会创建 pyproject.toml + autoresearch/__init__.py + main.py（我们要替换）
```

**Task 3.1b**: 用 apply_patch 改写 `pyproject.toml` 为以下内容（**保留 uv 生成结构**，但改写 metadata + dependencies + scripts）：

```toml
[project]
name = "autoresearch"
version = "0.1.0"
description = "Local LLM training workflow platform"
readme = "README.md"
requires-python = ">=3.11"
license = {text = "MIT"}
authors = [
    {name = "AutoResearch Contributors"},
]
keywords = ["llm", "training", "workflow", "wandb", "prometheus"]
dependencies = [
    "click>=8.1",
    "requests>=2.31",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-cov>=4.1",
]

[project.scripts]
autoresearch = "autoresearch.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["autoresearch"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-v --tb=short"
```

**Task 3.1c**: 加 click + requests + pytest 依赖并生成 lockfile：

```bash
cd /Users/Zhuanz/Documents/autoresearch
uv add click requests
uv add --dev pytest pytest-cov
uv sync   # 触发 lockfile 生成
```

  </action>
  <verify>
```bash
cd /Users/Zhuanz/Documents/autoresearch
test -f pyproject.toml
test -f uv.lock
grep -q '"click' pyproject.toml
grep -q '"requests' pyproject.toml
grep -q 'autoresearch = "autoresearch.cli:main"' pyproject.toml
# 验证 uv 可 sync
uv sync --frozen 2>&1 | tail -5
```
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` 存在，含 `click` 和 `requests` 依赖
    - `[project.scripts]` 含 `autoresearch = "autoresearch.cli:main"`
    - `uv.lock` 存在
    - `uv sync --frozen` 退出码 0
  </acceptance_criteria>
  <done>Python 包初始化 + 依赖锁完成</done>
</task>

<task type="auto">
  <name>Task 3.2: 写 autoresearch 包入口 (__init__ / __main__ / cli)</name>
  <files>
    - autoresearch/__init__.py
    - autoresearch/__main__.py
    - autoresearch/cli.py
  </files>
  <read_first>
    - .planning/phases/01-repo-foundation-services/01-CONTEXT.md (D-04)
    - .planning/phases/01-repo-foundation-services/01-RESEARCH.md (Example 1)
  </read_first>
  <action>
**Task 3.2a**: 删除 uv 生成的默认 main.py：

```bash
cd /Users/Zhuanz/Documents/autoresearch
rm -f main.py
```

**Task 3.2b**: 写 `autoresearch/__init__.py`：

```python
"""autoresearch — Local LLM training workflow platform."""

__version__ = "0.1.0"
```

**Task 3.2c**: 写 `autoresearch/__main__.py`（D-04a 入口）：

```python
"""Allow running autoresearch as `python -m autoresearch`."""
from autoresearch.cli import main

if __name__ == "__main__":
    main()
```

**Task 3.2d**: 写 `autoresearch/cli.py`（D-04 + D-04b 树状子命令）：

```python
"""autoresearch CLI entry point."""
from __future__ import annotations

import click

from autoresearch import __version__


@click.group()
@click.version_option(__version__, prog_name="autoresearch")
def main() -> None:
    """autoresearch — local LLM training workflow platform.

    8 个 skill 串成最小循环 (customer-config → local-services-health → ...)，
    单二进制入口；每个 skill 挂一个 group。
    """
    pass


@main.group()
def services() -> None:
    """管理本地开发服务（Archon、wandb、Prometheus、Grafana）。"""
    pass


@services.command(name="status")
@click.option(
    "--json",
    "as_json",
    is_flag=True,
    help="Output machine-readable JSON.",
)
@click.option(
    "--lang",
    default="zh",
    type=click.Choice(["zh", "en"]),
    help="输出语言 (zh/en)。",
)
def services_status(as_json: bool, lang: str) -> None:
    """并发查询 4 个服务的 /healthz 端点。"""
    from autoresearch.services.status import run_status
    raise click.exceptions.Exit(run_status(as_json=as_json, lang=lang))


@services.command(name="start")
@click.option(
    "--lang",
    default="zh",
    type=click.Choice(["zh", "en"]),
    help="输出语言 (zh/en)。",
)
def services_start(lang: str) -> None:
    """启动 3 个 docker-compose 服务（wandb/prometheus/grafana）。

    Archon 由 `archon serve` 自行管理，不在 autoresearch 范围（D-05）。
    """
    from autoresearch.services.start import run_start
    raise click.exceptions.Exit(run_start(lang=lang))


@services.command(name="stop")
@click.option(
    "--lang",
    default="zh",
    type=click.Choice(["zh", "en"]),
    help="输出语言 (zh/en)。",
)
def services_stop(lang: str) -> None:
    """停止 3 个 docker-compose 服务。"""
    from autoresearch.services.stop import run_stop
    raise click.exceptions.Exit(run_stop(lang=lang))


if __name__ == "__main__":
    main()
```

注意：
- click `group` + `command` 装饰器；group 不带 name 表示仅做分组
- `click.exceptions.Exit(code)` 让子进程以非零状态退出（status 任一 unhealthy = exit 1）
- `--lang` 默认为 `zh`（M1 阶段默认中文，与 PROJECT.md 锁的中文用户群体一致）

  </action>
  <verify>
```bash
cd /Users/Zhuanz/Documents/autoresearch
test -f autoresearch/__init__.py
test -f autoresearch/__main__.py
test -f autoresearch/cli.py
grep -q "__version__" autoresearch/__init__.py
grep -q "from autoresearch.cli import main" autoresearch/__main__.py
grep -q "@click.group" autoresearch/cli.py
grep -q "@main.group" autoresearch/cli.py
grep -q 'def services_status' autoresearch/cli.py
grep -q 'def services_start' autoresearch/cli.py
grep -q 'def services_stop' autoresearch/cli.py
# 验证 CLI 入口能跑
uv run autoresearch --version
uv run autoresearch --help
```
  </verify>
  <acceptance_criteria>
    - 3 个文件存在
    - `autoresearch --version` 输出 `0.1.0`
    - `autoresearch --help` 显示 `services` 子命令组
    - `autoresearch services --help` 显示 `status/start/stop` 3 个子命令
  </acceptance_criteria>
  <done>CLI 入口 + 树状子命令结构完成</done>
</task>

<task type="auto">
  <name>Task 3.3: 写 autoresearch/services/_common.py (并发 healthz 探测)</name>
  <files>autoresearch/services/_common.py</files>
  <read_first>
    - .planning/phases/01-repo-foundation-services/01-RESEARCH.md (Pattern 2 + Pitfall 4)
  </read_first>
  <action>
创建 `autoresearch/services/_common.py`（4 服务 healthz 并发探测核心逻辑）：

```python
"""Common helpers for autoresearch.services subcommands.

SVC-CHK-STAT-01..03: 4 services, concurrent healthz, machine-readable JSON.
"""
from __future__ import annotations

import time
from concurrent.futures import ThreadPoolExecutor
from typing import TypedDict

import requests


class HealthResult(TypedDict):
    """Per-service health check result."""

    name: str
    url: str
    healthy: bool
    latency_ms: int
    error: str | None


# 4 services fixed (D-03c); 端口可用环境变量覆盖（默认 .env.example）
# 注意：archon 端口固定 8088（D-05 锁定，不在 compose 里）
SERVICES: list[tuple[str, str]] = [
    ("archon", "http://localhost:8088/healthz"),
    ("wandb", "http://localhost:8080/healthz"),
    ("prometheus", "http://localhost:9090/-/healthy"),
    ("grafana", "http://localhost:3000/api/health"),
]

DEFAULT_TIMEOUT_S = 3.0


def check_one(name: str, url: str, timeout: float = DEFAULT_TIMEOUT_S) -> HealthResult:
    """Probe a single service's health endpoint.

    Returns a HealthResult dict; never raises.
    """
    t0 = time.perf_counter()
    try:
        resp = requests.get(url, timeout=timeout)
        latency_ms = int((time.perf_counter() - t0) * 1000)
        return HealthResult(
            name=name,
            url=url,
            healthy=resp.ok,
            latency_ms=latency_ms,
            error=None if resp.ok else f"HTTP {resp.status_code}",
        )
    except requests.RequestException as e:
        latency_ms = int((time.perf_counter() - t0) * 1000)
        return HealthResult(
            name=name,
            url=url,
            healthy=False,
            latency_ms=latency_ms,
            error=str(e),
        )


def check_all(timeout: float = DEFAULT_TIMEOUT_S) -> list[HealthResult]:
    """Concurrently probe all 4 services.

    Uses ThreadPoolExecutor with 4 workers (one per service) — order is preserved
    via the result iterator.
    """
    with ThreadPoolExecutor(max_workers=len(SERVICES)) as ex:
        return list(ex.map(lambda s: check_one(s[0], s[1], timeout), SERVICES))
```

注意：
- 用 `ThreadPoolExecutor` 而非裸 `threading`（stdlib，自动 shutdown）
- `check_one` 永不 raise（异常被捕获填到 `error` 字段）
- `SERVICES` 常量 4 服务**固定**（D-03c 锁定），仅端口可变量
- 端口未走 `${PORT_WANDB}` 读取 — 简化 M1 阶段 01；Phase 4 增强时改读 env

  </action>
  <verify>
```bash
cd /Users/Zhuanz/Documents/autoresearch
test -f autoresearch/services/_common.py
grep -q "ThreadPoolExecutor" autoresearch/services/_common.py
grep -q "8088" autoresearch/services/_common.py
grep -q "8080" autoresearch/services/_common.py
grep -q "9090" autoresearch/services/_common.py
grep -q "3000" autoresearch/services/_common.py
# 语法验证
uv run python -c "from autoresearch.services._common import SERVICES, check_all; print(len(SERVICES))"
```
  </verify>
  <acceptance_criteria>
    - `autoresearch/services/_common.py` 存在
    - 含 `ThreadPoolExecutor`
    - 4 个端口（8088/8080/9090/3000）全部出现
    - `from autoresearch.services._common import SERVICES` 可成功
  </acceptance_criteria>
  <done>并发 healthz 探测核心完成</done>
</task>

<task type="auto">
  <name>Task 3.4: 写 autoresearch/services/status.py (status 子命令实现)</name>
  <files>autoresearch/services/status.py</files>
  <read_first>
    - .planning/phases/01-repo-foundation-services/01-RESEARCH.md (Example 2)
  </read_first>
  <action>
创建 `autoresearch/services/status.py`：

```python
"""autoresearch services status — 并发查 4 服务 healthz。

实现 SVC-CHK-STAT-01 (并发)、SVC-CHK-STAT-02 (字段)、
SVC-CHK-STAT-03 (--json 机读输出)。
"""
from __future__ import annotations

import json
import sys

from ._common import check_all, SERVICES


def _print_human(results: list, lang: str) -> None:
    """人读表格输出。"""
    name_w = max(len("NAME"), max(len(r["name"]) for r in results))
    url_w = max(len("URL"), max(len(r["url"]) for r in results))
    hdr = f"{'NAME':<{name_w}}  {'URL':<{url_w}}  {'HEALTHY':<8}  {'LATENCY_MS':<10}"
    print(hdr)
    for r in results:
        mark = "✓" if r["healthy"] else "✗"
        print(
            f"{r['name']:<{name_w}}  {r['url']:<{url_w}}  {mark:<8}  {r['latency_ms']:<10}"
        )


def _print_json(results: list) -> None:
    """机读 JSON 输出（最终 stdout 唯一 JSON 对象）。"""
    out = {
        "services": results,
        "summary": {
            "total": len(results),
            "healthy": sum(1 for r in results if r["healthy"]),
            "unhealthy": sum(1 for r in results if not r["healthy"]),
        },
    }
    print(json.dumps(out, ensure_ascii=False, indent=2))


def run_status(*, as_json: bool, lang: str) -> int:
    """Status 子命令主入口。

    Returns exit code: 0 = all healthy, 1 = any unhealthy, 2 = error.
    """
    try:
        results = check_all()
    except Exception as e:
        msg = f"错误：探测失败: {e}" if lang == "zh" else f"Error: probe failed: {e}"
        print(msg, file=sys.stderr)
        return 2

    if as_json:
        _print_json(results)
    else:
        _print_human(results, lang)
        n_h = sum(1 for r in results if r["healthy"])
        n_total = len(results)
        if lang == "zh":
            print(
                f"\n共 {n_total} 服务，健康 {n_h}/{n_total}",
                file=sys.stderr,
            )
        else:
            print(
                f"\n{n_total} services, {n_h}/{n_total} healthy",
                file=sys.stderr,
            )

    return 0 if all(r["healthy"] for r in results) else 1
```

注意：
- **stdout 是唯一 JSON 对象**（D-04e，--json 模式）；人读模式 stdout 是表格
- 进度/摘要信息走 stderr（D-04d `__AR_PROGRESS__` 协议的 Phase 1 占位）
- exit code 0/1/2 区分正常/有 unhealthy/异常

  </action>
  <verify>
```bash
cd /Users/Zhuanz/Documents/autoresearch
test -f autoresearch/services/status.py
grep -q "def run_status" autoresearch/services/status.py
grep -q 'as_json' autoresearch/services/status.py
grep -q "_print_json" autoresearch/services/status.py
# 语法验证
uv run python -c "from autoresearch.services.status import run_status; print('OK')"
```
  </verify>
  <acceptance_criteria>
    - `autoresearch/services/status.py` 存在
    - 含 `run_status(as_json, lang)` 函数
    - `--json` 走 `_print_json` 路径
    - `from autoresearch.services.status import run_status` 可成功
  </acceptance_criteria>
  <done>status 子命令实现完成</done>
</task>

<task type="auto">
  <name>Task 3.5: 写 autoresearch/services/start.py (start 子命令 — 串行 up)</name>
  <files>autoresearch/services/start.py</files>
  <read_first>
    - .planning/PROJECT.md (D-05c, D-05d)
    - .planning/phases/01-repo-foundation-services/01-RESEARCH.md (Example 3)
  </read_first>
  <action>
创建 `autoresearch/services/start.py`（串行调 3 个 compose up）：

```python
"""autoresearch services start — 串行调 3 个 docker compose up。

实现 SVC-CHK-START-01 (调 docker compose up) + SVC-CHK-DEPS-01 (缺 docker 可读错误)。

D-05/D-05c: Archon 不在我们的 compose 里；输出明确提示用户用 `archon serve`。
"""
from __future__ import annotations

import shutil
import subprocess
import sys
from pathlib import Path

ROOT = Path(__file__).resolve().parents[2]
COMPOSE_DIR = ROOT / "services"

# Archon 由 `archon serve` 管理；不列在这里
COMPOSE_SERVICES = ["wandb", "prometheus", "grafana"]


def _check_docker() -> str | None:
    """检查 docker CLI 是否可用；返回错误消息或 None。"""
    if shutil.which("docker") is None:
        return "错误：找不到 `docker` 命令。请先安装 Docker Desktop（macOS）。"
    return None


def _run_compose_up(svc: str, lang: str) -> bool:
    """对单个服务执行 `docker compose -f <path> up -d`；失败返回 False。"""
    compose_file = COMPOSE_DIR / svc / "compose.yml"
    if not compose_file.exists():
        msg = (
            f"错误：找不到 compose 文件 {compose_file}"
            if lang == "zh"
            else f"Error: compose file not found: {compose_file}"
        )
        print(msg, file=sys.stderr)
        return False

    info = f"[{svc}] docker compose -f {compose_file} up -d"
    print(info, file=sys.stderr)

    try:
        result = subprocess.run(
            ["docker", "compose", "-f", str(compose_file), "up", "-d"],
            check=False,
            capture_output=True,
            text=True,
        )
    except FileNotFoundError:
        print("错误：找不到 docker 命令。", file=sys.stderr)
        return False

    if result.returncode != 0:
        print(f"[{svc}] FAILED (exit={result.returncode}):", file=sys.stderr)
        if result.stderr:
            print(result.stderr, file=sys.stderr)
        return False

    print(f"[{svc}] OK", file=sys.stderr)
    return True


def run_start(*, lang: str) -> int:
    """Start 子命令主入口。

    Returns exit code: 0 = all started, 1 = any failed, 2 = docker missing.
    """
    err = _check_docker()
    if err is not None:
        print(err, file=sys.stderr)
        return 2

    all_ok = True
    for svc in COMPOSE_SERVICES:
        if not _run_compose_up(svc, lang):
            all_ok = False
            break  # 串行：失败则停，避免错误叠加

    # D-05c: Archon 不在范围
    if lang == "zh":
        print(
            "Archon：由 `archon serve` 自行管理，不在 autoresearch 范围（D-05）。",
            file=sys.stderr,
        )
    else:
        print(
            "Archon: managed by `archon serve`, not by autoresearch (D-05).",
            file=sys.stderr,
        )

    return 0 if all_ok else 1
```

注意：
- 串行（`for` 循环）而非并行 — D-03b 锁定（避免端口竞争告警噪声）
- 失败即停（`break`）— 不让后续服务在错状态叠加启动
- 缺 docker 给出可读中文错误（SVC-CHK-DEPS-01）

  </action>
  <verify>
```bash
cd /Users/Zhuanz/Documents/autoresearch
test -f autoresearch/services/start.py
grep -q "def run_start" autoresearch/services/start.py
grep -q "subprocess.run" autoresearch/services/start.py
grep -q "wandb" autoresearch/services/start.py
grep -q "prometheus" autoresearch/services/start.py
grep -q "grafana" autoresearch/services/start.py
grep -q "archon serve" autoresearch/services/start.py
# 语法验证
uv run python -c "from autoresearch.services.start import run_start; print('OK')"
```
  </verify>
  <acceptance_criteria>
    - `autoresearch/services/start.py` 存在
    - 含 `run_start(lang)` 函数
    - 列出 3 个服务（wandb/prometheus/grafana），不含 archon
    - 缺 docker 时给中文错误消息
  </acceptance_criteria>
  <done>start 子命令实现完成</done>
</task>

<task type="auto">
  <name>Task 3.6: 写 autoresearch/services/stop.py (stop 子命令 — 串行 down)</name>
  <files>autoresearch/services/stop.py</files>
  <read_first>
    - autoresearch/services/start.py
  </read_first>
  <action>
创建 `autoresearch/services/stop.py`（串行 down）：

```python
"""autoresearch services stop — 串行调 3 个 docker compose down。

实现 SVC-CHK-STOP-01。
"""
from __future__ import annotations

import shutil
import subprocess
import sys
from pathlib import Path

ROOT = Path(__file__).resolve().parents[2]
COMPOSE_DIR = ROOT / "services"

# 与 start.py 对称；Archon 仍由 `archon serve` 管理
COMPOSE_SERVICES = ["wandb", "prometheus", "grafana"]


def _check_docker() -> str | None:
    """检查 docker CLI 是否可用。"""
    if shutil.which("docker") is None:
        return "错误：找不到 `docker` 命令。"
    return None


def _run_compose_down(svc: str, lang: str) -> bool:
    """对单个服务执行 `docker compose -f <path> down`。"""
    compose_file = COMPOSE_DIR / svc / "compose.yml"
    if not compose_file.exists():
        msg = (
            f"错误：找不到 compose 文件 {compose_file}"
            if lang == "zh"
            else f"Error: compose file not found: {compose_file}"
        )
        print(msg, file=sys.stderr)
        return False

    info = f"[{svc}] docker compose -f {compose_file} down"
    print(info, file=sys.stderr)

    try:
        result = subprocess.run(
            ["docker", "compose", "-f", str(compose_file), "down"],
            check=False,
            capture_output=True,
            text=True,
        )
    except FileNotFoundError:
        print("错误：找不到 docker 命令。", file=sys.stderr)
        return False

    if result.returncode != 0:
        print(f"[{svc}] FAILED (exit={result.returncode}):", file=sys.stderr)
        if result.stderr:
            print(result.stderr, file=sys.stderr)
        return False

    print(f"[{svc}] OK", file=sys.stderr)
    return True


def run_stop(*, lang: str) -> int:
    """Stop 子命令主入口。

    Returns exit code: 0 = all stopped, 1 = any failed, 2 = docker missing.
    """
    err = _check_docker()
    if err is not None:
        print(err, file=sys.stderr)
        return 2

    all_ok = True
    for svc in COMPOSE_SERVICES:
        if not _run_compose_down(svc, lang):
            all_ok = False
            break

    if lang == "zh":
        print(
            "Archon：由 `archon serve` 自行管理；autoresearch services stop 不影响 Archon。",
            file=sys.stderr,
        )
    else:
        print(
            "Archon: managed by `archon serve`; autoresearch services stop does not affect it.",
            file=sys.stderr,
        )

    return 0 if all_ok else 1
```

注意：与 start.py 镜像对称；同样 D-05 提示。

  </action>
  <verify>
```bash
cd /Users/Zhuanz/Documents/autoresearch
test -f autoresearch/services/stop.py
grep -q "def run_stop" autoresearch/services/stop.py
grep -q "subprocess.run" autoresearch/services/stop.py
grep -q 'down' autoresearch/services/stop.py
# 语法验证
uv run python -c "from autoresearch.services.stop import run_stop; print('OK')"
```
  </verify>
  <acceptance_criteria>
    - `autoresearch/services/stop.py` 存在
    - 含 `run_stop(lang)` 函数
    - 调 `docker compose down`（不是 up）
    - 缺 docker 时给中文错误消息
  </acceptance_criteria>
  <done>stop 子命令实现完成</done>
</task>

<task type="auto">
  <name>Task 3.7: 写 autoresearch/services/__init__.py</name>
  <files>autoresearch/services/__init__.py</files>
  <read_first>
    - autoresearch/services/_common.py
  </read_first>
  <action>
创建 `autoresearch/services/__init__.py`（空包，标注版本）：

```python
"""autoresearch.services subpackage.

M1 阶段 01: status / start / stop 三个子命令。
后续 phase 在这里加 config / hw / net / ... 子包。
"""
```

  </action>
  <verify>
```bash
cd /Users/Zhuanz/Documents/autoresearch
test -f autoresearch/services/__init__.py
```
  </verify>
  <acceptance_criteria>
    - `autoresearch/services/__init__.py` 存在
  </acceptance_criteria>
  <done>services 子包标记完成</done>
</task>

<task type="auto">
  <name>Task 3.8: 写 tests/ (3 个 happy path 单测)</name>
  <files>
    - tests/__init__.py
    - tests/test_cli.py
    - tests/test_status.py
    - tests/test_start_stop.py
  </files>
  <read_first>
    - autoresearch/cli.py
    - autoresearch/services/_common.py
  </read_first>
  <action>
**Task 3.8a**: 创建 `tests/__init__.py`（空）：

```python
"""autoresearch tests."""
```

**Task 3.8b**: 创建 `tests/test_cli.py`（CLI 帮助/版本 smoke）：

```python
"""CLI smoke tests: --version, --help."""
from click.testing import CliRunner

from autoresearch.cli import main


def test_version():
    runner = CliRunner()
    result = runner.invoke(main, ["--version"])
    assert result.exit_code == 0
    assert "0.1.0" in result.output


def test_help_shows_services():
    runner = CliRunner()
    result = runner.invoke(main, ["--help"])
    assert result.exit_code == 0
    assert "services" in result.output


def test_services_help_shows_subcommands():
    runner = CliRunner()
    result = runner.invoke(main, ["services", "--help"])
    assert result.exit_code == 0
    assert "status" in result.output
    assert "start" in result.output
    assert "stop" in result.output
```

**Task 3.8c**: 创建 `tests/test_status.py`（_common + status 行为）：

```python
"""Tests for services.status subcommand."""
import json
from unittest.mock import patch

from click.testing import CliRunner

from autoresearch.cli import main
from autoresearch.services._common import SERVICES, check_all


def test_services_constant_has_4_entries():
    assert len(SERVICES) == 4
    names = {s[0] for s in SERVICES}
    assert names == {"archon", "wandb", "prometheus", "grafana"}


def test_check_all_handles_connection_error():
    """When all services are down, check_all should return 4 unhealthy results (not raise)."""
    # No mocks; localhost:8088/8080/9090/3000 are almost certainly unbound in test env
    results = check_all(timeout=0.5)
    assert len(results) == 4
    # All results should be unhealthy (no service running)
    assert all(r["healthy"] is False for r in results)
    # Each result has all required fields
    for r in results:
        assert "name" in r
        assert "url" in r
        assert "healthy" in r
        assert "latency_ms" in r
        assert "error" in r


def test_status_json_output():
    runner = CliRunner()
    result = runner.invoke(main, ["services", "status", "--json"])
    # exit code is 1 if any unhealthy (expected in test env)
    assert result.exit_code in (0, 1)
    # stdout is a valid JSON object
    payload = json.loads(result.output)
    assert "services" in payload
    assert "summary" in payload
    assert len(payload["services"]) == 4
    assert payload["summary"]["total"] == 4


def test_status_human_output():
    runner = CliRunner()
    result = runner.invoke(main, ["services", "status"])
    assert result.exit_code in (0, 1)
    # stdout has table headers
    assert "NAME" in result.output
    assert "URL" in result.output
    assert "HEALTHY" in result.output
```

**Task 3.8d**: 创建 `tests/test_start_stop.py`（docker 缺失时的错误路径）：

```python
"""Tests for services.start / services.stop — focus on error paths."""
from unittest.mock import patch

from click.testing import CliRunner

from autoresearch.cli import main
from autoresearch.services.start import run_start
from autoresearch.services.stop import run_stop


def test_run_start_returns_2_when_docker_missing():
    """When docker CLI is missing, run_start should return 2 (and print Chinese error)."""
    with patch("autoresearch.services.start.shutil.which", return_value=None):
        code = run_start(lang="zh")
    assert code == 2


def test_run_stop_returns_2_when_docker_missing():
    with patch("autoresearch.services.stop.shutil.which", return_value=None):
        code = run_stop(lang="zh")
    assert code == 2


def test_start_help_does_not_crash():
    runner = CliRunner()
    result = runner.invoke(main, ["services", "start", "--help"])
    assert result.exit_code == 0


def test_stop_help_does_not_crash():
    runner = CliRunner()
    result = runner.invoke(main, ["services", "stop", "--help"])
    assert result.exit_code == 0
```

**Task 3.8e**: 跑测试验证全绿：

```bash
cd /Users/Zhuanz/Documents/autoresearch
uv run pytest tests/ -v 2>&1 | tail -30
```

期望：8 个测试全部 PASS（test_version / test_help_shows_services / test_services_help_shows_subcommands / test_services_constant_has_4_entries / test_check_all_handles_connection_error / test_status_json_output / test_status_human_output / test_run_start_returns_2_when_docker_missing / test_run_stop_returns_2_when_docker_missing / test_start_help_does_not_crash / test_stop_help_does_not_crash = 11 个）。

注意：test_start_help_does_not_crash 和 test_stop_help_does_not_crash 不需要 mock；只是验证 --help 渲染。

  </action>
  <verify>
```bash
cd /Users/Zhuanz/Documents/autoresearch
test -f tests/__init__.py
test -f tests/test_cli.py
test -f tests/test_status.py
test -f tests/test_start_stop.py
# 跑测试
uv run pytest tests/ -v 2>&1 | tail -20
```
  </verify>
  <acceptance_criteria>
    - 4 个测试文件存在
    - `uv run pytest tests/` 全部 PASS（11 个测试）
  </acceptance_criteria>
  <done>单测覆盖 CLI smoke + status 行为 + start/stop 错误路径</done>
</task>

<task type="auto">
  <name>Task 3.9: git add + commit (CLI + 测试)</name>
  <files>
    - pyproject.toml
    - uv.lock
    - autoresearch/__init__.py
    - autoresearch/__main__.py
    - autoresearch/cli.py
    - autoresearch/services/__init__.py
    - autoresearch/services/_common.py
    - autoresearch/services/status.py
    - autoresearch/services/start.py
    - autoresearch/services/stop.py
    - tests/__init__.py
    - tests/test_cli.py
    - tests/test_status.py
    - tests/test_start_stop.py
  </files>
  <read_first>
    - .gitignore
  </read_first>
  <action>
把 CLI + 测试加入 git 并提交：

```bash
cd /Users/Zhuanz/Documents/autoresearch
git add pyproject.toml uv.lock autoresearch/ tests/
git status

git commit -m "feat(01): add autoresearch CLI (services status/start/stop)

- pyproject.toml: uv-managed, click + requests + pytest, autoresearch script entry
- autoresearch/cli.py: @click.group() tree; services subcommand group
- autoresearch/services/_common.py: ThreadPoolExecutor concurrent healthz probe
- autoresearch/services/status.py: --json machine-readable + human table
- autoresearch/services/start.py: serial docker compose up -d for 3 services
- autoresearch/services/stop.py: serial docker compose down for 3 services
- tests/: 11 pytest happy path + error-path tests (all pass)

Closes SVC-CHK-STAT-01..03, SVC-CHK-START-01, SVC-CHK-STOP-01, SVC-CHK-DEPS-01"
```

注意：
- 验证 `git status` 干净
- 验证 `uv run autoresearch --version` 和 `autoresearch services --help` 工作
- 验证 `uv run pytest tests/` 全绿

  </action>
  <verify>
```bash
cd /Users/Zhuanz/Documents/autoresearch
git log -1 --format="%h %s" | grep -q "feat(01): add autoresearch CLI"
git show --stat HEAD | grep -E "autoresearch/|tests/|pyproject|uv.lock" | wc -l   # 至少 10 行
uv run autoresearch --version
uv run autoresearch services --help 2>&1 | head -5
uv run pytest tests/ 2>&1 | tail -3
```
  </verify>
  <acceptance_criteria>
    - 14 个文件被 commit
    - `uv run autoresearch --version` 输出 `autoresearch, version 0.1.0`
    - `uv run autoresearch services --help` 显示 status/start/stop
    - `uv run pytest tests/` 全绿
  </acceptance_criteria>
  <done>CLI + 测试 commit 完成</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run autoresearch --version` 成功
- [ ] `uv run autoresearch services --help` 显示 status/start/stop
- [ ] `uv run autoresearch services status` 输出 4 行表格
- [ ] `uv run autoresearch services status --json` 输出合法 JSON
- [ ] `uv run autoresearch services start` 在缺 docker 时返回 exit 2 + 中文错误
- [ ] `uv run pytest tests/` 11 个测试全 PASS
- [ ] 4 个健康探测 URL 端口固定（8088/8080/9090/3000）
- [ ] Archon 不在 start/stop 列表里（D-05 锁定）
</verification>

<success_criteria>
- All 9 tasks completed
- All verification checks pass
- SVC-CHK-STAT-01..03, SVC-CHK-START-01, SVC-CHK-STOP-01, SVC-CHK-DEPS-01 全部满足
- CLI 树状子命令结构（autoresearch → services → status/start/stop）
- 并发 healthz 探测 4 服务
- 11 个 pytest 单测全绿
- `autoresearch` 暴露为单二进制（`uv run autoresearch`）
- 默认中文 + `--lang en` 切英文
</success_criteria>

<output>
After completion, create `.planning/phases/01-repo-foundation-services/01-03-SUMMARY.md`
</output>
