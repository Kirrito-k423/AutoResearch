---
phase: 01-repo-foundation-services
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - LICENSE
  - .gitignore
  - AGENTS.md
  - CLAUDE.md
  - README.md
autonomous: true
requirements:
  - REPO-01
  - REPO-02
  - REPO-03
  - REPO-04
  - REPO-05
user_setup: []
must_haves:
  truths:
    - "仓根 5 个文件存在：README.md / AGENTS.md / CLAUDE.md / LICENSE / .gitignore"
    - "README.md ≤ 80 行；含哲学 + 1 行 quickstart + LICENSE 链接 + 文档引用"
    - "CLAUDE.md 是 AGENTS.md 的相对路径 symlink（git 可追踪）"
    - "AGENTS.md 含 8 步循环 / 三沉淀层 / 进度协议 / 测试规范 4 个章节"
    - "LICENSE 是 MIT 全文"
    - ".gitignore 覆盖 Python / macOS / IDE / secrets 4 类"
  artifacts:
    - path: README.md
      must_exist: true
      max_lines: 80
    - path: AGENTS.md
      must_exist: true
    - path: CLAUDE.md
      must_exist: true
      must_be_symlink_to: AGENTS.md
    - path: LICENSE
      must_exist: true
      must_contain: "MIT License"
    - path: .gitignore
      must_exist: true
  key_links:
    - from: CLAUDE.md
      to: AGENTS.md
      via: "symlink (relative path)"
    - from: README.md
      to: LICENSE
      via: "Markdown link"
---

<objective>
Create the repo-root document skeleton: README (宣言式) + AGENTS (单源真相) + CLAUDE (symlink) + LICENSE (MIT) + .gitignore (Python/macOS/IDE/secrets). 满足 REPO-01..05。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-repo-foundation-services/01-CONTEXT.md
@.planning/phases/01-repo-foundation-services/01-RESEARCH.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1.1: 写 LICENSE (MIT)</name>
  <files>LICENSE</files>
  <read_first>
    - .planning/PROJECT.md
  </read_first>
  <action>
创建 `LICENSE` 文件，内容为 MIT 许可证全文（2003 年版本）。Copyright line 写 `Copyright (c) 2026 AutoResearch Contributors`。M1 阶段 01 任务级别最小：把 MIT 完整文本贴进 LICENSE，包括：
- "MIT License" 标题
- Copyright line
- "Permission is hereby granted, free of charge..." 整段
- "THE SOFTWARE IS PROVIDED \"AS IS\"..." 结尾

不要修改成 Apache / BSD；PROJECT.md Key Decisions 锁 MIT。
  </action>
  <verify>
```bash
cd /Users/Zhuanz/Documents/autoresearch
test -f LICENSE
grep -q "MIT License" LICENSE
grep -q "Copyright (c) 2026" LICENSE
```
  </verify>
  <acceptance_criteria>
    - `LICENSE` 文件存在
    - 含 "MIT License" 字符串
    - 含 "Copyright (c) 2026" 字符串
    - 含 "Permission is hereby granted" 字符串
  </acceptance_criteria>
  <done>LICENSE 存在且为 MIT 全文</done>
</task>

<task type="auto">
  <name>Task 1.2: 写 .gitignore (Python / macOS / IDE / secrets)</name>
  <files>.gitignore</files>
  <read_first>
    - .planning/PROJECT.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
创建 `.gitignore` 文件，覆盖以下类别（每类至少 3-5 条典型规则），每段用 `# --- <类别> ---` 注释分隔：

**Python 类：**
```
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
.pytest_cache/
.coverage
htmlcov/
dist/
build/
.mypy_cache/
.ruff_cache/
venv/
.venv/
```

**macOS 类：**
```
.DS_Store
.AppleDouble
.LSOverride
Icon?
._*
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent
```

**IDE 类：**
```
.vscode/
.idea/
*.swp
*.swo
*~
.cursor/
.aider.*
```

**secrets / 环境变量类：**
```
.env
.env.local
*.pem
*.key
secrets.yaml
```

**项目特有（autoresearch）：**
```
.planning/milestones/
data/
runs/
.autoresearch/
```
  </action>
  <verify>
```bash
cd /Users/Zhuanz/Documents/autoresearch
test -f .gitignore
grep -q "__pycache__" .gitignore
grep -q ".DS_Store" .gitignore
grep -q ".vscode/" .gitignore
grep -q "^.env$" .gitignore
```
  </verify>
  <acceptance_criteria>
    - `.gitignore` 文件存在
    - 含 `__pycache__`（Python 规则）
    - 含 `.DS_Store`（macOS 规则）
    - 含 `.vscode/`（IDE 规则）
    - 含独立的 `.env` 行（secrets 规则）
  </acceptance_criteria>
  <done>.gitignore 存在并覆盖 4+ 类别</done>
</task>

<task type="auto">
  <name>Task 1.3: 写 AGENTS.md (单源真相)</name>
  <files>AGENTS.md</files>
  <read_first>
    - .planning/PROJECT.md
    - .planning/phases/01-repo-foundation-services/01-CONTEXT.md
    - .planning/phases/01-repo-foundation-services/01-RESEARCH.md
  </read_first>
  <action>
创建 `AGENTS.md` 文件，包含以下章节（**这是单源真相**，CLAUDE.md 是它的 symlink）。完整内容如下（按顺序写）：

```
# AGENTS.md — AutoResearch 仓协作者指南

> **单源真相**：本文件即真相；`CLAUDE.md` 是它的相对路径 symlink。
> 任何 AI / 人类协作者进入此仓，先读本文件。

## 仓约定
- **Python 3.11+**；包管理用 [uv](https://github.com/astral-sh/uv)
- **CLI 框架**：[click](https://click.palletsprojects.com/) 8.1+；所有子命令挂在 `autoresearch` 单二进制下
- **服务编排**：每服务一个 `services/<name>/compose.yml`；`autoresearch services start` 串行 `docker compose -f services/X/compose.yml up -d`
- **测试**：`pytest`；M1 阶段 01 可只做单测；CLI 子命令至少 1 个 happy path 单测
- **进度协议**：子命令最终 stdout 必须是唯一一个 JSON 对象；过程进度走 stderr `__AR_PROGRESS__=<json>` 标记（Phase 2 落 CORE-PROTO-01..02）
- **本地优先**：所有 run 数据、log、wandb 落本地 Mac (`~/.autoresearch/`)，远程服务器不留状态
- **MIT License**

## 8 步最小循环（1:1 映射 8 skill）

| # | Skill | CLI group | Phase | REQ 组 |
|---|-------|-----------|-------|--------|
| 1 | customer-config | `autoresearch config` | 3 | CFG-* |
| 2 | local-services-health | `autoresearch services` | 1, 4 | SVC-* / SVC-CHK-* |
| 3 | server-hardware-probe | `autoresearch hw` | 5 | HW-* |
| 4 | network-check | `autoresearch net` | 6 | NET-* |
| 5 | service-reachability | `autoresearch reach` | 7 | REACH-* |
| 6 | train-stack-health | `autoresearch stack` | 8 | STACK-* |
| 7 | data-collection | `autoresearch collect` | 9 | COLL-* |
| 8 | experiment-report | `autoresearch report` | 10 | RPT-* |

## 三沉淀层
- `workspace-core/`：通用（SSH / config / secrets / progress / log / layout）
- `workspace-adapter/`：训练栈适配（verl / veomni）
- `datalake/`：数据采集与持久化（wandb / log / prom / manifest）

互不混用；每个 skill 可独立维护。

## 服务端口固定
- Archon: 8088（**不**在我们的 compose 里；走 `archon serve`）
- wandb (local): 8080
- Prometheus: 9090
- Grafana: 3000

## 进度协议模板
```python
import sys, json
def emit_progress(stage: str, **fields):
    msg = {"stage": stage, **fields}
    print(f"__AR_PROGRESS__={json.dumps(msg, ensure_ascii=False)}", file=sys.stderr)
```

## 测试规范
- 单测放 `tests/`；命名 `test_<module>.py`
- 使用 `click.testing.CliRunner` 测 CLI 子命令
- 业务逻辑必须有断言；不允许 "no assertions in test"
- M1 不强制覆盖率；但每个 CLI 子命令至少 1 个 happy path 单测

## 仓结构（M1 完整态）
```
README.md
AGENTS.md
CLAUDE.md          # symlink → AGENTS.md
LICENSE
.gitignore
pyproject.toml
autoresearch/      # Python 包
  __init__.py
  __main__.py
  cli.py
  services/        # M1 范围
    __init__.py
    _common.py
    status.py
    start.py
    stop.py
  config/          # Phase 3
  hw/              # Phase 5
  ...
services/          # Docker Compose
  archon/          # 只有 README
  wandb/
  prometheus/
  grafana/
tests/
diagram/
docs/
```

## 重要决策
- Archon 集成走 `archon serve`（不在 docker-compose 里）
- Docker Compose v2 plugin（`docker compose` 子命令），不用 v1 binary
- `host.docker.internal` 给 Grafana 连本机 wandb 用
- 默认 CLI 错误信息中文；`--lang en` 切英文
```
  </action>
  <verify>
```bash
cd /Users/Zhuanz/Documents/autoresearch
test -f AGENTS.md
grep -q "8 步最小循环" AGENTS.md
grep -q "三沉淀层" AGENTS.md
grep -q "__AR_PROGRESS__" AGENTS.md
grep -q "pytest" AGENTS.md
```
  </verify>
  <acceptance_criteria>
    - `AGENTS.md` 文件存在
    - 含 "8 步最小循环" 章节
    - 含 "三沉淀层" 章节
    - 含 `__AR_PROGRESS__` 字符串
    - 含 `pytest` 字符串
  </acceptance_criteria>
  <done>AGENTS.md 存在并含 4 个必填章节</done>
</task>

<task type="auto">
  <name>Task 1.4: 创建 CLAUDE.md (AGENTS.md 的相对路径 symlink)</name>
  <files>CLAUDE.md</files>
  <read_first>
    - AGENTS.md
  </read_first>
  <action>
创建 `CLAUDE.md` 作为 `AGENTS.md` 的相对路径 symlink：
```bash
cd /Users/Zhuanz/Documents/autoresearch
ln -s AGENTS.md CLAUDE.md
```

**关键**：
- 必须用**相对路径** `AGENTS.md`（不是绝对路径 `/Users/.../AGENTS.md`），这样跨机器 / git clone 不会坏
- git 默认可追踪 symlink（mode 120000）
  </action>
  <verify>
```bash
cd /Users/Zhuanz/Documents/autoresearch
test -L CLAUDE.md
target=$(readlink CLAUDE.md)
test "$target" = "AGENTS.md" || { echo "FAIL: CLAUDE.md symlink target is '$target', expected 'AGENTS.md'"; exit 1; }
git ls-files -s CLAUDE.md   # 应显示 mode 120000
```
  </verify>
  <acceptance_criteria>
    - `CLAUDE.md` 是 symlink（`test -L` 通过）
    - symlink 目标 = `AGENTS.md`（**相对路径**，不是绝对）
    - git 把它识别为 mode 120000（symlink）
  </acceptance_criteria>
  <done>CLAUDE.md 是 AGENTS.md 的相对路径 symlink</done>
</task>

<task type="auto">
  <name>Task 1.5: 写 README.md (极简宣言, ≤ 80 行)</name>
  <files>README.md</files>
  <read_first>
    - .planning/PROJECT.md
    - AGENTS.md
    - LICENSE
    - .planning/ROADMAP.md
  </read_first>
  <action>
创建 `README.md`，**严格 ≤ 80 行**。完整内容如下（每节都保留，**不要扩展 FAQ / Contributing / Changelog**）：

```
# AutoResearch

> 本地驱动的 LLM 训练/调试工作流平台。所有数据留在本机 Mac；远程 NPU 服务器只是被远控的执行终端。

## 核心价值
"常实践，详记录，知得失，会设计，有整理" —— 每个 skill 跑一次都留下可被复盘、可被二次开发的产物。

## 快速开始
```bash
git clone https://github.com/<org>/autoresearch.git
cd autoresearch
# 1) 装 Archon CLI（前置条件；详见 services/archon/README.md）
brew install archon   # 或见 archon.diy
archon serve &        # 启动 Archon（用户自行管理）
# 2) 启本地服务栈
autoresearch services start
# 3) 验证全绿
autoresearch services status
```

## 架构
4 列架构图见 [diagram/autoresearch_arch.svg](diagram/autoresearch_arch.svg)。

## 8 步最小循环
详细描述见 [.planning/ROADMAP.md](.planning/ROADMAP.md)。概览：customer-config → local-services-health → server-hardware-probe → network-check → service-reachability → train-stack-health → data-collection → experiment-report。

## 文档
- [AGENTS.md](AGENTS.md) — AI 协作者指南（必读）
- [.planning/PROJECT.md](.planning/PROJECT.md) — 项目哲学 / 约束 / 关键决策
- [.planning/ROADMAP.md](.planning/ROADMAP.md) — 14 阶段路线图

## License
[MIT](LICENSE)
```

注意：quickstart 实际是多步命令的可视化呈现；不写 badges / FAQ / Contributing / Changelog。
  </action>
  <verify>
```bash
cd /Users/Zhuanz/Documents/autoresearch
test -f README.md
lines=$(wc -l < README.md)
test "$lines" -le 80 || { echo "FAIL: README.md has $lines lines, expected ≤ 80"; exit 1; }
grep -q "# AutoResearch" README.md
grep -q "MIT" README.md
grep -q "AGENTS.md" README.md
grep -q "archon serve" README.md
# 反向检查：不含 FAQ / Contributing / Changelog 章节
! grep -q "^## FAQ" README.md
! grep -q "^## Contributing" README.md
! grep -q "^## Changelog" README.md
```
  </verify>
  <acceptance_criteria>
    - `README.md` 文件存在
    - 行数 ≤ 80
    - 含 "# AutoResearch" 标题
    - 含 "MIT" 字样
    - 含 "AGENTS.md" 引用
    - 含 `archon serve` 提及
    - **不**含 FAQ / Contributing / Changelog 章节
  </acceptance_criteria>
  <done>README.md 是 ≤ 80 行的极简宣言</done>
</task>

<task type="auto">
  <name>Task 1.6: git add + commit (仓根文档)</name>
  <files>
    - LICENSE
    - .gitignore
    - AGENTS.md
    - CLAUDE.md
    - README.md
  </files>
  <read_first>
    - .planning/STATE.md
  </read_first>
  <action>
把 5 个仓根文件加入 git 并提交：
```bash
cd /Users/Zhuanz/Documents/autoresearch
git add LICENSE .gitignore AGENTS.md CLAUDE.md README.md
git status   # 确认 5 个文件 staged

git commit -m "docs(01): add repo root skeleton (README/AGENTS/CLAUDE/LICENSE/.gitignore)

- README.md: D-01 极简宣言，≤ 80 行，含 quickstart
- AGENTS.md: D-02 单源真相，含 8 步循环 / 三沉淀层 / 进度协议 / 测试规范
- CLAUDE.md: D-02a AGENTS.md 的相对路径 symlink
- LICENSE: MIT 全文
- .gitignore: Python / macOS / IDE / secrets 四类

Closes REPO-01..05"
```

注意：
- commit message 格式遵循 conventional commits
- 如果 git hook 拦截，先 `git status` 看是否有未 tracked 的非计划文件
  </action>
  <verify>
```bash
cd /Users/Zhuanz/Documents/autoresearch
git log -1 --format="%h %s" | grep -q "docs(01): add repo root skeleton"
git show --stat HEAD | grep -E "README|AGENTS|CLAUDE|LICENSE|\.gitignore" | wc -l   # 至少 5 行
```
  </verify>
  <acceptance_criteria>
    - 5 个文件被 commit
    - commit message 以 "docs(01):" 开头
  </acceptance_criteria>
  <done>仓根文档 commit 完成</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] 5 个仓根文件全部存在且 git tracked
- [ ] `wc -l README.md` ≤ 80
- [ ] `readlink CLAUDE.md` 输出 `AGENTS.md`
- [ ] `git ls-files -s CLAUDE.md` 显示 mode 120000
- [ ] `grep "MIT License" LICENSE` 命中
- [ ] `grep "8 步最小循环" AGENTS.md` 命中
- [ ] `git log -1` 包含 "docs(01): add repo root skeleton"
</verification>

<success_criteria>
- All 6 tasks completed
- All verification checks pass
- No errors or warnings introduced
- REPO-01..05 全部满足
- README.md ≤ 80 行（D-01 锁定）
- CLAUDE.md 是 AGENTS.md 的相对路径 symlink（D-02a 锁定）
</success_criteria>

<output>
After completion, create `.planning/phases/01-repo-foundation-services/01-01-SUMMARY.md`
</output>
