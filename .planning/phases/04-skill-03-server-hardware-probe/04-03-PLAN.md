---
phase: 04-skill-03-server-hardware-probe
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - autoresearch/hw/models.py
  - autoresearch/hw/parser.py
  - autoresearch/hw/probe.py
  - autoresearch/cli.py
  - tests/fixtures/hw/npu_smi_with_processes.txt
  - tests/test_hw_parser.py
  - tests/test_hw_probe.py
  - tests/test_hw_cli.py
autonomous: true
requirements:
  - HW-CONN-01
  - HW-CONN-02
  - HW-NPU-01
  - HW-NPU-02
  - HW-NPU-03
  - HW-OCC-01
  - HW-OCC-02
  - HW-DRV-01
must_haves:
  truths:
    - "D-18/D-19: npu-smi 进程记录补齐 pid/user/process_name/memory，process_name 只取可执行文件名"
    - "D-20/D-21: 进程退出或 ps 权限不足保留 PID，补全字段为 null + warning，不令核心指标失败"
    - "D-03/D-27: config 中每台服务器都必须完成真机核心指标验收，不能只验 A2 或 SSH"
    - "D-05/D-29: 单台失败不取消其他探测，但阻止聚合成功并列出通过/失败服务器"
    - "D-07: `--all` 最多 3 个并发 worker，每个 worker 独立 SSHClient，最终结果按配置顺序"
    - "D-28: fixture 覆盖 lspci、未知格式、无进程、有进程、竞态和权限不足"
  artifacts:
    - path: autoresearch/hw/parser.py
      must_exist: true
      must_contain: "def parse_processes"
    - path: autoresearch/hw/probe.py
      must_exist: true
      must_contain: "def probe_all"
    - path: autoresearch/cli.py
      must_exist: true
      must_contain: "--all"
  key_links:
    - from: autoresearch/hw/probe.py
      to: autoresearch/hw/parser.py
      via: "parsed numeric PIDs drive batch ps enrichment"
    - from: autoresearch/hw/probe.py
      to: workspace-core/config/loader.py
      via: "probe_all reads every configured server"
    - from: autoresearch/cli.py
      to: autoresearch/hw/probe.py
      via: "--all selects bounded aggregate probe"
---

<objective>
关闭 Phase 4 的剩余业务闭环：解析 NPU 占用进程并安全补齐 user/process name，增加 `--all` 最大 3 并发和部分失败聚合，最后对配置中的全部真实服务器执行同一套完整验收。

完成标准不是“测试绿”或“A2 能连”，而是当前配置的每台服务器都能返回每张卡的显存、温度、利用率，并且聚合结果整体成功。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/04-skill-03-server-hardware-probe/04-CONTEXT.md
@.planning/phases/04-skill-03-server-hardware-probe/04-RESEARCH.md
@.planning/phases/04-skill-03-server-hardware-probe/04-VALIDATION.md
@.planning/phases/04-skill-03-server-hardware-probe/04-02-SUMMARY.md
@autoresearch/hw/models.py
@autoresearch/hw/parser.py
@autoresearch/hw/probe.py
@autoresearch/cli.py
@autoresearch/services/_common.py
@config/config.yaml
</context>

<threat_model>
  <threat id="T-04-03-01" severity="HIGH">
    <asset>远程 shell 与训练秘密</asset>
    <attack>恶意 PID/进程文本造成命令注入，或完整命令行泄露 token/路径/训练参数</attack>
    <mitigation>PID 在 parser 中转为整数后才进入逗号列表；固定执行 `ps -o pid=,user=,comm= -p ...`；禁止 args/cmdline/environ</mitigation>
    <verification>`tests/test_hw_probe.py` 断言命令只含数字 PID 和 `comm=`，恶意 PID fixture 被拒绝</verification>
  </threat>
  <threat id="T-04-03-02" severity="MEDIUM">
    <asset>并发探测隔离和结果完整性</asset>
    <attack>线程共享 SSH client、异常逃逸取消其他 future、完成顺序导致结果错配</attack>
    <mitigation>每 worker 调用独立 `probe_server`；future 异常在 server 边界转 CheckResult；最终按配置索引重排</mitigation>
    <verification>`tests/test_hw_probe.py` 用不同延迟/失败 fake server 断言独立 client 和稳定顺序</verification>
  </threat>
  <threat id="T-04-03-03" severity="HIGH">
    <asset>Phase 4 完成声明的真实性</asset>
    <attack>用 fixture、单台 A2 或 SSH 成功替代全部配置服务器核心指标验收</attack>
    <mitigation>最终任务强制逐台真实命令和 `--all`；任一失败保持计划/阶段未完成；SUMMARY 记录每台验收结果</mitigation>
    <verification>执行 `uv run autoresearch hw probe --all`，要求每个配置 server 的每张设备四个核心字段非 null 且顶层 ok=true</verification>
  </threat>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 04-03-01: 解析占用进程并用安全批量 ps 补全</name>
  <files>
    - autoresearch/hw/models.py
    - autoresearch/hw/parser.py
    - autoresearch/hw/probe.py
    - tests/fixtures/hw/npu_smi_with_processes.txt
    - tests/test_hw_parser.py
    - tests/test_hw_probe.py
  </files>
  <read_first>
    - .planning/phases/04-skill-03-server-hardware-probe/04-CONTEXT.md
    - autoresearch/hw/models.py
    - autoresearch/hw/parser.py
    - autoresearch/hw/probe.py
    - tests/fixtures/hw/npu_smi_25_3_rc1_no_processes.txt
    - tests/test_hw_parser.py
    - tests/test_hw_probe.py
  </read_first>
  <action>
实现/扩展 `parse_processes(text)`，从 `npu-smi info` Processes 区段提取 `npu_id/chip_id/pid/memory_used_mib`。PID 和内存必须严格转换为非负整数；无法转换的记录产生 warning，不得把原字符串用于命令。

实现 `parse_ps_output(text)` 和 `enrich_processes(client, processes)`。去重并排序有效 PID，构造唯一固定命令 `ps -o pid=,user=,comm= -p <comma-separated integers>`。按 PID 合并 `user` 和 `process_name`，其中 name 只来自 `comm`。

若 PID 在两次采集间退出，或 `ps` 非零/权限不足，保留原 process；无法补齐的 `user/process_name` 为 `None` 并追加 warning。该 warning 使完整核心指标结果最多降为 WARN，不变成 FAIL。无进程时不执行 `ps`。
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_hw_parser.py tests/test_hw_probe.py -k "process or ps or race or permission"</automated>
  </verify>
  <acceptance_criteria>
    - process fixture 解析出 pid、设备标识和进程显存，空进程 fixture 得到 `[]`
    - `ps` 命令只含去重排序后的数字 PID，且字段严格为 `pid=,user=,comm=`
    - JSON 不包含完整命令行、args、cmdline 或环境变量
    - race/permission 场景保留 PID，user/name 为 `None`，warnings 非空，核心设备仍可 WARN 成功
    - 恶意或非数字 PID 不进入远程命令
  </acceptance_criteria>
  <done>HW-OCC-01/02 可用，且进程补全不泄露训练参数、不把竞态升级为硬件失败</done>
</task>

<task type="auto">
  <name>Task 04-03-02: 增加 `--all` 最大 3 并发和部分失败聚合</name>
  <files>
    - autoresearch/hw/probe.py
    - autoresearch/cli.py
    - tests/test_hw_probe.py
    - tests/test_hw_cli.py
  </files>
  <read_first>
    - autoresearch/hw/probe.py
    - autoresearch/cli.py
    - autoresearch/services/_common.py
    - workspace-core/result/check.py
    - workspace-core/config/loader.py
    - tests/test_hw_probe.py
    - tests/test_hw_cli.py
  </read_first>
  <action>
实现 `probe_all(config_path=None, max_workers=3, probe_fn=probe_server) -> CheckResult`。读取 config 中全部服务器，以 `ThreadPoolExecutor(max_workers=min(3, len(servers)))` 并发。每个 future 捕获所有异常并转为该 server 的 FAIL；不要使用会丢失 per-server data 的 `workspace_core.result.merge()`。

顶层 `data.results` 按 config 顺序保存 `{server_name: CheckResult}` 或有序列表，并提供 `total/passed/failed/warned/passed_servers/failed_servers`。只要有一台 FAIL，顶层 `ok=false/severity=fail`；WARN 服务器无 FAIL 时顶层 `ok=true/severity=warn`，同时 CLI exit 0，因为核心指标成功。不要调用会丢 per-server data 且把 WARN 设为 `ok=false` 的现有 `merge()`。

把 Click 参数改为互斥且二选一的 `--server NAME` / `--all`。`--all` 走 `probe_all`；参数/配置错误 exit 2，任一 server FAIL exit 1，全部核心成功 exit 0。每台 server 都发 begin/complete/fail progress，stdout 仍只有一个 JSON。
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_hw_probe.py tests/test_hw_cli.py -k all</automated>
  </verify>
  <acceptance_criteria>
    - 5 台 fake server 的最大同时运行数不超过 3
    - 一个 fake server 失败时其余 4 台仍完成并保留结果，顶层 exit 1
    - 人为打乱 future 完成顺序后，JSON 仍按 config server 顺序
    - `--server` 与 `--all` 同时或都不传时 exit 2
    - `--all` happy path stdout 是唯一 JSON，stderr 有每台 server progress
  </acceptance_criteria>
  <done>多服务器探测有界并发、失败隔离、稳定顺序和明确聚合语义</done>
</task>

<task type="auto">
  <name>Task 04-03-03: 跑完整回归并通过全部配置服务器真机闸门</name>
  <files>
    - tests/test_hw_parser.py
    - tests/test_hw_probe.py
    - tests/test_hw_cli.py
  </files>
  <read_first>
    - AGENTS.md
    - .planning/phases/04-skill-03-server-hardware-probe/04-CONTEXT.md
    - .planning/phases/04-skill-03-server-hardware-probe/04-VALIDATION.md
    - autoresearch/hw/models.py
    - autoresearch/hw/parser.py
    - autoresearch/hw/probe.py
    - autoresearch/cli.py
    - tests/test_hw_parser.py
    - tests/test_hw_probe.py
    - tests/test_hw_cli.py
    - config/config.yaml
  </read_first>
  <action>
补齐验证矩阵中遗漏的回归断言：lspci fallback、未知格式、无进程、有进程、缺字段、typed supplement、进程竞态、ps 权限不足、raw log、单服务器退出码和 all-server 部分失败。运行完整 pytest，修复本阶段引入的回归。

从 `config/config.yaml` 读取当前全部 server 名（不得把 host/IP/secret 写进提交文件），逐台执行 `uv run autoresearch hw probe --server NAME`。每台都要求设备非空，每张设备四个核心字段是非 null 整数。把 server 名、exit code、设备数、severity 和失败原因记录到 04-03 SUMMARY；不要记录 IP、用户名、原始配置或凭据。

最后执行 `uv run autoresearch hw probe --all`，要求结果包含每台配置 server、顺序与 config 一致、顶层 `ok=true`。2026-06-09 规划时五台 server 曾全部 SSH timeout；若仍不可达，停止计划/Phase 4 完成标记，保留可读阻塞信息，待网络恢复后重跑，绝不以 fixture 或单台成功豁免。
  </action>
  <verify>
    <automated>uv run pytest</automated>
    <human-check>
      <test>逐台运行 `uv run autoresearch hw probe --server NAME`，再运行 `uv run autoresearch hw probe --all`</test>
      <expected>当前 config 中每台 server 都有完整设备结果；每张卡 memory_total_mib/memory_used_mib/temperature_c/utilization_pct 非 null；aggregate ok=true 且 failed_servers=[]</expected>
      <why_human>真实硬件、SSH/VPN 和驱动输出属于外部状态；必须批量验收，fixture 只能证明 parser</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - `uv run pytest` 全绿，且每个 `hw probe` CLI 至少有 happy path 单测
    - VALIDATION.md 列出的所有 fixture 场景都有业务断言，不存在 no-assertion test
    - config 中每台 server 的单独探测均 exit 0，设备核心字段全部非 null
    - `uv run autoresearch hw probe --all` exit 0、顶层 `ok=true`、`failed_servers=[]`
    - 任一真实 server 不可达或核心字段缺失时，本任务和 Phase 4 不得宣称完成
  </acceptance_criteria>
  <done>所有自动测试和当前全部配置服务器的真实完整探测同时通过，Phase 4 才具备关闭条件</done>
</task>

</tasks>

<verification>
1. `uv run pytest -q tests/test_hw_parser.py tests/test_hw_probe.py tests/test_hw_cli.py`
2. `uv run pytest`
3. 对 `config/config.yaml` 中每个 server 运行 `uv run autoresearch hw probe --server NAME`
4. 运行 `uv run autoresearch hw probe --all`
5. 检查所有设备核心字段非 null、driver versions 有来源、process 记录不含完整命令行、aggregate 无失败服务器。
</verification>

<success_criteria>
- `HW-OCC-01/02` 完整且遵守最小进程信息原则。
- `--all` 最大并发 3，一台失败不阻断其他服务器，但顶层失败。
- 全部 8 个 HW requirement 均有自动覆盖和/或明确真机证据。
- 当前配置全部服务器真机通过；任何网络/SSH/解析阻塞都会阻止 Phase 4 完成。
- 三个 HIGH/MEDIUM threats 的安全行为均被测试或真实闸门验证。
</success_criteria>

## Artifacts this phase produces

- `autoresearch.hw.parser.parse_processes`
- `autoresearch.hw.parser.parse_ps_output`
- `autoresearch.hw.probe.enrich_processes`
- `autoresearch.hw.probe.probe_all`
- `NPUProcess.user`
- `NPUProcess.process_name`
- Click flag `autoresearch hw probe --all`
- Aggregate fields `results/total/passed/failed/warned/passed_servers/failed_servers`
- Fixture path `tests/fixtures/hw/npu_smi_with_processes.txt`
- End-of-phase real-server UAT evidence in `04-03-SUMMARY.md`

<output>
After completion, create `.planning/phases/04-skill-03-server-hardware-probe/04-03-SUMMARY.md`.
</output>
