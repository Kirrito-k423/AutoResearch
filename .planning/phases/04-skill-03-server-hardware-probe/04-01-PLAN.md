---
phase: 04-skill-03-server-hardware-probe
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - autoresearch/hw/__init__.py
  - autoresearch/hw/models.py
  - autoresearch/hw/parser.py
  - autoresearch/hw/probe.py
  - autoresearch/cli.py
  - tests/fixtures/hw/npu_smi_25_3_rc1_no_processes.txt
  - tests/fixtures/hw/npu_smi_missing_metric.txt
  - tests/test_hw_parser.py
  - tests/test_hw_probe.py
  - tests/test_hw_cli.py
autonomous: true
requirements:
  - HW-CONN-01
  - HW-CONN-02
  - HW-NPU-01
  - HW-NPU-02
user_setup:
  - "执行真机检查前连接可访问服务器网段的 LAN/VPN，并确保 config/config.yaml 中 A2-AK-225 的 SSH 凭据可用"
must_haves:
  truths:
    - "D-01/D-02: `autoresearch hw probe --server A2-AK-225` 通过 workspace_core.ssh 探测 Ascend 910 系列，不引入 NVIDIA 路径"
    - "D-04/D-10/D-12: 每张设备的显存总量、已用显存、温度、利用率都有统一单位；缺失时为 null + field_errors，不能用 0 冒充"
    - "D-06/D-11/D-14: 单服务器 CLI 可运行，stdout 只有一个 JSON，进度写 stderr，退出码按 0/1/2 分层"
    - "D-08/D-09/D-13: 返回真实 CheckResult 五字段，data 含 server/devices/processes/driver_versions/warnings/field_errors"
    - "D-22: 设备解析按表头和分隔结构，不按固定字符位置切片"
  artifacts:
    - path: autoresearch/hw/models.py
      must_exist: true
      must_contain: "class NPUDevice"
    - path: autoresearch/hw/parser.py
      must_exist: true
      must_contain: "def parse_npu_smi_info"
    - path: autoresearch/hw/probe.py
      must_exist: true
      must_contain: "def probe_server"
    - path: autoresearch/cli.py
      must_exist: true
      must_contain: "def hw_probe"
  key_links:
    - from: autoresearch/hw/probe.py
      to: workspace-core/ssh/client.py
      via: "SSHClient.connect(connect_timeout=5.0) + SSHClient.exec"
    - from: autoresearch/hw/probe.py
      to: autoresearch/hw/parser.py
      via: "npu-smi stdout passed to pure parser"
    - from: autoresearch/cli.py
      to: autoresearch/hw/probe.py
      via: "hw probe command calls run_probe"
---

<objective>
交付 Phase 4 的第一个纵向结果：真实服务器名经过配置解析、SSH、`npu-smi info`、表头驱动解析后，直接由 `autoresearch hw probe --server NAME` 输出每张 Ascend NPU 的显存、温度和利用率。

本计划结束时用户已经能看到 A2-AK-225 的真实硬件 JSON，而不是只得到模型或解析器零件。兼容补充、驱动文件、lspci fallback、进程补全和 `--all` 留给后续计划。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/04-skill-03-server-hardware-probe/04-CONTEXT.md
@.planning/phases/04-skill-03-server-hardware-probe/04-RESEARCH.md
@.planning/phases/04-skill-03-server-hardware-probe/04-PATTERNS.md
@.planning/phases/04-skill-03-server-hardware-probe/04-VALIDATION.md
@workspace-core/ssh/client.py
@workspace-core/result/check.py
@workspace-core/progress/emitter.py
@autoresearch/ping.py
@autoresearch/cli.py
</context>

<threat_model>
  <threat id="T-04-01-01" severity="HIGH">
    <asset>远程服务器 shell</asset>
    <attack>把用户提供的 server 名或未校验文本拼进远程命令，造成命令注入</attack>
    <mitigation>server 仅做 config 精确键查找；远程命令固定为代码常量；本计划不插值任何用户文本</mitigation>
    <verification>`tests/test_hw_probe.py` 断言执行命令集合只有固定的 `npu-smi info`，未知 server 在连接前以配置错误退出</verification>
  </threat>
  <threat id="T-04-01-02" severity="MEDIUM">
    <asset>解析器可用性与结果正确性</asset>
    <attack>畸形或超长表格令解析器崩溃，或把缺失指标误判为 0</attack>
    <mitigation>纯函数逐行解析、整数转换有边界、未知/缺失字段返回 field_errors，不吞异常、不伪造默认值</mitigation>
    <verification>`tests/test_hw_parser.py` 覆盖缺字段、非数字字段和未知行</verification>
  </threat>
  <threat id="T-04-01-03" severity="HIGH">
    <asset>本地 stdout 和配置秘密</asset>
    <attack>进度、异常对象或配置内容污染最终 JSON，泄露主机凭据</attack>
    <mitigation>stdout 只在 `run_probe` 末尾写一次 JSON；进度和诊断走 stderr；server 元信息不含密码、密钥内容或 secret 字段</mitigation>
    <verification>`tests/test_hw_cli.py` 对 stdout 做单次 `json.loads`，并断言敏感键不存在、进度只出现在 stderr</verification>
  </threat>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 04-01-01: 建立硬件结果契约和 25.3.rc1 核心指标解析器</name>
  <files>
    - autoresearch/hw/__init__.py
    - autoresearch/hw/models.py
    - autoresearch/hw/parser.py
    - tests/fixtures/hw/npu_smi_25_3_rc1_no_processes.txt
    - tests/fixtures/hw/npu_smi_missing_metric.txt
    - tests/test_hw_parser.py
  </files>
  <read_first>
    - AGENTS.md
    - .planning/phases/04-skill-03-server-hardware-probe/04-CONTEXT.md
    - .planning/phases/04-skill-03-server-hardware-probe/04-RESEARCH.md
    - workspace-core/result/check.py
    - autoresearch/services/_common.py
    - autoresearch/hw/models.py
    - autoresearch/hw/parser.py
    - tests/test_hw_parser.py
  </read_first>
  <action>
创建 `autoresearch.hw` 包和硬件领域类型。`NPUDevice` 必须暴露 `id/chip_id/name/health/bus_id/memory_total_mib/memory_used_mib/temperature_c/utilization_pct`；`NPUProcess`、`DriverVersions` 和 `HardwareData` 先建立稳定空结构，供后续计划填充。

实现纯函数 `parse_npu_smi_info(text: str) -> HardwareParseResult`。它按 `|` 分隔表格、规范化表头、识别 A2 样本中的双行设备记录，将 HBM 的 `used / total` 映射到 MiB，将 Temp 映射为整数摄氏度，将 AICore Usage 映射为整数百分比。解析 `No running processes found` 为 `processes=[]`。

每张设备结束时检查四个核心字段。任何字段缺失或无法转整数都保留其他已解析字段，将目标字段设为 `None`，并向 `field_errors` 添加含设备 id、字段名和原因的结构化条目。完全没有设备时返回可识别的 parse failure，不抛出未处理异常。

把 2026-06-09 的 A2-AK-225 输出脱敏后固化为 fixture：8×Ascend 910B2、每卡 65536 MiB、无进程；不得包含 IP、用户名、home 路径或密钥信息。另建缺字段 fixture。
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_hw_parser.py -k "baseline or missing or no_process"</automated>
  </verify>
  <acceptance_criteria>
    - `autoresearch/hw/models.py` 包含 `NPUDevice`、`NPUProcess`、`DriverVersions`、`HardwareData` 类型
    - baseline fixture 解析得到 8 张设备，设备名为 Ascend 910B2，`memory_total_mib == 65536`
    - baseline 每张设备四个核心字段都是 `int` 且 `field_errors == []`
    - 缺字段 fixture 的对应值是 `None`，`field_errors` 非空，其他字段仍保留
    - parser 文件不 import Paramiko、Click 或 filesystem API
  </acceptance_criteria>
  <done>核心指标 parser 能从真实版本 fixture 生成完整设备列表，并明确表达部分解析失败</done>
</task>

<task type="auto">
  <name>Task 04-01-02: 用 workspace_core.ssh 交付单服务器探测编排</name>
  <files>
    - autoresearch/hw/__init__.py
    - autoresearch/hw/probe.py
    - tests/test_hw_probe.py
  </files>
  <read_first>
    - autoresearch/hw/models.py
    - autoresearch/hw/parser.py
    - autoresearch/hw/probe.py
    - workspace-core/ssh/client.py
    - workspace-core/ssh/host.py
    - workspace-core/config/loader.py
    - workspace-core/config/schema.py
    - workspace-core/result/check.py
    - workspace-core/progress/emitter.py
    - autoresearch/ping.py
    - tests/test_ping.py
    - tests/test_hw_probe.py
  </read_first>
  <action>
实现 `resolve_server_host(server_name, config_path=None) -> tuple[ServerSpec, HostSpec]`，沿用 ping 的 config 精确查找和 identity file override，但不要从 `autoresearch.ping` 跨 feature import 私有函数。

实现 `probe_server(server_name, config_path=None, ssh_client_factory=SSHClient) -> CheckResult`。每次调用创建独立 `SSHClient`，显式 `connect(connect_timeout=5.0)`，执行固定命令 `npu-smi info`，把 stdout 交给 parser，并在 finally 中关闭连接。连接、命令、解析阶段分别用 `emit_progress`。

结果必须是 `workspace_core.result.CheckResult` 的五字段结构。`data` 至少含脱敏 server 元信息、`devices/processes/driver_versions/warnings/field_errors/fallback/raw_log_path`。所有核心字段齐全且无 warning 时 `ok=true/severity=ok`；核心字段齐全但仅有非核心 warning 时 `ok=true/severity=warn`；任一核心字段缺失、命令非零或 SSH 异常时 `ok=false/severity=fail`，但 parser 已取得的设备数据仍保留。不要调用当前会把 WARN 设为 `ok=false` 的 `fail(..., WARN)` 或 `merge()`；硬件域显式构造 warning success。错误文本区分连接失败、命令失败和解析失败。
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_hw_probe.py -k "single or timeout or partial"</automated>
  </verify>
  <acceptance_criteria>
    - fake SSH happy path 只执行 `npu-smi info`，返回 `ok=true`、`severity="ok"` 和 8 张设备
    - `connect` 使用 5.0 秒 timeout；连接异常变为 FAIL，不泄露配置 secret
    - 命令 exit code 非 0 时返回 exit-domain failure 数据，未抛到 CLI
    - 缺一个核心字段时返回 `ok=false`、`severity="fail"`，且其他设备/字段仍在 data
    - 每个 fake client 都被关闭
  </acceptance_criteria>
  <done>单服务器 SSH→命令→解析→CheckResult 的完整调用链可独立测试</done>
</task>

<task type="auto">
  <name>Task 04-01-03: 挂载 `autoresearch hw probe --server` 并跑首台真机</name>
  <files>
    - autoresearch/cli.py
    - autoresearch/hw/probe.py
    - tests/test_hw_cli.py
  </files>
  <read_first>
    - autoresearch/cli.py
    - autoresearch/hw/probe.py
    - autoresearch/ping.py
    - tests/test_ping.py
    - tests/test_hw_cli.py
    - config/config.yaml
  </read_first>
  <action>
在根 Click tree 新增 `hw` group 和 `probe` command。本计划只开放必填 `--server NAME`，并保留 `--config PATH`、`--lang zh|en`；04-03 再扩 `--all`。实现 `run_probe(server, all_servers=False, config=None, lang="zh") -> int`，配置/usage 错误返回 2，探测或解析 FAIL 返回 1，OK/WARN 且核心指标完整返回 0。

无论成功失败，stdout 最终只调用一次 `json.dumps` 输出一个 CheckResult JSON；severity 序列化为小写字符串。stderr 只放 progress 和可读诊断。CLI 测试通过 monkeypatch `probe_server` 验证 happy/fail/config 三类退出码。

自动测试通过后运行 `uv run autoresearch hw probe --server A2-AK-225`。若 LAN/VPN 仍不可达，保留失败证据并停止本计划完成标记；不得用 fixture 替代这一真机检查。
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_hw_parser.py tests/test_hw_probe.py tests/test_hw_cli.py</automated>
    <human-check>
      <test>在可访问服务器网段时运行 `uv run autoresearch hw probe --server A2-AK-225`</test>
      <expected>exit 0；stdout 是单个 JSON；8 张 910B2 每张都有非 null 的 memory_total_mib、memory_used_mib、temperature_c、utilization_pct；stderr 有 `__AR_PROGRESS__=`</expected>
      <why_human>真实 SSH、驱动版本和现场硬件不能由 fixture 证明；2026-06-09 规划时服务器曾临时超时</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - `uv run autoresearch hw probe --help` 同时显示 `--server`、`--config`、`--lang`
    - happy path `result.stdout` 可被一次 `json.loads` 解析，且 `result.stderr` 包含 progress marker
    - 配置错误 exit 2；SSH/命令/核心解析失败 exit 1；完整核心指标 exit 0
    - `autoresearch/cli.py` 含 `hw` group 和 `hw_probe` command
    - A2-AK-225 真机检查通过；若环境不可达，本计划保持未完成并记录阻塞
  </acceptance_criteria>
  <done>用户可用一个真实服务器名直接看到 Ascend NPU 核心硬件 JSON</done>
</task>

</tasks>

<verification>
1. `uv run pytest -q tests/test_hw_parser.py tests/test_hw_probe.py tests/test_hw_cli.py`
2. `uv run pytest`
3. `uv run autoresearch hw probe --server A2-AK-225`
4. 检查 stdout 是唯一 JSON，8 张卡的四个核心字段均非 null，错误分支保持部分数据。
</verification>

<success_criteria>
- `HW-CONN-01/02`：单服务器配置查找和 5 秒 SSH 连接路径可用，超时可读。
- `HW-NPU-01/02`：真实 25.3.rc1 fixture 和 A2 真机都能列出全部卡及核心指标。
- 没有新增 Paramiko 直连、没有 stdout 进度污染、没有缺失值伪装为 0。
- 三个 HIGH threat 都有自动测试或真机验证证据。
</success_criteria>

## Artifacts this phase produces

- `autoresearch.hw.models.NPUDevice`
- `autoresearch.hw.models.NPUProcess`
- `autoresearch.hw.models.DriverVersions`
- `autoresearch.hw.models.HardwareData`
- `autoresearch.hw.parser.parse_npu_smi_info`
- `autoresearch.hw.probe.resolve_server_host`
- `autoresearch.hw.probe.probe_server`
- `autoresearch.hw.probe.run_probe`
- Click group `autoresearch hw`
- Click command `autoresearch hw probe --server NAME`
- Fixture paths `tests/fixtures/hw/npu_smi_25_3_rc1_no_processes.txt` and `npu_smi_missing_metric.txt`

<output>
After completion, create `.planning/phases/04-skill-03-server-hardware-probe/04-01-SUMMARY.md`.
</output>
