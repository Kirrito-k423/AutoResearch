---
phase: 04-skill-03-server-hardware-probe
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - autoresearch/hw/models.py
  - autoresearch/hw/parser.py
  - autoresearch/hw/probe.py
  - tests/fixtures/hw/npu_smi_variant.txt
  - tests/fixtures/hw/npu_smi_unknown_format.txt
  - tests/fixtures/hw/lspci_ascend.txt
  - tests/fixtures/hw/driver_version_info.txt
  - tests/test_hw_parser.py
  - tests/test_hw_probe.py
autonomous: true
requirements:
  - HW-NPU-01
  - HW-NPU-02
  - HW-NPU-03
  - HW-DRV-01
must_haves:
  truths:
    - "D-02/D-23: Ascend 910 系列解析不绑定单一 910B2 版本，fixture 覆盖多版本、字段缺失和未知格式"
    - "D-15/D-16/D-17: 成功不返回 raw output；失败写本地日志、只返有界摘要和路径，并保留部分数据"
    - "D-24/D-25: npu-smi 不可用或未知格式时 fallback 到 lspci，但动态核心指标缺失使结果保持 FAIL"
    - "D-26: 同时报告 npu-smi header 和 driver version.info，文件缺失只产生 warning"
  artifacts:
    - path: autoresearch/hw/parser.py
      must_exist: true
      must_contain: "def parse_lspci_devices"
    - path: autoresearch/hw/parser.py
      must_exist: true
      must_contain: "def parse_driver_version_info"
    - path: autoresearch/hw/probe.py
      must_exist: true
      must_contain: "def write_raw_failure_log"
  key_links:
    - from: autoresearch/hw/probe.py
      to: workspace-core/layout/paths.py
      via: "failed raw output written under LOGS_DIR"
    - from: autoresearch/hw/probe.py
      to: autoresearch/hw/parser.py
      via: "typed query and lspci outputs merged into partial hardware data"
---

<objective>
让单服务器探测面对不同 Ascend 910 驱动输出时可扩展、可诊断：增加 typed query 能力探测、驱动版本、未知格式日志和 `lspci` 降级。

本计划不改变 04-01 的主路径和 JSON 契约；它只扩充兼容输入和失败信息，确保“看到设备存在”不会被误报为核心指标探测成功。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/04-skill-03-server-hardware-probe/04-CONTEXT.md
@.planning/phases/04-skill-03-server-hardware-probe/04-RESEARCH.md
@.planning/phases/04-skill-03-server-hardware-probe/04-VALIDATION.md
@.planning/phases/04-skill-03-server-hardware-probe/04-01-SUMMARY.md
@autoresearch/hw/models.py
@autoresearch/hw/parser.py
@autoresearch/hw/probe.py
@workspace-core/layout/paths.py
@workspace-core/log/logger.py
</context>

<threat_model>
  <threat id="T-04-02-01" severity="HIGH">
    <asset>远程服务器 shell 与探测可信度</asset>
    <attack>把不受支持的查询参数或输出内容拼接成后续命令</attack>
    <mitigation>typed query 由固定命令模板和解析后的整数 device id 生成；退出码非零只记 warning，不执行输出驱动的命令</mitigation>
    <verification>`tests/test_hw_probe.py` 断言 typed command allowlist 和非零退出路径</verification>
  </threat>
  <threat id="T-04-02-02" severity="MEDIUM">
    <asset>远程系统完整性</asset>
    <attack>驱动版本采集写入或修改远端文件</attack>
    <mitigation>只执行固定 `cat /usr/local/Ascend/driver/version.info`，不创建、不 chmod、不 sudo</mitigation>
    <verification>fake SSH 断言 driver command 精确等于只读 cat 命令</verification>
  </threat>
  <threat id="T-04-02-03" severity="HIGH">
    <asset>本地敏感数据与完成状态</asset>
    <attack>raw output 进入 stdout 泄露信息，或 lspci 设备存在被误判为完整成功</attack>
    <mitigation>raw 仅在失败时写本地 LOGS_DIR，JSON 摘要最长 512 字符；fallback 无条件 FAIL 且核心字段为 null</mitigation>
    <verification>`tests/test_hw_probe.py` 断言日志位置/摘要长度/成功无 raw，以及 fallback severity=fail</verification>
  </threat>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 04-02-01: 扩展表头解析和 typed query 核心指标补充</name>
  <files>
    - autoresearch/hw/models.py
    - autoresearch/hw/parser.py
    - autoresearch/hw/probe.py
    - tests/fixtures/hw/npu_smi_variant.txt
    - tests/fixtures/hw/npu_smi_unknown_format.txt
    - tests/test_hw_parser.py
    - tests/test_hw_probe.py
  </files>
  <read_first>
    - .planning/phases/04-skill-03-server-hardware-probe/04-RESEARCH.md
    - autoresearch/hw/models.py
    - autoresearch/hw/parser.py
    - autoresearch/hw/probe.py
    - tests/fixtures/hw/npu_smi_25_3_rc1_no_processes.txt
    - tests/test_hw_parser.py
    - tests/test_hw_probe.py
  </read_first>
  <action>
把 parser 的列名映射集中为可扩展 alias 表，支持大小写、空格和单位变化；保持表头/分隔结构驱动，不引入固定字符索引。新增至少一个 sanitized variant fixture 和一个完全未知格式 fixture。

实现 typed output 的纯解析入口，支持把 memory/temp/usages 查询结果按 integer device id 合并回已有 `NPUDevice`。在 probe 中仅当默认表缺核心字段时尝试固定候选命令：`npu-smi info -t memory -i <id>`、`npu-smi info -t temp -i <id>`、`npu-smi info -t usages -i <id>`。`<id>` 必须来自已解析整数。某命令不受支持或输出未知时记 warning，不能覆盖已成功值，也不能把服务器升级为成功。

明确 merge 优先级：默认表有效值优先；typed query 只填 `None`；冲突值保留默认表并增加 warning。若补充后四个核心字段全部完整，可按核心指标成功处理，但保留“默认格式部分未知/typed supplement used” warning。
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_hw_parser.py tests/test_hw_probe.py -k "variant or typed or unknown"</automated>
  </verify>
  <acceptance_criteria>
    - variant fixture 在不修改固定列宽的情况下得到正确设备数和核心字段
    - typed query 只在核心字段为 `None` 时执行，device id 全部是整数
    - typed query 非零退出不抛异常、不覆盖已有值，并在 warnings 中说明 command/type
    - unknown fixture 返回明确 parse failure，不产生空的假成功设备
    - 冲突 typed 值不会静默覆盖主表值
  </acceptance_criteria>
  <done>核心 parser 能适应表头变化，并用受控 typed query 填补而非掩盖缺失</done>
</task>

<task type="auto">
  <name>Task 04-02-02: 采集 npu-smi 与 Ascend driver 版本</name>
  <files>
    - autoresearch/hw/models.py
    - autoresearch/hw/parser.py
    - autoresearch/hw/probe.py
    - tests/fixtures/hw/driver_version_info.txt
    - tests/test_hw_parser.py
    - tests/test_hw_probe.py
  </files>
  <read_first>
    - autoresearch/hw/models.py
    - autoresearch/hw/parser.py
    - autoresearch/hw/probe.py
    - tests/fixtures/hw/npu_smi_25_3_rc1_no_processes.txt
    - tests/test_hw_parser.py
    - tests/test_hw_probe.py
  </read_first>
  <action>
在 `parse_npu_smi_info` 中提取 header 中的 `npu-smi`/driver software version。实现 `parse_driver_version_info(text)`，至少识别 `Version=` 和 `package_version=`，映射到 `DriverVersions.npu_smi/driver/package`。

probe 在主命令后执行固定只读命令 `cat /usr/local/Ascend/driver/version.info`。文件不存在或命令非零时保留 header 版本并追加 warning，结果为 `ok=true/severity=warn`，不令核心指标成功的服务器 FAIL；文件可用时合并字段，不在 error/message 中输出整份文件。
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_hw_parser.py tests/test_hw_probe.py -k driver</automated>
  </verify>
  <acceptance_criteria>
    - A2 fixture 提取 `npu_smi == "25.3.rc1"`
    - driver fixture 提取 `Version=25.3.rc1` 和 `package_version=25.3.rc1`
    - probe 执行的 driver 命令是精确固定只读路径
    - driver 文件缺失时核心指标完整的结果为 `ok=true/severity="warn"`，header 版本仍保留
    - JSON 不包含 driver 文件完整 raw text
  </acceptance_criteria>
  <done>HW-DRV-01 的两个版本来源都进入稳定 JSON，缺少文件时可诊断但不误伤核心指标</done>
</task>

<task type="auto">
  <name>Task 04-02-03: 增加 lspci fallback 和本地失败原始日志</name>
  <files>
    - autoresearch/hw/models.py
    - autoresearch/hw/parser.py
    - autoresearch/hw/probe.py
    - tests/fixtures/hw/lspci_ascend.txt
    - tests/test_hw_parser.py
    - tests/test_hw_probe.py
  </files>
  <read_first>
    - .planning/phases/04-skill-03-server-hardware-probe/04-CONTEXT.md
    - autoresearch/hw/models.py
    - autoresearch/hw/parser.py
    - autoresearch/hw/probe.py
    - workspace-core/layout/paths.py
    - workspace-core/log/logger.py
    - tests/test_hw_parser.py
    - tests/test_hw_probe.py
  </read_first>
  <action>
实现 `parse_lspci_devices(text)`，只收集 Huawei `Processing accelerators`（允许已知 Ascend accelerator 描述/ID alias），输出 PCI 地址和原始单行描述；明确排除 Huawei bridge、NIC 和 management controller。

当 `npu-smi info` 命令不存在、非零且不能解析，或输出完全未知时执行固定 `lspci -Dnn`。把 fallback 设备放入 `data.devices` 的 presence-only 结构，四个核心动态字段全部 `None`，`fallback="lspci"`，最终 `ok=false/severity=fail`。

实现 `write_raw_failure_log(server_name, command_records) -> Path`：server 名只保留 `[A-Za-z0-9_.-]`，日志写 `LOGS_DIR/hw-<safe>-<UTC>.log`，含命令、exit code、stdout、stderr。只在失败/未知格式时调用。JSON 返回 `raw_log_path` 和最多 512 字符 `raw_output_summary`；成功路径两者均为 null。
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_hw_parser.py tests/test_hw_probe.py -k "lspci or fallback or raw_log"</automated>
  </verify>
  <acceptance_criteria>
    - lspci fixture 只统计 Ascend processing accelerator，不统计同厂 bridge/NIC
    - fallback 结果含 PCI 地址和描述，但四个核心指标均为 `None`
    - fallback 无条件 `ok=false`、`severity="fail"`，`field_errors` 列出动态指标不可用
    - 失败日志位于 monkeypatched 本地 LOGS_DIR，摘要 UTF-8 截断后不超过 512 字符
    - 成功探测不创建 raw log，也不在 JSON 填 raw output
  </acceptance_criteria>
  <done>未知 `npu-smi` 输出不再是黑盒，用户能看到设备存在和本地诊断路径，但不会得到假成功</done>
</task>

</tasks>

<verification>
1. `uv run pytest -q tests/test_hw_parser.py tests/test_hw_probe.py`
2. `uv run pytest`
3. 在 A2-AK-225 上运行 `uv run autoresearch hw probe --server A2-AK-225`，确认 driver_versions 已填且成功 JSON 不含 raw output。
4. 用 fake SSH 的 unknown fixture 确认 lspci fallback 是 FAIL 并返回本地日志路径。
</verification>

<success_criteria>
- `HW-NPU-01/02` 对表头变化和补充查询有测试保障。
- `HW-NPU-03` 的 lspci fallback 能识别设备但绝不伪造核心动态指标成功。
- `HW-DRV-01` 同时覆盖 npu-smi header 和 driver version file。
- D-15..D-17 的本地诊断规则和三个 security threats 都有自动断言。
</success_criteria>

## Artifacts this phase produces

- `autoresearch.hw.parser.parse_typed_metric_output`
- `autoresearch.hw.parser.parse_driver_version_info`
- `autoresearch.hw.parser.parse_lspci_devices`
- `autoresearch.hw.probe.write_raw_failure_log`
- Fixed typed query command builders/constants
- Fixed driver command `cat /usr/local/Ascend/driver/version.info`
- Fixed fallback command `lspci -Dnn`
- Fixture paths `npu_smi_variant.txt`, `npu_smi_unknown_format.txt`, `driver_version_info.txt`, `lspci_ascend.txt`
- `HardwareData.fallback`
- `HardwareData.raw_log_path`
- `HardwareData.raw_output_summary`

<output>
After completion, create `.planning/phases/04-skill-03-server-hardware-probe/04-02-SUMMARY.md`.
</output>
