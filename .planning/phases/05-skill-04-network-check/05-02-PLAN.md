---
phase: 05-skill-04-network-check
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - autoresearch/net/models.py
  - autoresearch/net/tunnel.py
  - autoresearch/net/probe.py
  - workspace-core/layout/paths.py
  - tests/test_net_tunnel.py
  - tests/test_net_probe.py
  - tests/workspace-core/test_layout.py
autonomous: true
requirements:
  - NET-REMOTE-01
  - NET-REMOTE-02
  - NET-REMOTE-03
  - NET-TUNNEL-01
must_haves:
  truths:
    - "D-14: remote direct failure automatically falls back through `ssh -R <remote_proxy_port>:127.0.0.1:7890` and remote `ALL_PROXY`"
    - "D-15/D-16: remote proxy port defaults to 17890; local proxy URL defaults to `http://127.0.0.1:7890` and can be overridden"
    - "D-17: proxy unavailable is explicit in rows and participates in failure semantics"
    - "D-18: Phase 5 owns reusable tunnel ensure for Phase 6"
    - "D-19/D-20: tunnel state lives in `~/.autoresearch/tunnels/<server>.json`; healthy existing tunnels are reused, failed ones rebuilt"
    - "D-23/D-24: higher-level tunnel lifecycle builds on `open_reverse_tunnel`; tunnel process/state remain local to the Mac"
    - "D-25: tunnel JSON exposes only log path plus redacted 500-char tail, never complete raw ssh output"
  artifacts:
    - path: autoresearch/net/tunnel.py
      must_exist: true
      must_contain: "def ensure_tunnel"
    - path: autoresearch/net/tunnel.py
      must_exist: true
      must_contain: "def load_tunnel_state"
    - path: autoresearch/net/probe.py
      must_exist: true
      must_contain: "proxy"
    - path: workspace-core/layout/paths.py
      must_exist: true
      must_contain: "TUNNELS_DIR"
  key_links:
    - from: autoresearch/net/tunnel.py
      to: workspace-core/ssh/tunnel.py
      via: "ensure_tunnel calls open_reverse_tunnel"
    - from: autoresearch/net/probe.py
      to: autoresearch/net/tunnel.py
      via: "remote direct failures call ensure_tunnel then retry curl with ALL_PROXY"
    - from: autoresearch/net/tunnel.py
      to: workspace-core/layout/paths.py
      via: "state is stored under TUNNELS_DIR"
---

<objective>
把 Phase 5 的“远程无网自动建反向代理”接到网络矩阵：当远程 direct curl 失败时，本地自动 ensure 一条 `ssh -R` 到 Mac 代理的隧道，再用远程 `ALL_PROXY` 重试，并把 direct/proxy attempts 都保留在同一 row。

本计划建立可复用 tunnel state 和 ensure/rebuild 语义，但 30s heartbeat 循环、retry supervisor 和 tunnel CLI 完整体验留给 05-03。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/05-skill-04-network-check/05-CONTEXT.md
@.planning/phases/05-skill-04-network-check/05-PATTERNS.md
@.planning/phases/05-skill-04-network-check/05-01-SUMMARY.md
@workspace-core/ssh/tunnel.py
@workspace-core/ssh/client.py
@workspace-core/layout/paths.py
@autoresearch/net/probe.py
@autoresearch/net/curl.py
</context>

<threat_model>
  <threat id="T-05-02-01" severity="HIGH">
    <asset>本机代理凭据与 SSH 日志</asset>
    <attack>自定义 proxy URL 中的用户名密码进入 JSON、state 文件或 tunnel log 摘要</attack>
    <mitigation>所有 user-facing/state/error 输出都通过 redaction；state 只保留脱敏 proxy URL 或分离字段</mitigation>
    <verification>`tests/test_net_tunnel.py` 断言 `http://user:pass@127.0.0.1:7890` 在 JSON/state/log tail 中被脱敏</verification>
  </threat>
  <threat id="T-05-02-02" severity="MEDIUM">
    <asset>本地隧道进程生命周期</asset>
    <attack>复用已经失效或 pid 被重用的 state，导致 proxy retry 假成功/假失败</attack>
    <mitigation>ensure 必须验证进程存活和基础 heartbeat；失败时停止旧进程并重建状态</mitigation>
    <verification>mock state + dead process 测试断言会重建并覆盖旧 pid</verification>
  </threat>
  <threat id="T-05-02-03" severity="HIGH">
    <asset>远程服务器 shell</asset>
    <attack>remote proxy env 或 target URL 未 quote，导致远程命令注入</attack>
    <mitigation>remote `ALL_PROXY` 值来自 validated local parameters；target 仍走 05-01 URL allowlist 和 safe quoting</mitigation>
    <verification>`tests/test_net_probe.py` 断言 proxy retry 远程命令只包含 expected `ALL_PROXY=http://127.0.0.1:17890` 和 quoted http/https target</verification>
  </threat>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 05-02-01: 建立 tunnel state、proxy URL 脱敏和 TUNNELS_DIR</name>
  <files>
    - autoresearch/net/models.py
    - autoresearch/net/tunnel.py
    - workspace-core/layout/paths.py
    - tests/test_net_tunnel.py
    - tests/workspace-core/test_layout.py
  </files>
  <read_first>
    - .planning/phases/05-skill-04-network-check/05-CONTEXT.md
    - workspace-core/layout/paths.py
    - workspace-core/ssh/tunnel.py
    - tests/workspace-core/test_layout.py
    - tests/workspace-core/test_tunnel.py
  </read_first>
  <action>
在 `workspace-core/layout/paths.py` 新增 `TUNNELS_DIR = ROOT_DIR / "tunnels"`，并让 layout ensure helper 覆盖该目录（若当前 helper 存在）。

在 `autoresearch/net/tunnel.py` 定义 `TunnelState` 结构，字段包括 `server/pid/remote_port/local_proxy_url/started_at/log_path/last_heartbeat_at/last_heartbeat_ok/error`。实现 `redact_proxy_url(url)`，隐藏 userinfo；实现 `state_path_for(server_name)`，server 名只允许 `[A-Za-z0-9_.-]`，其他字符转 `_`。

实现 `load_tunnel_state(server_name)`、`write_tunnel_state(state)`、`delete_tunnel_state(server_name)`。state 文件必须是本地 JSON，不写入 identity file、bootstrap secret、remote host/user 或未脱敏 proxy 凭据。
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_net_tunnel.py tests/workspace-core/test_layout.py -k "state or redact or tunnels_dir"</automated>
  </verify>
  <acceptance_criteria>
    - `workspace-core/layout/paths.py` 暴露 `TUNNELS_DIR`
    - `redact_proxy_url("http://u:p@127.0.0.1:7890")` 不包含 `u:p`
    - `state_path_for("bad/name;server")` 文件名不含 `/` 或 `;`
    - 写出的 state JSON 不包含 `identity_file`、`bootstrap_password_secret`、`password`
    - `load_tunnel_state` 在文件不存在或 JSON 损坏时返回明确的可恢复状态，不抛未处理异常到 CLI
  </acceptance_criteria>
  <done>可复用 tunnel 的本地状态和脱敏边界就位</done>
</task>

<task type="auto">
  <name>Task 05-02-02: 实现 `ensure_tunnel` 复用/重建和远程 proxy URL</name>
  <files>
    - autoresearch/net/tunnel.py
    - tests/test_net_tunnel.py
  </files>
  <read_first>
    - autoresearch/net/tunnel.py
    - workspace-core/ssh/tunnel.py
    - workspace-core/ssh/client.py
    - workspace-core/config/loader.py
    - autoresearch/ping.py
    - tests/workspace-core/test_tunnel.py
    - tests/test_net_tunnel.py
  </read_first>
  <action>
实现 `resolve_server_host(server_name, config_path=None)`，保持与 Phase 4 exact config lookup 一致。实现 `is_process_alive(pid)` 和基础 `heartbeat_tunnel(...)` 占位能力：本计划至少检查本地进程存活；05-03 会补远程 proxy curl heartbeat。

实现 `ensure_tunnel(server_name, config_path=None, local_proxy_url="http://127.0.0.1:7890", remote_proxy_port=17890, tunnel_opener=open_reverse_tunnel) -> TunnelState`。流程：读取 state；若 pid 存活且基础 heartbeat 通过则复用；否则停止/清理旧状态并调用 `open_reverse_tunnel(host, remote_port=remote_proxy_port, local_port=<local_proxy_port>, identity_file=host.identity_file)`。

从 local proxy URL 解析本地端口，默认 7890；不支持非 localhost/127.0.0.1 host 时返回配置错误。写入新 state，并返回远程可用 proxy URL `http://127.0.0.1:<remote_proxy_port>` 供 probe 使用。tunnel 启动失败时返回/抛出可被 CLI 转成 FAIL 的错误，错误摘要只含脱敏 log tail。
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_net_tunnel.py -k "ensure or reuse or rebuild or remote_proxy"</automated>
  </verify>
  <acceptance_criteria>
    - 无 state 时调用 `open_reverse_tunnel`，remote_port 默认 17890，local_port 默认 7890
    - 健康 state + 活 pid 时不重复打开 tunnel，返回原 remote_proxy_port
    - dead pid 或 heartbeat false 时清理旧 state 并重建
    - 非 localhost proxy host 或非法 proxy URL 返回配置错误
    - tunnel 启动失败的用户可见摘要不超过 500 字符且已脱敏
  </acceptance_criteria>
  <done>`ensure_tunnel` 可以建立、复用和重建本地持有的反向代理隧道</done>
</task>

<task type="auto">
  <name>Task 05-02-03: 把远程 proxy fallback 接入网络矩阵</name>
  <files>
    - autoresearch/net/probe.py
    - autoresearch/net/tunnel.py
    - tests/test_net_probe.py
    - tests/test_net_tunnel.py
  </files>
  <read_first>
    - autoresearch/net/probe.py
    - autoresearch/net/curl.py
    - autoresearch/net/tunnel.py
    - tests/test_net_probe.py
    - tests/test_net_tunnel.py
  </read_first>
  <action>
扩展 `probe_remote_direct` 或新增 `probe_remote_with_proxy_fallback`：每个 target 先执行 direct curl；direct 成功则不建 tunnel；direct 失败时调用 `ensure_tunnel`，然后远程重试同一 target，命令前缀设置 `ALL_PROXY=http://127.0.0.1:<remote_proxy_port>` 和 `HTTPS_PROXY`。

同一个 `NetworkRow` 保留 direct attempt 和 proxy attempt。proxy 成功时 `effective_mode="proxy"`；proxy 不可用时 `error` 或 attempt error 标出 `proxy_unavailable`。summary 必须能统计哪些服务器/target 需要 proxy 才通，以及 proxy 后仍失败的 target。

更新 `run_probe` 接收 `remote_proxy_port` 和 `local_proxy_url`，传入 fallback 逻辑。CLI flag `--remote-proxy-port` 可以先在本任务或 05-03 挂载；若本任务不挂 CLI，必须保证函数参数已经存在并有测试。
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_net_probe.py tests/test_net_tunnel.py -k "proxy_fallback or remote_proxy_port or proxy_unavailable"</automated>
  </verify>
  <acceptance_criteria>
    - remote direct success 不调用 `ensure_tunnel`
    - remote direct failure 调用 `ensure_tunnel` 一次，并追加 proxy attempt
    - proxy retry 命令包含 `ALL_PROXY=http://127.0.0.1:17890`
    - ensure_tunnel 抛出 proxy unavailable 时 row 保留 direct attempt 和失败 proxy attempt
    - summary 暴露 `proxy_used` / `proxy_failed` 计数或等价字段
  </acceptance_criteria>
  <done>远程无网时自动建 `ssh -R` 并重试，满足 NET-TUNNEL-01 的业务路径</done>
</task>

</tasks>

<verification>
1. `uv run pytest -q tests/test_net_tunnel.py tests/test_net_probe.py`
2. `uv run pytest -q tests/test_net_curl.py tests/test_net_probe.py tests/test_net_cli.py tests/test_net_tunnel.py`
3. 在网络可达时运行 `uv run autoresearch net probe --server A2-AK-225 --remote-proxy-port 17890`
</verification>

<success_criteria>
- `NET-TUNNEL-01`：远程 direct 失败会自动建立 `ssh -R` 并通过 remote proxy retry。
- `D-14..D-20`、`D-23..D-25` 在代码和测试中有可追踪实现。
- proxy URL 凭据、SSH identity、bootstrap secret 不进入 JSON/state/log 摘要。
- 远程 fallback 不破坏 05-01 的 rows + attempts + summary 契约。
</success_criteria>

## Artifacts this phase produces

- `workspace_core.layout.TUNNELS_DIR`
- `autoresearch.net.tunnel.TunnelState`
- `autoresearch.net.tunnel.redact_proxy_url`
- `autoresearch.net.tunnel.state_path_for`
- `autoresearch.net.tunnel.load_tunnel_state`
- `autoresearch.net.tunnel.write_tunnel_state`
- `autoresearch.net.tunnel.delete_tunnel_state`
- `autoresearch.net.tunnel.ensure_tunnel`
- `autoresearch.net.probe.probe_remote_with_proxy_fallback`
- CLI/function option `remote_proxy_port`
- Summary fields `proxy_used` and `proxy_failed`

<output>
After completion, create `.planning/phases/05-skill-04-network-check/05-02-SUMMARY.md`.
</output>

