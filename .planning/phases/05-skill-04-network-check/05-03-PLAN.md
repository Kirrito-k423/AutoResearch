---
phase: 05-skill-04-network-check
plan: 03
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - autoresearch/net/tunnel.py
  - autoresearch/net/probe.py
  - autoresearch/cli.py
  - tests/test_net_tunnel.py
  - tests/test_net_probe.py
  - tests/test_net_cli.py
autonomous: true
requirements:
  - NET-LOCAL-01
  - NET-LOCAL-02
  - NET-LOCAL-03
  - NET-REMOTE-01
  - NET-REMOTE-02
  - NET-REMOTE-03
  - NET-TUNNEL-01
  - NET-TUNNEL-02
user_setup:
  - "执行真实 UAT 前连接可访问全部配置服务器的 LAN/VPN，确认 Mac 本机代理 `127.0.0.1:7890` 可用"
must_haves:
  truths:
    - "D-21: tunnel heartbeat checks both local ssh process and remote proxy curl"
    - "D-22: heartbeat interval is 30s; reconnect retries are capped at 3 with 1s/2s/4s backoff"
    - "D-24/D-25: tunnel state/logs remain local and only redacted 500-char tails appear in JSON"
    - "D-26/D-27: Phase 5 cannot be complete until every configured server passes real net probe + tunnel fallback UAT"
    - "D-28: tests cover curl parsing, URL rejection, attempts, proxy unavailable, tunnel reuse/rebuild, heartbeat retry, JSON stdout and redaction"
    - "D-29: multi-server remote probing reuses bounded concurrency and failure isolation; one server failure does not cancel others"
    - "D-18/D-19/D-20: `net tunnel ensure` is exposed for Phase 6 and reuses/rebuilds state correctly"
  artifacts:
    - path: autoresearch/net/tunnel.py
      must_exist: true
      must_contain: "def heartbeat_tunnel"
    - path: autoresearch/net/tunnel.py
      must_exist: true
      must_contain: "def ensure_tunnel"
    - path: autoresearch/cli.py
      must_exist: true
      must_contain: "def net_tunnel_ensure"
    - path: autoresearch/cli.py
      must_exist: true
      must_contain: "--remote-proxy-port"
  key_links:
    - from: autoresearch/cli.py
      to: autoresearch/net/tunnel.py
      via: "net tunnel ensure calls run_tunnel_ensure"
    - from: autoresearch/net/tunnel.py
      to: workspace-core/ssh/client.py
      via: "heartbeat uses SSHClient.exec to run remote proxy curl"
    - from: autoresearch/net/probe.py
      to: autoresearch/net/tunnel.py
      via: "probe uses ensure_tunnel before proxy retry"
---

<objective>
完成 Phase 5 闭环：隧道有 30s heartbeat 和 3 次重连策略，`autoresearch net tunnel ensure` 可给 Phase 6 复用，`net probe` 暴露完整 proxy 参数，并用全部配置服务器真实 UAT 作为最终完成闸门。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/05-skill-04-network-check/05-CONTEXT.md
@.planning/phases/05-skill-04-network-check/05-PATTERNS.md
@.planning/phases/05-skill-04-network-check/05-02-SUMMARY.md
@autoresearch/net/tunnel.py
@autoresearch/net/probe.py
@autoresearch/cli.py
@workspace-core/ssh/client.py
@workspace-core/ssh/tunnel.py
@config/config.yaml
</context>

<threat_model>
  <threat id="T-05-03-01" severity="HIGH">
    <asset>Phase 5 完成声明真实性</asset>
    <attack>用 mock、单台服务器或本机成功替代全部配置服务器真实网络/隧道 UAT</attack>
    <mitigation>最终任务强制逐台真实 `net probe` 和 aggregate `net probe`；任一服务器失败保持计划/阶段 blocked</mitigation>
    <verification>05-03 SUMMARY 记录每台 server name、exit code、rows 数、severity 和失败分类；不得记录 host/user/secret</verification>
  </threat>
  <threat id="T-05-03-02" severity="MEDIUM">
    <asset>本地进程稳定性</asset>
    <attack>无限重连或 orphan tunnel 进程耗尽资源</attack>
    <mitigation>retry capped at 3 with 1s/2s/4s backoff; stop/status helpers clean local process/state; no infinite loop in tests</mitigation>
    <verification>`tests/test_net_tunnel.py` 断言 retry 次数、backoff 序列和 stop 清理 state</verification>
  </threat>
  <threat id="T-05-03-03" severity="HIGH">
    <asset>远程 shell 与代理链路</asset>
    <attack>heartbeat 只检查进程导致链路假活，后续 Phase 6 误以为 wandb/prom 可达</attack>
    <mitigation>heartbeat 同时检查 ssh pid 和远程 proxy curl；proxy curl 失败会触发 rebuild 或 FAIL</mitigation>
    <verification>fake SSH 返回 process alive 但 proxy curl fail 时，heartbeat 返回 false 并触发 retry/rebuild</verification>
  </threat>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 05-03-01: 增加远程 proxy curl heartbeat 和 3 次重连</name>
  <files>
    - autoresearch/net/tunnel.py
    - tests/test_net_tunnel.py
  </files>
  <read_first>
    - .planning/phases/05-skill-04-network-check/05-CONTEXT.md
    - autoresearch/net/tunnel.py
    - autoresearch/net/curl.py
    - workspace-core/ssh/client.py
    - workspace-core/ssh/tunnel.py
    - tests/test_net_tunnel.py
  </read_first>
  <action>
完善 `heartbeat_tunnel(server_name, state, config_path=None, target_url=None, ssh_client_factory=SSHClient) -> bool`。它必须先确认本地 `ssh` pid 存活，再用 SSH 在远程执行一个带 `ALL_PROXY=http://127.0.0.1:<remote_port>` 的短 timeout curl。默认 heartbeat target 优先 baidu；config 中无 baidu 时用第一个 http/https target。

扩展 `ensure_tunnel`：heartbeat false 时最多重建 3 次，backoff 为 1s、2s、4s；每次写 progress：`net.tunnel.heartbeat`、`net.tunnel.retry`、`net.tunnel.ready` 或 `net.tunnel.fail`。所有 sleep/backoff 在测试中可注入 fake sleeper。

失败后 state 中记录 `last_heartbeat_ok=false` 和脱敏 error；最终错误转成 CheckResult/CLI 可读 FAIL，不抛出未处理异常。
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_net_tunnel.py -k "heartbeat or retry or backoff"</automated>
  </verify>
  <acceptance_criteria>
    - pid dead 时 heartbeat 返回 false，不执行远程 curl
    - pid alive + remote proxy curl exit 0 时 heartbeat 返回 true
    - pid alive + remote proxy curl fail 时 heartbeat 返回 false，并记录脱敏错误
    - ensure 最多重建 3 次，backoff 序列是 `[1, 2, 4]`
    - progress stderr stage 覆盖 heartbeat、retry、ready/fail
  </acceptance_criteria>
  <done>NET-TUNNEL-02 的心跳和重连策略有确定实现和测试</done>
</task>

<task type="auto">
  <name>Task 05-03-02: 暴露 `net tunnel ensure/status/stop` 与 probe proxy flags</name>
  <files>
    - autoresearch/net/tunnel.py
    - autoresearch/net/probe.py
    - autoresearch/cli.py
    - tests/test_net_cli.py
    - tests/test_net_tunnel.py
  </files>
  <read_first>
    - autoresearch/cli.py
    - autoresearch/net/tunnel.py
    - autoresearch/net/probe.py
    - autoresearch/hw/probe.py
    - tests/test_hw_cli.py
    - tests/test_net_cli.py
  </read_first>
  <action>
新增 `run_tunnel_ensure(server, config=None, local_proxy_url="http://127.0.0.1:7890", remote_proxy_port=17890, lang="zh") -> int`，stdout 输出唯一 CheckResult JSON，成功 data 包含 `server/remote_proxy_url/remote_port/pid/log_path/last_heartbeat_ok`，错误不泄露 proxy credentials。

在 `autoresearch/cli.py` 增加 `net tunnel ensure --server NAME --config PATH --local-proxy-url URL --remote-proxy-port INT --lang zh|en`。如果实现需要，增加 `net tunnel status` 和 `net tunnel stop`；若不加，必须在计划执行总结中说明 stop/status 延后原因。

把 `net probe` 的 `--remote-proxy-port` flag 接到 05-02 的 proxy fallback。确保 `net probe` 与 `net tunnel ensure` 都保持唯一 JSON stdout 和 stderr progress。
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_net_cli.py tests/test_net_tunnel.py -k "tunnel or remote_proxy_port or json"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run autoresearch net tunnel ensure --help` 显示 `--server`、`--local-proxy-url`、`--remote-proxy-port`、`--config`
    - `net tunnel ensure` happy path stdout 可被一次 `json.loads` 解析，stderr 可包含 progress
    - proxy URL 带凭据时 stdout/stderr 都不包含明文凭据
    - `net probe --remote-proxy-port 17891` 将 17891 传入 ensure/fallback
    - 参数或 config 错误 exit 2；tunnel 建立或 heartbeat 失败 exit 1；healthy ensure exit 0
  </acceptance_criteria>
  <done>Phase 6 可以通过 CLI 复用 Phase 5 的 tunnel ensure 能力</done>
</task>

<task type="auto">
  <name>Task 05-03-03: 全量回归和全部配置服务器真实 UAT 闸门</name>
  <files>
    - tests/test_net_curl.py
    - tests/test_net_probe.py
    - tests/test_net_tunnel.py
    - tests/test_net_cli.py
  </files>
  <read_first>
    - AGENTS.md
    - .planning/phases/05-skill-04-network-check/05-CONTEXT.md
    - autoresearch/net/curl.py
    - autoresearch/net/probe.py
    - autoresearch/net/tunnel.py
    - autoresearch/cli.py
    - config/config.yaml
  </read_first>
  <action>
补齐测试矩阵，确保 D-28 的场景全部有断言：curl parser、URL scheme 拒绝、direct/proxy attempts、proxy unavailable、tunnel state reuse/rebuild、heartbeat retry、CLI 唯一 JSON stdout、敏感字段脱敏。

运行完整 `uv run pytest`。随后在本机代理 `127.0.0.1:7890` 可用、LAN/VPN 可连全部配置服务器时，逐台执行 `uv run autoresearch net probe --server NAME`，再执行 `uv run autoresearch net probe` 默认全矩阵。每台必须产生本机+远程 rows，远程 direct 失败时 proxy retry 能建立或明确失败。最终 Phase 5 关闭要求全部配置服务器真实 UAT 通过；任何 SSH banner、网络、proxy 或 heartbeat 失败都必须记录为 blocked，不得用 mock 代替。

05-03 SUMMARY 只记录 server name、exit code、rows 数、severity、failed targets 和 sanitized error category；不得记录 host、user、identity file、bootstrap secret 或带凭据 proxy URL。
  </action>
  <verify>
    <automated>uv run pytest</automated>
    <human-check>
      <test>逐台运行 `uv run autoresearch net probe --server NAME`，再运行默认 `uv run autoresearch net probe`</test>
      <expected>当前 config 全部服务器都有 rows；direct/proxy attempts 可见；顶层 `ok=true` 或仅 warn；无 failed servers；tunnel heartbeat 成功</expected>
      <why_human>真实 SSH、外网、Mac proxy、远程代理和网络策略都是外部状态，单测无法证明</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - `uv run pytest` 全绿，且 net CLI happy/fail/config/tunnel paths 都有断言
    - `uv run autoresearch net probe --local-only` 在本机输出 baidu/hf/github rows
    - config 中每台 server 的 `net probe --server NAME` 都 exit 0 或仅 warn；若任一 FAIL，计划保持 blocked
    - 默认 `uv run autoresearch net probe` 覆盖全部配置服务器，结果顺序稳定，失败隔离
    - SUMMARY 不包含 host/IP/user/secret/proxy credential 明文
  </acceptance_criteria>
  <done>Phase 5 的自动测试、tunnel heartbeat 和全部真实服务器网络 UAT 同时达标，才允许关闭阶段</done>
</task>

</tasks>

<verification>
1. `uv run pytest -q tests/test_net_curl.py tests/test_net_probe.py tests/test_net_tunnel.py tests/test_net_cli.py`
2. `uv run pytest`
3. `uv run autoresearch net probe --local-only`
4. 对 `config/config.yaml` 中每台 server 运行 `uv run autoresearch net probe --server NAME`
5. 运行默认 `uv run autoresearch net probe`
6. 至少一次运行 `uv run autoresearch net tunnel ensure --server NAME`，确认 heartbeat 成功且 state 落在 `~/.autoresearch/tunnels/`
</verification>

<success_criteria>
- `NET-TUNNEL-02`：隧道有 30s heartbeat 和 capped retry 语义。
- `NET-LOCAL-01..03`、`NET-REMOTE-01..03`、`NET-TUNNEL-01..02` 均被自动测试或真实 UAT 覆盖。
- `D-18..D-29` 均在计划任务、must_haves 或测试中有可追踪实现点。
- 全部配置服务器真实 UAT 是 Phase 5 关闭门槛；当前 Phase 4 SSH blocker 若未恢复，Phase 5 也必须保持 blocked。
</success_criteria>

## Artifacts this phase produces

- `autoresearch.net.tunnel.heartbeat_tunnel`
- `autoresearch.net.tunnel.run_tunnel_ensure`
- `autoresearch.net.tunnel.stop_tunnel` (if status/stop helpers are implemented)
- `autoresearch.net.tunnel.status_tunnel` (if status/stop helpers are implemented)
- Click subgroup `autoresearch net tunnel`
- Click command `autoresearch net tunnel ensure`
- CLI flag `--remote-proxy-port`
- Progress stages `net.tunnel.heartbeat`, `net.tunnel.retry`, `net.tunnel.ready`, `net.tunnel.fail`
- End-of-phase real-server UAT evidence in `05-03-SUMMARY.md`

<output>
After completion, create `.planning/phases/05-skill-04-network-check/05-03-SUMMARY.md`.
</output>

