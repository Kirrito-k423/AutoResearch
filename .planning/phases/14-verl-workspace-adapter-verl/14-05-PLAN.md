---
phase: 14-verl-workspace-adapter-verl
plan: 05
type: execute
wave: 5
depends_on:
  - 14-04
files_modified:
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/phases/14-verl-workspace-adapter-verl/14-UAT.md
  - .planning/phases/14-verl-workspace-adapter-verl/14-VERIFICATION.md
  - .planning/phases/14-verl-workspace-adapter-verl/14-04-SUMMARY.md
  - workspace-adapter/verl/model_sync.py
  - autoresearch/orchestrator/verl_case.py
  - docs/verl-case.md
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "Phase 14 只有在真实 8-row sync/async matrix 成功后才算完成。"
    - "A3-AX-180 是当前 exact-image formal host；A2-AK-225 只保留为已知受阻 fallback。"
    - "Qwen/Qwen3.5-2B 本地 shard 下载必须可恢复，并在完成后自动接续 formal run。"
    - "最终 artifact bundle 必须同时包含 config snapshot、provenance、matrix results、log、wandb、prometheus、manifest、report。"
    - "最终 summary 必须记录 AutoResearch 与所有被修改依赖仓库的 commit SHA 和 GitHub 链接。"
  artifacts:
    - path: .planning/phases/14-verl-workspace-adapter-verl/14-UAT.md
      must_exist: true
      must_contain: "Complete The Real 8-Row Matrix"
    - path: .planning/phases/14-verl-workspace-adapter-verl/14-VERIFICATION.md
      must_exist: true
      must_contain: "No successful real 8-row sync/async matrix run exists yet"
    - path: ~/.autoresearch/runs/
      must_exist: true
      must_contain: "formal-"
  key_links:
    - from: workspace-adapter/verl/model_sync.py
      to: autoresearch/orchestrator/verl_case.py
      via: "本地 Qwen3.5 cache 完成后自动进入 formal orchestration"
    - from: autoresearch/orchestrator/verl_case.py
      to: .planning/phases/14-verl-workspace-adapter-verl/14-UAT.md
      via: "真实运行结果要反写到 UAT / verification / summary"
---

# Phase 14 Plan 05: Real Formal Runtime Completion And Artifact Closure

## Objective

把 Phase 14 从“代码与报告层完成、真实运行未完成”的中间态推进到真正收口：补齐本地模型缓存、在 `A3-AX-180` 上跑完真实 8-row formal matrix、验证本地 artifact bundle、把最终 run id / commit sha / GitHub links 固化到阶段文档中。

## Threat Model

- **T-14-05-01 假完成:** 仅凭测试通过、部分缓存或单机 smoke 结果误判为 formal case 完成。
- **T-14-05-02 断点丢失:** 本地大 shard 续传中断后回退或重下，导致 formal run 永远启动不了。
- **T-14-05-03 证据断链:** 真实成功运行后，没有把 config/provenance/matrix/report 与具体 commit / GitHub links 对上。
- **T-14-05-04 工作流误路由:** GSD 继续把 Phase 14 当成 complete，提前跳去 milestone-complete。

## Tasks

<task id="14-05-01" type="execute">
  <title>Keep the local Qwen3.5 cache resumable until the final shard lands</title>
  <read_first>
    - workspace-adapter/verl/model_sync.py
    - autoresearch/orchestrator/verl_case.py
    - .planning/phases/14-verl-workspace-adapter-verl/14-04-SUMMARY.md
  </read_first>
  <action>
    Continue hardening the local Qwen3.5 shard-resume path only as needed to keep the real run moving. Preserve existing bytes, prefer resumable shard-specific recovery over restart, and keep the detached supervisor path able to auto-launch `autoresearch run verl-case` as soon as `model.safetensors-00001-of-00001.safetensors` is complete.
  </action>
  <verify>
    uv run pytest -q tests/test_verl_case_runner.py -k "resume_model_download or resolve_expected_size_via_curl or resume_via_parallel_curl"
  </verify>
  <acceptance_criteria>
    - The active local shard grows across repeated resume attempts instead of restarting from zero.
    - Detached supervision remains alive after the initiating terminal session exits.
    - No manual rename/surgery is required for the Hugging Face shard layout.
  </acceptance_criteria>
</task>

<task id="14-05-02" type="execute">
  <title>Finish the real 8-row matrix on A3-AX-180</title>
  <read_first>
    - config/config.yaml
    - docs/verl-case.md
    - .planning/phases/14-verl-workspace-adapter-verl/14-UAT.md
  </read_first>
  <action>
    Once the local model cache is ready, run the real formal command on `A3-AX-180` with the exact requested image and let it finish all sync/async rows for `1024 -> 2048/4096/8192/16384`. Keep `ignore_eos=false`, keep the immutable config snapshot tied to the final run id, and fail closed if any row is missing or not `passed`.
  </action>
  <verify>
    uv run autoresearch run verl-case --server A3-AX-180 --config config/config.yaml --local-proxy-url http://127.0.0.1:7890 --remote-proxy-port 17894 --run-id <real-run-id>
  </verify>
  <acceptance_criteria>
    - The run directory under `~/.autoresearch/runs/<real-run-id>/` contains `matrix-results.jsonl` with 8 passed rows.
    - Each row records input tokens, output tokens, inference mode, elapsed seconds, tokens/s, latency, sample count, accuracy, and consistency.
    - The top-level orchestration payload returns `ok=true`.
  </acceptance_criteria>
</task>

<task id="14-05-03" type="execute">
  <title>Verify the final local-first artifact bundle and provenance chain</title>
  <read_first>
    - autoresearch/report/verl_case.py
    - autoresearch/report/render.py
    - .planning/phases/14-verl-workspace-adapter-verl/14-VERIFICATION.md
  </read_first>
  <action>
    Verify that the successful real run produced a complete local-first bundle: immutable config snapshot, provenance JSON, matrix results, Verl log, local wandb sync artifact, Prometheus evidence, manifest, and final `report.html`. Confirm the report shows full matrix, sequence-length impact, sync-vs-async impact, accuracy, consistency, and provenance sections. Record commit SHA and GitHub branch/commit links for AutoResearch and any modified dependency repos touched by the run.
  </action>
  <verify>
    uv run autoresearch report render --run-id <real-run-id>
    uv run autoresearch e2e report-check --run-id <real-run-id> --prometheus-url http://localhost:9090
  </verify>
  <acceptance_criteria>
    - `report.html` renders without missing-formal warnings.
    - `manifest.json` points to config snapshot, provenance, matrix results, log, wandb path, and Prometheus evidence.
    - Phase 14 docs record the final run id plus GitHub links for every modified repo.
  </acceptance_criteria>
</task>

## Verification

```bash
uv run pytest -q tests/test_verl_case_runner.py -k "resume_model_download or resolve_expected_size_via_curl or resume_via_parallel_curl"
uv run autoresearch run verl-case --server A3-AX-180 --config config/config.yaml --local-proxy-url http://127.0.0.1:7890 --remote-proxy-port 17894 --run-id <real-run-id>
uv run autoresearch report render --run-id <real-run-id>
```

## Artifacts This Phase Produces

- A completed real formal run under `~/.autoresearch/runs/<real-run-id>/`
- `matrix-results.jsonl` with all 8 sync/async rows passed
- Final `manifest.json`, `report.html`, local wandb artifact, and Prometheus evidence for the real run
- Updated Phase 14 UAT / verification / summary entries tied to concrete run ids and GitHub links
