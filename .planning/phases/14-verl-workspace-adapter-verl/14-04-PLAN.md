---
phase: 14-verl-workspace-adapter-verl
plan: 04
type: execute
wave: 4
depends_on:
  - 14-01
  - 14-02
  - 14-03
files_modified:
  - autoresearch/report/models.py
  - autoresearch/report/loader.py
  - autoresearch/report/render.py
  - autoresearch/report/verl_case.py
  - autoresearch/e2e/report_check.py
  - docs/verl-case.md
  - tests/test_report_verl_case.py
  - tests/test_e2e_smoke.py
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "D-97: report displays all planned lengths through 16k."
    - "D-98: report compares sync and async modes."
    - "D-99: verification treats any missing/failed matrix row as incomplete."
    - "D-100: report includes geometry3k ground-truth accuracy."
    - "D-101: report includes sync/async answer consistency."
    - "D-102: report includes tokens/s, latency, success rate, resource observations, accuracy, and consistency."
    - "D-107: report links local config, provenance, logs, W&B, Prometheus, matrix results, and manifest."
  artifacts:
    - path: autoresearch/report/verl_case.py
      must_exist: true
      must_contain: "def load_verl_case_view"
    - path: docs/verl-case.md
      must_exist: true
      must_contain: "autoresearch run verl-case"
  key_links:
    - from: autoresearch/report/loader.py
      to: datalake/manifest/schema.py
      via: "formal_case is loaded from RunManifest"
    - from: autoresearch/report/render.py
      to: autoresearch/report/verl_case.py
      via: "renderer uses normalized formal-case view"
---

# Phase 14 Plan 04: Formal Case Report, Verification, And UAT Closure

## Objective

Make the formal Verl case reviewable and verifiable: load matrix results, render performance/accuracy/consistency sections, document the user command, and enforce strict completion in tests and UAT.

## Threat Model

- **T-14-04-01 misleading report:** Report must not label partial matrix data as pass.
- **T-14-04-02 hidden artifact gaps:** Missing config/provenance/log/W&B/Prometheus/matrix artifacts must surface as warnings or failures.
- **T-14-04-03 stale docs:** User-facing docs must match actual CLI options.

## Tasks

<task id="14-04-01" type="execute">
  <title>Add formal-case report view model and loader</title>
  <read_first>
    - autoresearch/report/models.py
    - autoresearch/report/loader.py
    - datalake/manifest/schema.py
    - workspace-adapter/verl/evaluation.py
  </read_first>
  <action>
    Create `autoresearch/report/verl_case.py` with `load_verl_case_view(manifest)`. Add report models for matrix rows, length summary, async comparison, accuracy summary, and artifact status. The loader must read `matrix-results.jsonl` from manifest `formal_case`, compute per-length tokens/s and latency, compute per-mode accuracy, compute paired sync/async consistency where sample ids overlap, and expose warnings for missing rows or missing artifacts.
  </action>
  <verify>
    uv run pytest -q tests/test_report_verl_case.py -k loader
  </verify>
  <acceptance_criteria>
    - A fixture with 8 passed rows reports `complete_matrix is True`.
    - A fixture missing async 16k reports `complete_matrix is False` and includes a warning mentioning `async` and `16384`.
    - Accuracy and consistency percentages are numeric fields in the view.
  </acceptance_criteria>
</task>

<task id="14-04-02" type="execute">
  <title>Render formal case sections in HTML report</title>
  <read_first>
    - autoresearch/report/render.py
    - autoresearch/report/models.py
    - autoresearch/report/verl_case.py
  </read_first>
  <action>
    Extend `render_report` so when `ReportBundle` contains formal-case data it renders sections titled `Verl Formal Case Matrix`, `Sequence Length Impact`, `Sync vs Async Impact`, `Accuracy`, `Consistency`, and `Provenance`. Include a table with input tokens, output tokens, mode, status, tokens/s, latency, sample count, accuracy, consistency, and artifact links. Preserve the existing minimal report view for smoke runs.
  </action>
  <verify>
    uv run pytest -q tests/test_report_verl_case.py -k render
  </verify>
  <acceptance_criteria>
    - Rendered formal report HTML contains `Verl Formal Case Matrix`.
    - Rendered minimal smoke report HTML does not require formal-case data.
    - Rendered report contains links or labels for config snapshot, provenance, matrix results, logs, W&B, Prometheus, and manifest when present.
  </acceptance_criteria>
</task>

<task id="14-04-03" type="execute">
  <title>Add strict report completeness and docs</title>
  <read_first>
    - autoresearch/e2e/report_check.py
    - docs/
    - README.md
  </read_first>
  <action>
    Extend report completeness checks with a formal-case mode that requires manifest, config snapshot, provenance, matrix results, log, W&B path, Prometheus query/push evidence, and complete 8-row matrix. Add `docs/verl-case.md` documenting the command, configurable cache root, proxy/no-network path, Docker image, model/dataset, strict matrix, local artifact layout, and how to map a run id to commit SHAs/GitHub links.
  </action>
  <verify>
    uv run pytest -q tests/test_report_verl_case.py tests/test_e2e_smoke.py -k "verl_case or report"
  </verify>
  <acceptance_criteria>
    - Formal report completeness fails when a matrix row is missing.
    - `docs/verl-case.md` contains `autoresearch run verl-case`, `/Users/Zhuanz/autoResearchData`, `Qwen/Qwen3.5-2B`, `hiyouga/geometry3k`, and the Docker image name.
  </acceptance_criteria>
</task>

<task id="14-04-04" type="execute">
  <title>Run final local tests and real UAT handoff</title>
  <read_first>
    - .planning/phases/14-verl-workspace-adapter-verl/14-VALIDATION.md
    - docs/verl-case.md
    - config/config.yaml
  </read_first>
  <action>
    Run the focused Phase 14 tests, then the full test suite. If credentials and remote host are available, run the real formal command on the selected host. Record the exact run id, local artifact paths, report path, immutable config path, provenance path, and any dependency GitHub branch URLs in the Phase 14 SUMMARY during execute-phase. If real UAT cannot run because of external availability, leave the plan summary self-check failed and do not mark Phase 14 complete.
  </action>
  <verify>
    uv run pytest -q tests/test_verl_case_config.py tests/test_verl_case_runner.py tests/test_orchestrator_verl_case.py tests/test_report_verl_case.py
    uv run pytest -q
    uv run autoresearch run verl-case --server A2-AK-225 --config config/config.yaml --local-proxy-url http://127.0.0.1:7890 --remote-proxy-port 17892
  </verify>
  <acceptance_criteria>
    - Full pytest suite exits 0.
    - Real UAT produces a run directory under `~/.autoresearch/runs/<run_id>/`.
    - Real UAT report shows all sync/async length rows through 16k as passed.
    - Phase summary records commit SHAs and GitHub links for every repo modified during the run.
  </acceptance_criteria>
</task>

## Verification

```bash
uv run pytest -q tests/test_report_verl_case.py tests/test_e2e_smoke.py
uv run pytest -q
uv run autoresearch run verl-case --server A2-AK-225 --config config/config.yaml --local-proxy-url http://127.0.0.1:7890 --remote-proxy-port 17892
```

## Artifacts This Phase Produces

- `autoresearch/report/verl_case.py`
- `autoresearch/report/verl_case.py::load_verl_case_view`
- `autoresearch/report/models.py::VerlCaseView`
- Formal report sections `Verl Formal Case Matrix`, `Sequence Length Impact`, `Sync vs Async Impact`, `Accuracy`, `Consistency`, `Provenance`
- `docs/verl-case.md`
- Formal report completeness checks for complete matrix and artifact recovery
