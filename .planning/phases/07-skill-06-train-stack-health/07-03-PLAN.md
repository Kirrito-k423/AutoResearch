---
phase: 07-skill-06-train-stack-health
plan: 03
type: execute
wave: 3
depends_on:
  - 07-02
files_modified:
  - autoresearch/stack/checker.py
  - autoresearch/cli.py
  - tests/test_stack_checker.py
  - tests/test_stack_cli.py
autonomous: true
requirements:
  - STACK-VERL-01
  - STACK-VERL-02
  - STACK-VERL-03
  - STACK-VEOMNI-01
  - STACK-VEOMNI-02
  - STACK-VEOMNI-03
user_setup:
  - "07-01 + 07-02 已落地, 4 台真机可达 + conda env 配好"
must_haves:
  truths:
    - "D-07/D-29 复用: `ar-stack check --all` 走最多 3 worker 并发, 单机失败不取消其他"
    - "结果按 config 顺序输出, 4 台都过 = overall ok=True"
    - "CLI `--all` 与 `--server` 互斥 (与 Phase 6 reach 一致)"
    - "测试覆盖并发 / 失败隔离 / 自动顺序输出"
    - "4 台真机 UAT: 至少 1 台 verl + 1 台 veomni 1-step 真的打印 SUM + NPU_COUNT"
  artifacts:
    - path: autoresearch/stack/checker.py
      must_exist: true
      must_contain: "def check_all_servers"
    - path: autoresearch/cli.py
      must_exist: true
      must_contain: '"--all"'
  key_links:
    - from: autoresearch/stack/checker.py
      to: autoresearch/reach/tester.py
      via: "复用 bounded concurrency 模式 (ThreadPoolExecutor max_workers=3)"
---
# Phase 07 Plan 03: stack check --all 并发 + 4 台真机 UAT

`ar-stack check --all` 走并发, 4 台真机 UAT 一次性出报告.

## Tasks

1. **`check_all_servers` 实现** (`autoresearch/stack/checker.py`)
   - 同 Phase 4/6 模式: `ThreadPoolExecutor(max_workers=3)`
   - 每 worker 调 `check_stack(spec)`
   - 失败隔离 + 顺序输出
2. **CLI `--all` 接入** (`autoresearch/cli.py`)
   - `--all` 与 `--server` 互斥
   - 输出 JSON: `{"ok": bool, "severity": str, "data": {"results": {...}, "total": N, ...}}`
3. **测试** (`tests/test_stack_checker.py`)
   - mock 3 worker, fixture:
     - 4 机器全 PASS -> overall ok=True
     - 1 机器 fail -> 其他 PASS, overall fail
     - 顺序保持
   - click CliRunner 测 `--all` happy + 互斥
4. **UAT** (`07-03-SUMMARY.md`):
   - 4 台真机 `ar-stack check --all`, 期望 passed=4 (基于 07-02 已 OK)
   - 记录每台: 1-step 干跑 latency + npu.device_count + 库版本
   - 至少 1 台 verl + 1 台 veomni 实际打印 SUM + NPU_COUNT

## 风险

- **D-41.C4 timeout 并发**: 4 台并发跑 30s timeout, 总时长 ≤ 30s (并发); 但单台超时不会拖慢其他

