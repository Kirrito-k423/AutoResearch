---
phase: 02-workspace-core
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - workspace-core/secrets/__init__.py
  - workspace-core/secrets/resolver.py
  - workspace-core/secrets/_keyring_backend.py
  - workspace-core/config/__init__.py
  - workspace-core/config/schema.py
  - workspace-core/config/loader.py
  - tests/workspace-core/test_secrets.py
  - tests/workspace-core/test_config.py
autonomous: true
requirements:
  - CORE-SEC-01
  - CORE-SEC-02
  - CORE-CFG-01
  - CORE-CFG-02
user_setup: []
must_haves:
  truths:
    - "敏感字段占位符 <keyring:xxx> 从系统 keyring 取值"
    - "敏感字段占位符 <env:VAR> 从 env 取值"
    - "keyring 不可用时软失败 (warning + fallback env)"
    - "CI 测试用 keyring.alt.file.PlaintextKeyring fake backend"
    - "Pydantic v2 校验 config/config.yaml 成功"
    - "校验失败给中文错误: 字段路径 + 原因 + 期望"
    - "默认 ./config/config.yaml + --config flag 覆盖"
  artifacts:
    - path: workspace-core/secrets/resolver.py
      must_exist: true
      must_contain: "def resolve_secret"
    - path: workspace-core/config/schema.py
      must_exist: true
      must_contain: "class Config"
    - path: workspace-core/config/loader.py
      must_exist: true
      must_contain: "def from_yaml"
    - path: tests/workspace-core/test_secrets.py
      must_exist: true
    - path: tests/workspace-core/test_config.py
      must_exist: true
  key_links:
    - from: workspace-core/config/schema.py
      to: workspace-core/secrets/resolver.py
      via: "SecretRef type 调 resolve_secret"
    - from: workspace-core/config/loader.py
      to: workspace-core/config/schema.py
      via: "Pydantic v2 Config.model_validate"
---

<objective>
Build workspace-core/secrets/ + workspace-core/config/: keyring 双占位符解析 (D-06..09) + Pydantic v2 schema (D-10..13) + 中文错误信息。满足 CORE-SEC-01..02 + CORE-CFG-01..02。8 skill 用 `Config.from_yaml(path)` 加载配置, 敏感字段自动解密。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-workspace-core/02-CONTEXT.md (D-06..13)
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 2.1: 写 secrets/resolver.py (双占位符 + 软失败)</name>
  <files>
    - workspace-core/secrets/__init__.py
    - workspace-core/secrets/resolver.py
  </files>
  <read_first>
    - .planning/phases/02-workspace-core/02-CONTEXT.md (D-06..09: 双占位符 + 软失败)
  </read_first>
  <action>
写 secrets 解析器. 支持两种占位符:
- `<keyring:NAME>`: 从系统 keyring 取
- `<env:VAR>`: 从环境变量取

```python
# workspace-core/secrets/__init__.py
from .resolver import resolve_secret, SecretError, KEYRING_AVAILABLE

# workspace-core/secrets/resolver.py
"""敏感字段占位符解析 (D-06..09).

支持两种格式:
- <keyring:NAME>  从系统 keyring 取
- <env:VAR>       从 env 取

keyring 不可用时 (B4.b 软失败): warning + fallback env lookup.
"""
from __future__ import annotations

import os
import re
import sys
import warnings
from typing import Final


PLACEHOLDER_RE: Final = re.compile(r"^<(keyring|env):([A-Za-z0-9_\-]+)>$")


class SecretError(Exception):
    """无法解析占位符时抛出. 包含占位符字符串 + 尝试过的 backend."""


try:
    import keyring
    import keyring.alt.file
    import keyring.backend
    KEYRING_AVAILABLE: bool = True
    _KEYRING_IMPORT_ERROR: str | None = None
except ImportError as e:
    KEYRING_AVAILABLE = False
    _KEYRING_IMPORT_ERROR = str(e)
    keyring = None  # type: ignore


# 软失败警告已发出标记 (避免重复)
_warned_keyring_failure: bool = False


def _warn_keyring_failure_once() -> None:
    global _warned_keyring_failure
    if not _warned_keyring_failure:
        msg = (
            "⚠ workspace-core.secrets: keyring 不可用 ("
            + (_KEYRING_IMPORT_ERROR or "unknown")
            + "); 将 fallback 到 <env:VAR> 解析. 首次启动可忽略, "
            "生产环境请装 keyring 包."
        )
        warnings.warn(msg, stacklevel=2)
        print(msg, file=sys.stderr)
        _warned_keyring_failure = True


def _try_keyring(name: str) -> str | None:
    if not KEYRING_AVAILABLE:
        return None
    try:
        v = keyring.get_password("autoresearch", name)
        return v
    except Exception:
        _warn_keyring_failure_once()
        return None


def _try_env(var: str) -> str | None:
    return os.environ.get(var)


def resolve_secret(value: str) -> str:
    """解析占位符, 返回真实值.

    - '<keyring:NAME>' → keyring.get_password("autoresearch", NAME)
    - '<env:VAR>'      → os.environ["VAR"]
    - 其他             → 原样返回 (非占位符)

    Raises:
        SecretError: 已知占位符但所有 backend 都没找到
    """
    if not isinstance(value, str):
        return value

    m = PLACEHOLDER_RE.match(value)
    if not m:
        return value  # 非占位符, 原样

    kind, name = m.group(1), m.group(2)

    if kind == "keyring":
        v = _try_keyring(name)
        if v is not None:
            return v
        # fallback: env (软失败路径 D-09)
        v = _try_env(name)
        if v is not None:
            _warn_keyring_failure_once()
            return v
        raise SecretError(
            f"无法解析占位符 {value}; keyring 不可用 + env {name} 未设置"
        )

    if kind == "env":
        v = _try_env(name)
        if v is not None:
            return v
        raise SecretError(f"无法解析占位符 {value}; env {name} 未设置")

    raise SecretError(f"未知占位符类型: {kind}")


def resolve_dict(d: dict) -> dict:
    """递归解析 dict 中所有 value 为占位符的项."""
    out: dict = {}
    for k, v in d.items():
        if isinstance(v, str):
            out[k] = resolve_secret(v)
        elif isinstance(v, dict):
            out[k] = resolve_dict(v)
        elif isinstance(v, list):
            out[k] = [resolve_dict(i) if isinstance(i, dict) else (resolve_secret(i) if isinstance(i, str) else i) for i in v]
        else:
            out[k] = v
    return out
```

注意：
- `<keyring:xxx>` 软失败到 env: D-09 + B4.b
- `resolve_dict` 递归用于配置加载时全 dict 解密
- `_warn_keyring_failure_once` 避免重复警告
  </action>
  <verify>
```bash
cd /Users/Zhuanz/Documents/autoresearch
test -f workspace-core/secrets/__init__.py
test -f workspace-core/secrets/resolver.py
grep -q "def resolve_secret" workspace-core/secrets/resolver.py
grep -q "keyring:" workspace-core/secrets/resolver.py
grep -q "env:" workspace-core/secrets/resolver.py
uv run python -c "from workspace_core.secrets import resolve_secret, KEYRING_AVAILABLE; print(KEYRING_AVAILABLE)"
```
  </verify>
  <acceptance_criteria>
    - 2 文件存在
    - `resolve_secret(value: str) -> str` 存在
    - 双占位符格式 `<keyring:xxx>` + `<env:VAR>` 都支持
    - 软失败: keyring 不可用时 fallback env (D-09)
    - 非占位符原样返回
  </acceptance_criteria>
  <done>secrets/resolver.py 就位 (D-06..09 满足)</done>
</task>

<task type="auto">
  <name>Task 2.2: 写 config/schema.py (Pydantic v2 单文件 schema)</name>
  <files>workspace-core/config/schema.py</files>
  <read_first>
    - .planning/phases/02-workspace-core/02-CONTEXT.md (D-10..12: Pydantic v2 + 单文件)
  </read_first>
  <action>
写 Pydantic v2 schema. M1 阶段单文件, 不拆:

```python
# workspace-core/config/schema.py
"""config/config.yaml 的 Pydantic v2 schema (D-10..12)."""
from __future__ import annotations

from typing import Literal

from pydantic import BaseModel, Field, field_validator


class ServerSpec(BaseModel):
    """单台远端服务器规格."""
    name: str = Field(min_length=1, description="服务器别名 (e.g. 'nvidia-01')")
    host: str = Field(min_length=1, description="hostname 或 IP")
    port: int = Field(default=22, ge=1, le=65535, description="SSH 端口")
    user: str = Field(min_length=1, description="登录用户名")
    identity_file: str | None = Field(default=None, description="可选: 私钥路径")
    bootstrap_password_secret: str | None = Field(
        default=None,
        description="<keyring:NAME> 或 <env:VAR>, 仅首次 bootstrap 用",
    )


class NetworkProbes(BaseModel):
    """外网探针配置."""
    enabled: bool = Field(default=True)
    targets: list[str] = Field(
        default_factory=lambda: ["https://baidu.com", "https://huggingface.co", "https://github.com"],
    )


class LogConfig(BaseModel):
    """日志配置."""
    level: Literal["DEBUG", "INFO", "WARNING", "ERROR"] = "INFO"
    json: bool = Field(default=False, description="JSON 格式 (vs 人类可读)")
    dir: str = Field(default="~/.autoresearch/logs")


class WandbConfig(BaseModel):
    """本地 wandb 配置."""
    enabled: bool = Field(default=True)
    entity: str | None = None
    project: str = "autoresearch"


class Config(BaseModel):
    """整份配置 schema."""
    version: int = Field(default=1, ge=1)
    servers: list[ServerSpec] = Field(default_factory=list)
    network: NetworkProbes = Field(default_factory=NetworkProbes)
    log: LogConfig = Field(default_factory=LogConfig)
    wandb: WandbConfig = Field(default_factory=WandbConfig)

    @field_validator("servers")
    @classmethod
    def _server_names_unique(cls, v: list[ServerSpec]) -> list[ServerSpec]:
        names = [s.name for s in v]
        if len(names) != len(set(names)):
            dupes = sorted({n for n in names if names.count(n) > 1})
            raise ValueError(f"servers.name 重复: {dupes}")
        return v
```

注意：
- Pydantic v2 语法: `Field(...)`, `field_validator`
- 每个字段带 description (供错误信息用)
- `_server_names_unique` 防 name 重
  </action>
  <verify>
```bash
cd /Users/Zhuanz/Documents/autoresearch
test -f workspace-core/config/schema.py
grep -q "class Config" workspace-core/config/schema.py
grep -q "class ServerSpec" workspace-core/config/schema.py
grep -q "from pydantic" workspace-core/config/schema.py
uv run python -c "from workspace_core.config.schema import Config, ServerSpec; print('OK')"
```
  </verify>
  <acceptance_criteria>
    - `schema.py` 存在
    - `Config` + `ServerSpec` + `NetworkProbes` + `LogConfig` + `WandbConfig` 5 个 model
    - Pydantic v2 语法 (`Field`, `field_validator`)
    - `from workspace_core.config.schema import Config` 成功
  </acceptance_criteria>
  <done>config schema 就位 (D-11 满足)</done>
</task>

<task type="auto">
  <name>Task 2.3: 写 config/loader.py (from_yaml + 中文错误 + 秘密解析)</name>
  <files>
    - workspace-core/config/__init__.py
    - workspace-core/config/loader.py
  </files>
  <read_first>
    - .planning/phases/02-workspace-core/02-CONTEXT.md (D-12, D-13)
    - workspace-core/config/schema.py
    - workspace-core/secrets/resolver.py
  </read_first>
  <action>
写配置加载器: yaml → resolve secrets → Pydantic 校验 → 中文错误:

```python
# workspace-core/config/__init__.py
from .schema import Config, ServerSpec, LogConfig, WandbConfig, NetworkProbes
from .loader import from_yaml, from_path, ConfigError


  - workspace-core/config/loader.py
"""YAML → resolve secrets → Pydantic 校验 → Config (D-12, D-13)."""
from __future__ import annotations

import os
import sys
from pathlib import Path
from typing import Any

import yaml
from pydantic import ValidationError

from .schema import Config
from ..secrets import resolve_dict, SecretError


DEFAULT_PATH = Path("./config/config.yaml")


class ConfigError(Exception):
    """配置加载/校验失败. message 已是中文格式."""


def _format_validation_error(e: ValidationError, path: str) -> str:
    """把 pydantic ValidationError 翻成中文: 字段路径 + 原因 + 期望."""
    lines = [f"配置错误: {path}"]
    for err in e.errors():
        loc = ".".join(str(x) for x in err["loc"])
        msg = err["msg"]
        typ = err["type"]
        # 简化错误信息
        if typ == "missing":
            lines.append(f"  字段 {loc}: 必填但未提供")
        elif typ == "string_too_short":
            lines.append(f"  字段 {loc}: 字符串过短 (期望 ≥ {err.get('ctx', {}).get('min_length', '?')})")
        elif typ == "value_error":
            lines.append(f"  字段 {loc}: {msg}")
        elif typ == "int_parsing" or typ == "int_type":
            lines.append(f"  字段 {loc}: 期望整数, 收到 {err.get('input', '?')}")
        elif typ == "literal_error":
            lines.append(f"  字段 {loc}: 取值不在允许范围 ({err.get('ctx', {}).get('expected', '?')})")
        else:
            lines.append(f"  字段 {loc}: {msg} (type={typ})")
    return "\n".join(lines)


def from_yaml(yaml_text: str) -> Config:
    """从 yaml 文本加载 + 解密 + 校验. 失败抛 ConfigError (中文消息)."""
    try:
        raw: Any = yaml.safe_load(yaml_text) or {}
    except yaml.YAMLError as e:
        raise ConfigError(f"配置 YAML 解析失败: {e}") from e

    if not isinstance(raw, dict):
        raise ConfigError(f"配置必须是 dict, 收到 {type(raw).__name__}")

    # 解析所有 <keyring:xxx> / <env:VAR> 占位符
    try:
        resolved = resolve_dict(raw)
    except SecretError as e:
        raise ConfigError(f"配置中敏感字段无法解析: {e}") from e

    # Pydantic 校验
    try:
        cfg = Config.model_validate(resolved)
    except ValidationError as e:
        raise ConfigError(_format_validation_error(e, "config")) from e

    return cfg


def from_path(path: Path | str | None = None) -> Config:
    """从文件路径加载. 优先级: path 参数 > --config flag > env AUTORESEARCH_CONFIG > 默认路径."""
    if path is None:
        path = os.environ.get("AUTORESEARCH_CONFIG", str(DEFAULT_PATH))
    path = Path(path).expanduser()

    if not path.exists():
        raise ConfigError(f"配置文件不存在: {path}")

    try:
        text = path.read_text(encoding="utf-8")
    except OSError as e:
        raise ConfigError(f"无法读取配置文件 {path}: {e}") from e

    return from_yaml(text)
```

注意：
- `from_yaml(yaml_text)` 纯函数, 便于单测
- `from_path(path)` 默认 `./config/config.yaml` + env `AUTORESEARCH_CONFIG` 覆盖
- `ConfigError` message 已是中文, 调用方 `print(e, file=sys.stderr)` 即可
- `_format_validation_error` 把 pydantic 类型错误翻成中文字段路径
  </action>
  <verify>
```bash
cd /Users/Zhuanz/Documents/autoresearch
test -f workspace-core/config/loader.py
test -f workspace-core/config/__init__.py
grep -q "def from_yaml"
  - workspace-core/config/loader.py
grep -q "def from_path"
  - workspace-core/config/loader.py
grep -q "class ConfigError"
  - workspace-core/config/loader.py
grep -q "ConfigError"
  - workspace-core/config/loader.py
uv run python -c "from workspace_core.config import from_yaml, from_path, ConfigError; print('OK')"
```
  </verify>
  <acceptance_criteria>
    - 2 文件存在
    - `from_yaml(yaml_text) -> Config` 纯函数
    - `from_path(path) -> Config` 文件加载 + 默认 + env 覆盖
    - `ConfigError` 中文错误信息
    - 错误格式: `配置错误: {path}\n  字段 {loc}: {原因}`
  </acceptance_criteria>
  <done>config loader 就位 (D-12, D-13 满足)</done>
</task>

<task type="auto">
  <name>Task 2.4: 写测试 (D-08 PlaintextKeyring fake backend + 中文错误)</name>
  <files>
    - tests/workspace-core/test_secrets.py
    - tests/workspace-core/test_config.py
  </files>
  <read_first>
    - workspace-core/secrets/resolver.py
    - workspace-core/config/loader.py
    - workspace-core/config/schema.py
  </read_first>
  <action>
**tests/workspace-core/test_secrets.py** (用 keyring.alt.file.PlaintextKeyring fake backend):
```python
"""Tests for secrets resolver (D-06..09)."""
import os
import pytest

from workspace_core.secrets import resolver, resolve_secret, SecretError


@pytest.fixture
def fake_keyring(monkeypatch):
    """装一个 PlaintextKeyring fake backend + 写入测试值."""
    import keyring
    import keyring.alt.file
    import tempfile
    from pathlib import Path

    tmp_file = Path(tempfile.mkstemp(suffix=".keyring")[1])
    fake = keyring.alt.file.PlaintextKeyring()
    fake.file_path = str(tmp_file)
    fake.set_password("autoresearch", "test_key", "secret_value_123")
    monkeypatch.setattr(keyring, "get_keyring", lambda: fake)
    # 强制刷新内部引用
    resolver.keyring.get_keyring = lambda: fake
    yield "test_key", "secret_value_123"
    tmp_file.unlink(missing_ok=True)


def test_resolve_secret_passthrough():
    """非占位符原样返回."""
    assert resolve_secret("plain text") == "plain text"
    assert resolve_secret("") == ""
    assert resolve_secret("not-a-placeholder") == "not-a-placeholder"


def test_resolve_keyring_placeholder(fake_keyring):
    name, expected = fake_keyring
    assert resolve_secret(f"<keyring:{name}>") == expected


def test_resolve_env_placeholder(monkeypatch):
    monkeypatch.setenv("TEST_SECRET", "env_value_456")
    assert resolve_secret("<env:TEST_SECRET>") == "env_value_456"


def test_resolve_env_placeholder_missing_raises(monkeypatch):
    monkeypatch.delenv("NONEXISTENT_VAR", raising=False)
    with pytest.raises(SecretError) as exc:
        resolve_secret("<env:NONEXISTENT_VAR>")
    assert "NONEXISTENT_VAR" in str(exc.value)


def test_resolve_keyring_falls_back_to_env(monkeypatch):
    """D-09: keyring 不可用时 fallback env."""
    # 关闭 keyring (返回 None)
    import keyring
    monkeypatch.setattr(keyring, "get_password", lambda *a, **kw: None)
    monkeypatch.setenv("MY_SECRET", "fallback_value")
    assert resolve_secret("<keyring:MY_SECRET>") == "fallback_value"
```

**tests/workspace-core/test_config.py** (Pydantic 校验 + 中文错误):
```python
"""Tests for config loader (D-10..13)."""
import os
import pytest

from workspace_core.config import from_yaml, from_path, ConfigError


def test_from_yaml_minimal():
    cfg = from_yaml("version: 1\n")
    assert cfg.version == 1
    assert cfg.servers == []


def test_from_yaml_with_servers():
    yaml_text = """
version: 1
servers:
  - name: nvidia-01
    host: 192.168.1.10
    port: 22
    user: t00906153
    identity_file: ~/.ssh/id_ed25519
"""
    cfg = from_yaml(yaml_text)
    assert len(cfg.servers) == 1
    s = cfg.servers[0]
    assert s.name == "nvidia-01"
    assert s.port == 22
    assert s.user == "t00906153"


def test_from_yaml_resolves_env_placeholder(monkeypatch):
    monkeypatch.setenv("MY_TOKEN", "secret_token_xyz")
    yaml_text = """
version: 1
servers:
  - name: srv
    host: h
    user: u
    bootstrap_password_secret: "<env:MY_TOKEN>"
"""
    cfg = from_yaml(yaml_text)
    assert cfg.servers[0].bootstrap_password_secret == "secret_token_xyz"


def test_from_yaml_duplicate_server_names_raises():
    yaml_text = """
servers:
  - {name: a, host: h, user: u}
  - {name: a, host: h2, user: u2}
"""
    with pytest.raises(ConfigError) as exc:
        from_yaml(yaml_text)
    assert "servers.name" in str(exc.value) or "重复" in str(exc.value)


def test_from_yaml_missing_required_field_chinese_error():
    yaml_text = """
servers:
  - host: h
    user: u
"""
    with pytest.raises(ConfigError) as exc:
        from_yaml(yaml_text)
    msg = str(exc.value)
    assert "配置错误" in msg
    assert "name" in msg  # 缺 name 字段


def test_from_yaml_invalid_port_chinese_error():
    yaml_text = """
servers:
  - {name: a, host: h, user: u, port: 99999}
"""
    with pytest.raises(ConfigError) as exc:
        from_yaml(yaml_text)
    assert "port" in str(exc.value)


def test_from_path_default(tmp_path, monkeypatch):
    monkeypatch.chdir(tmp_path)
    cfg_dir = tmp_path / "config"
    cfg_dir.mkdir()
    (cfg_dir / "config.yaml").write_text("version: 1\nservers: []\n")
    cfg = from_path()
    assert cfg.version == 1


def test_from_path_env_override(tmp_path, monkeypatch):
    cfg_file = tmp_path / "my-config.yaml"
    cfg_file.write_text("version: 2\n")
    monkeypatch.setenv("AUTORESEARCH_CONFIG", str(cfg_file))
    cfg = from_path()
    assert cfg.version == 2


def test_from_path_missing_file_raises():
    with pytest.raises(ConfigError) as exc:
        from_path("/nonexistent/config.yaml")
    assert "不存在" in str(exc.value)
```

  </action>
  <verify>
```bash
cd /Users/Zhuanz/Documents/autoresearch
uv run pytest tests/workspace-core/test_secrets.py tests/workspace-core/test_config.py -v 2>&1 | tail -25
```
  </verify>
  <acceptance_criteria>
    - 2 测试文件存在
    - test_secrets: 5+ 测试 (passthrough / keyring / env / 缺 env 报错 / fallback)
    - test_config: 7+ 测试 (minimal / servers / env resolve / 重复 / 缺字段中文 / 端口 / 默认路径 / env 覆盖 / 文件不存在)
    - 全部 PASS
  </acceptance_criteria>
  <done>secrets + config 单测覆盖</done>
</task>

<task type="auto">
  <name>Task 2.5: git add + commit (secrets + config)</name>
  <files>
    - workspace-core/secrets/__init__.py
    - workspace-core/secrets/resolver.py
    - workspace-core/config/__init__.py
    - workspace-core/config/schema.py
    - workspace-core/config/loader.py
    - tests/workspace-core/test_secrets.py
    - tests/workspace-core/test_config.py
  </files>
  <read_first>
    - .gitignore
  </read_first>
  <action>
加 pydantic + pyyaml 依赖 + commit:

```bash
cd /Users/Zhuanz/Documents/autoresearch
uv add pydantic pyyaml
git add workspace-core/secrets/ workspace-core/config/ tests/workspace-core/test_secrets.py tests/workspace-core/test_config.py pyproject.toml uv.lock
git status

git commit -m "feat(02): add workspace-core/secrets/ + config/ (keyring + Pydantic v2)

- workspace-core/secrets/resolver.py: 双占位符 <keyring:xxx> + <env:VAR>; 软失败 (D-09)
- workspace-core/config/schema.py: Pydantic v2 单文件 (Config + ServerSpec + ...)
- # 覆盖 D-08 (CI PlaintextKeyring fake backend)
  - workspace-core/config/loader.py: from_yaml/from_path + 中文错误 + secret 解析
- tests/workspace-core/: 12+ pytest (keyring fake backend + 中文字段错误)

Closes CORE-SEC-01, CORE-SEC-02, CORE-CFG-01, CORE-CFG-02"
```
  </action>
  <verify>
```bash
cd /Users/Zhuanz/Documents/autoresearch
git log -1 --format="%h %s" | grep -q "feat(02): add workspace-core/secrets"
git show --stat HEAD | grep -E "workspace-core/secrets|workspace-core/config" | wc -l
uv run pytest tests/workspace-core/test_secrets.py tests/workspace-core/test_config.py 2>&1 | tail -3
```
  </verify>
  <acceptance_criteria>
    - 7+ 文件被 commit
    - pydantic + pyyaml 加入 pyproject.toml
    - `uv run pytest tests/workspace-core/test_secrets.py tests/workspace-core/test_config.py` 全部 PASS
  </acceptance_criteria>
  <done>secrets + config commit 完成</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] workspace-core/secrets/ 2 文件 + workspace-core/config/ 3 文件
- [ ] 双占位符 <keyring:xxx> / <env:VAR> 都解析
- [ ] keyring 不可用时软失败 (D-09)
- [ ] Pydantic v2 校验 config 成功 + 中文错误
- [ ] 默认 ./config/config.yaml + env AUTORESEARCH_CONFIG + --config flag
- [ ] 12+ pytest PASS
- [ ] CORE-SEC-01..02 + CORE-CFG-01..02 全部满足
</verification>

<success_criteria>
- All 5 tasks completed
- 8 skill 可直接 `from workspace_core.config import from_path; cfg = from_path()`
- 8 skill 配 servers 字段时可直接用 `<keyring:NAME>` / `<env:VAR>` 占位符
- 校验失败给中文错误 (字段路径 + 原因)
- secret 解析软失败不抛错
</success_criteria>

<output>
After completion, create `.planning/phases/02-workspace-core/02-02-SUMMARY.md`
</output>
