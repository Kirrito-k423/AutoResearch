---
phase: 02-workspace-core
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - workspace-core/progress/__init__.py
  - workspace-core/progress/emitter.py
  - workspace-core/log/__init__.py
  - workspace-core/log/formatter.py
  - workspace-core/log/logger.py
  - workspace-core/layout/__init__.py
  - workspace-core/layout/paths.py
  - workspace-core/result/__init__.py
  - workspace-core/result/check.py
  - tests/workspace-core/test_progress.py
  - tests/workspace-core/test_log.py
  - tests/workspace-core/test_layout.py
  - tests/workspace-core/test_result.py
autonomous: true
requirements:
  - CORE-PROTO-01
  - CORE-PROTO-02
  - CORE-LOG-01
  - CORE-LAYOUT-01
user_setup: []
must_haves:
  truths:
    - "emit_progress(stage, **fields) 写 stderr __AR_PROGRESS__=<json> 标记"
    - "最终 stdout 唯一 JSON 对象 (D-04e, CORE-PROTO-02)"
    - "统一日志格式: timestamp/level/host/msg/ctx 字段"
    - "CLI 输出人类可读, 文件输出 JSON 格式"
    - "Layout.ensure_run_dir(run_id) 创建 ~/.autoresearch/runs/<id>/{logs,wandb,prom}/"
    - "run-id 冲突硬失败 (D-17)"
    - "CheckResult TypedDict: ok/data/message/error (D-21)"
  artifacts:
    - path: workspace-core/progress/emitter.py
      must_exist: true
      must_contain: "def emit_progress"
    - path: workspace-core/log/logger.py
      must_exist: true
      must_contain: "def get_logger"
    - path: workspace-core/layout/paths.py
      must_exist: true
      must_contain: "def ensure_run_dir"
    - path: workspace-core/result/check.py
      must_exist: true
      must_contain: "CheckResult"
  key_links:
    - from: workspace-core/log/logger.py
      to: workspace-core/layout/paths.py
      via: "默认 log 路径 ~/.autoresearch/logs/"
    - from: workspace-core/layout/paths.py
      to: pathlib.Path.home()
      via: "~/.autoresearch/ 根目录"
---

<objective>
Build workspace-core/{progress,log,layout,result}/ 沉淀: 进度协议 (D-14..15) + 统一日志 (D-16) + 固定目录 (D-17) + CheckResult schema (D-21)。满足 CORE-PROTO-01..02 + CORE-LOG-01 + CORE-LAYOUT-01。8 skill 用统一的进度 / 日志 / 目录 / 结果接口, 互不重复造轮子。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-workspace-core/02-CONTEXT.md (D-14..17, D-21)
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 3.1: 写 progress/emitter.py (CORE-PROTO-01..02)</name>
  <files>
    - workspace-core/progress/__init__.py
    - workspace-core/progress/emitter.py
  </files>
  <read_first>
    - .planning/phases/02-workspace-core/02-CONTEXT.md (D-14, D-15)
    - .planning/AGENTS.md (进度协议模板)
  </read_first>
  <action>
写进度协议:

```python
# workspace-core/progress/__init__.py
from .emitter import emit_progress, ProgressEvent


# workspace-core/progress/emitter.py
"""__AR_PROGRESS__=<json> 协议 (D-14, D-15).

D-04e 锁定: 最终 stdout 唯一 JSON 对象.
D-04d 锁定: 进度走 stderr, 格式 __AR_PROGRESS__=<json>.
"""
from __future__ import annotations

import datetime
import json
import sys
from typing import Any, Literal


PROGRESS_PREFIX = "__AR_PROGRESS__="
"""stderr 行前缀. 下游消费者 (Archon / 自家 pipeline) grep 这个标记."""

Level = Literal["info", "warn", "error"]


def emit_progress(
    stage: str,
    *,
    level: Level = "info",
    data: dict[str, Any] | None = None,
    **fields: Any,
) -> None:
    """发一条进度事件到 stderr.

    Args:
        stage: 阶段名 (e.g. 'ssh.connect', 'secrets.resolve', 'config.load')
        level: info/warn/error
        data: 可选 dict, 写入 JSON.data 字段
        **fields: 任意附加字段, 展开到 JSON 顶层

    Output: stderr 一行, 格式 '__AR_PROGRESS__=<json>'
    """
    payload: dict[str, Any] = {
        "stage": stage,
        "ts": datetime.datetime.now(tz=datetime.timezone.utc).isoformat(timespec="seconds"),
        "level": level,
    }
    if data is not None:
        payload["data"] = data
    payload.update(fields)

    line = PROGRESS_PREFIX + json.dumps(payload, ensure_ascii=False)
    print(line, file=sys.stderr, flush=True)


# TypedDict (用于类型注解, 不强制)
class ProgressEvent(dict):
    """一帧进度事件 (dict subclass, 无强制 schema)."""
    def __init__(self, stage: str, level: Level = "info", data: dict | None = None, **fields: Any):
        super().__init__(
            stage=stage,
            ts=datetime.datetime.now(tz=datetime.timezone.utc).isoformat(timespec="seconds"),
            level=level,
            data=data or {},
            **fields,
        )
```

注意:
- 强制前缀 `__AR_PROGRESS__=` (与 AGENTS.md 锁定)
- ts 用 UTC ISO 格式
- `data` 字段是 dict (skill 自定义)
- 输出 stderr 不污染 stdout (D-04e 唯一 JSON)
  </action>
  <verify>
```bash
cd /Users/Zhuanz/Documents/autoresearch
test -f workspace-core/progress/emitter.py
test -f workspace-core/progress/__init__.py
grep -q "def emit_progress" workspace-core/progress/emitter.py
grep -q "__AR_PROGRESS__" workspace-core/progress/emitter.py
uv run python -c "from workspace_core.progress import emit_progress; emit_progress('test', data={'k':'v'})" 2>&1 | grep -q "__AR_PROGRESS__"
```
  </verify>
  <acceptance_criteria>
    - 2 文件存在
    - `emit_progress(stage, level='info', data=None, **fields)` 函数
    - 输出 `__AR_PROGRESS__=<json>` 到 stderr
    - `data` 字段是 dict
  </acceptance_criteria>
  <done>progress emitter 就位 (CORE-PROTO-01 满足)</done>
</task>

<task type="auto">
  <name>Task 3.2: 写 log/logger.py + formatter.py (CORE-LOG-01)</name>
  <files>
    - workspace-core/log/__init__.py
    - workspace-core/log/formatter.py
    - workspace-core/log/logger.py
  </files>
  <read_first>
    - .planning/phases/02-workspace-core/02-CONTEXT.md (D-16: stdlib + JSONFormatter)
    - workspace-core/layout/paths.py (log_dir 路径, 后续会建)
  </read_first>
  <action>
写统一日志: stdlib logging + 自定义 JsonFormatter (D-16):

```python
# workspace-core/log/__init__.py
from .logger import get_logger, configure_root


# workspace-core/log/formatter.py
"""日志 Formatter: 人类可读 (CLI) + JSON (文件)."""
from __future__ import annotations

import json
import logging
from datetime import datetime, timezone


_ANSI = {
    "DEBUG": "\033[90m",  # 灰
    "INFO": "\033[36m",   # 青
    "WARNING": "\033[33m", # 黄
    "ERROR": "\033[31m",  # 红
    "CRITICAL": "\033[1;31m",  # 粗红
    "RESET": "\033[0m",
}


class HumanFormatter(logging.Formatter):
    """人类可读 + ANSI 颜色."""
    def format(self, record: logging.LogRecord) -> str:
        ts = datetime.fromtimestamp(record.created, tz=timezone.utc).strftime("%H:%M:%S")
        level = record.levelname
        color = _ANSI.get(level, "")
        reset = _ANSI["RESET"]
        name = record.name
        msg = record.getMessage()
        # ctx 字段
        ctx = getattr(record, "ctx", None)
        ctx_str = f" ctx={ctx}" if ctx else ""
        return f"{ts} {color}{level:<7}{reset} {name}: {msg}{ctx_str}"


class JsonFormatter(logging.Formatter):
    """JSON 行格式 (落文件)."""
    def format(self, record: logging.LogRecord) -> str:
        payload = {
            "ts": datetime.fromtimestamp(record.created, tz=timezone.utc).isoformat(timespec="milliseconds"),
            "level": record.levelname,
            "logger": record.name,
            "msg": record.getMessage(),
        }
        if hasattr(record, "host") and record.host:
            payload["host"] = record.host
        if hasattr(record, "ctx") and record.ctx:
            payload["ctx"] = record.ctx
        if record.exc_info:
            payload["exc"] = self.formatException(record.exc_info)
        return json.dumps(payload, ensure_ascii=False)


# workspace-core/log/logger.py
"""统一日志接口 (D-16).

get_logger(name) → logger; 带 stderr (人类可读) + 文件 (JSON) handler.
"""
from __future__ import annotations

import logging
import sys
from pathlib import Path

from .formatter import HumanFormatter, JsonFormatter


_LOGGERS: dict[str, logging.Logger] = {}
_DEFAULT_LEVEL = logging.INFO


def configure_root(
    level: int = _DEFAULT_LEVEL,
    log_file: Path | None = None,
    enable_stderr: bool = True,
) -> None:
    """配置 root logger.

    Args:
        level: 日志级别 (logging.INFO 等)
        log_file: 可选, 落 JSON 行到这个文件 (parent dirs 自动建)
        enable_stderr: 是否输出到 stderr (人类可读)
    """
    root = logging.getLogger()
    root.setLevel(level)
    # 清掉旧 handler
    for h in list(root.handlers):
        root.removeHandler(h)

    if enable_stderr:
        h = logging.StreamHandler(sys.stderr)
        h.setFormatter(HumanFormatter())
        root.addHandler(h)

    if log_file is not None:
        log_file.parent.mkdir(parents=True, exist_ok=True)
        fh = logging.FileHandler(log_file, encoding="utf-8")
        fh.setFormatter(JsonFormatter())
        root.addHandler(fh)


def get_logger(name: str) -> logging.Logger:
    """取 logger. 不重名就返同一个; 不自动加 handler (由 configure_root 管)."""
    if name not in _LOGGERS:
        _LOGGERS[name] = logging.getLogger(name)
    return _LOGGERS[name]
```

注意:
- `get_logger` 是缓存, 多次调用同名返同一 logger (避免重复 handler)
- `configure_root` 由 entry CLI 调用 (不是自动)
- HumanFormatter 用 ANSI 颜色 (stderr 是 TTY 才显示, 重定向到 file 时无效)
- JsonFormatter 落文件时给 `~/.autoresearch/logs/<skill>-<date>.log`
  </action>
  <verify>
```bash
cd /Users/Zhuanz/Documents/autoresearch
test -f workspace-core/log/logger.py
test -f workspace-core/log/formatter.py
test -f workspace-core/log/__init__.py
grep -q "def get_logger" workspace-core/log/logger.py
grep -q "def configure_root" workspace-core/log/logger.py
grep -q "class HumanFormatter" workspace-core/log/formatter.py
grep -q "class JsonFormatter" workspace-core/log/formatter.py
uv run python -c "
import io, sys
from workspace_core.log import get_logger, configure_root
configure_root()
log = get_logger('test')
log.info('hello', extra={'ctx': {'a': 1}})
" 2>&1 | grep -q "INFO"
```
  </verify>
  <acceptance_criteria>
    - 3 文件存在
    - `get_logger(name) -> Logger` 缓存
    - `configure_root(level, log_file, enable_stderr)` 配置
    - HumanFormatter 颜色 + JSONFormatter 落文件
    - JSON 字段: ts/level/logger/msg (host/ctx 可选)
  </acceptance_criteria>
  <done>log logger/formatter 就位 (CORE-LOG-01 满足)</done>
</task>

<task type="auto">
  <name>Task 3.3: 写 layout/paths.py (CORE-LAYOUT-01)</name>
  <files>
    - workspace-core/layout/__init__.py
    - workspace-core/layout/paths.py
  </files>
  <read_first>
    - .planning/phases/02-workspace-core/02-CONTEXT.md (D-17: ~/.autoresearch/...)
  </read_first>
  <action>
写目录约定: `~/.autoresearch/{runs,logs,cache,ssh_keys}/`

```python
# workspace-core/layout/__init__.py
from .paths import (
    ROOT_DIR,
    RUNS_DIR,
    LOGS_DIR,
    CACHE_DIR,
    SSH_KEYS_DIR,
    run_dir,
    ensure_run_dir,
    DEFAULT_RUN_ID_RE,
)


# workspace-core/layout/paths.py
"""~/.autoresearch/ 固定目录 (D-17, CORE-LAYOUT-01).

惰性创建: 调 ensure_* 才建.
"""
from __future__ import annotations

import re
import shutil
from dataclasses import dataclass
from pathlib import Path
from typing import Final


# === 根目录常量 ===
ROOT_DIR: Final[Path] = Path.home() / ".autoresearch"
RUNS_DIR: Final[Path] = ROOT_DIR / "runs"
LOGS_DIR: Final[Path] = ROOT_DIR / "logs"
CACHE_DIR: Final[Path] = ROOT_DIR / "cache"
SSH_KEYS_DIR: Final[Path] = ROOT_DIR / "ssh_keys"


# === run-id 校验 ===
# 允许: 日期前缀 + 自由名 (e.g. 2026-06-06-smoke-001, my-exp-v2)
DEFAULT_RUN_ID_RE: Final[re.Pattern] = re.compile(r"^[A-Za-z0-9][A-Za-z0-9_\-\.]{0,127}$")


class RunIDError(ValueError):
    """run-id 非法 (含路径分隔符 / 太长 / 起始字符异常)."""


def _validate_run_id(run_id: str) -> str:
    if not isinstance(run_id, str) or not DEFAULT_RUN_ID_RE.match(run_id):
        raise RunIDError(
            f"run-id 非法 '{run_id}'; 须匹配 {DEFAULT_RUN_ID_RE.pattern}"
        )
    return run_id


@dataclass(frozen=True)
class RunPaths:
    """单次 run 的所有路径, 一次解析免得后面散落 Path 拼接."""
    run_id: str
    root: Path          # ~/.autoresearch/runs/<id>/
    logs: Path          # <root>/logs/
    wandb: Path         # <root>/wandb/
    prom: Path          # <root>/prom/
    manifest: Path      # <root>/manifest.json


def run_dir(run_id: str, *, create: bool = True) -> RunPaths:
    """获取 run 目录结构. create=True 时 ensure_run_dir 行为."""
    rid = _validate_run_id(run_id)
    root = RUNS_DIR / rid
    paths = RunPaths(
        run_id=rid,
        root=root,
        logs=root / "logs",
        wandb=root / "wandb",
        prom=root / "prom",
        manifest=root / "manifest.json",
    )
    if create:
        for p in (paths.logs, paths.wandb, paths.prom):
            p.mkdir(parents=True, exist_ok=True)
    return paths


def ensure_run_dir(run_id: str) -> RunPaths:
    """ensure + create. run-id 冲突时硬失败 (D-17)."""
    rid = _validate_run_id(run_id)
    target = RUNS_DIR / rid
    if target.exists() and any(target.iterdir()):
        raise FileExistsError(
            f"run-id '{rid}' 目录已存在且非空 ({target}); "
            f"换 --run-id 或先删. 已存在: {list(target.iterdir())[:3]}"
        )
    return run_dir(rid, create=True)


def ensure_root() -> Path:
    """确保 ~/.autoresearch/{runs,logs,cache,ssh_keys}/ 4 个目录都建."""
    for d in (RUNS_DIR, LOGS_DIR, CACHE_DIR, SSH_KEYS_DIR):
        d.mkdir(parents=True, exist_ok=True)
    return ROOT_DIR


def clean_run(run_id: str) -> None:
    """强制删一个 run 目录 (D-17 cleanup helper, 不在 M1 强制要求)."""
    rid = _validate_run_id(run_id)
    target = RUNS_DIR / rid
    if target.exists():
        shutil.rmtree(target)
```

注意:
- `_validate_run_id` 防路径注入 (../) + 长度限制
- `RunPaths` dataclass 一次返回所有路径, 避免后续散落 Path 拼接
- `ensure_run_dir` 冲突硬失败, 提示用户 `--run-id` 或先删
  </action>
  <verify>
```bash
cd /Users/Zhuanz/Documents/autoresearch
test -f workspace-core/layout/paths.py
test -f workspace-core/layout/__init__.py
grep -q "def run_dir" workspace-core/layout/paths.py
grep -q "def ensure_run_dir" workspace-core/layout/paths.py
grep -q "class RunPaths" workspace-core/layout/paths.py
grep -q "RUNS_DIR" workspace-core/layout/paths.py
uv run python -c "
import tempfile
import os
from pathlib import Path
from workspace_core.layout import ensure_run_dir, run_dir
with tempfile.TemporaryDirectory() as td:
    os.environ['HOME'] = td
    from workspace_core.layout.paths import RUNS_DIR
    p = ensure_run_dir('2026-06-06-test')
    assert p.logs.exists() and p.wandb.exists() and p.prom.exists()
    print('OK')
"
```
  </verify>
  <acceptance_criteria>
    - 2 文件存在
    - `run_dir(run_id) -> RunPaths` 返回 dataclass
    - `ensure_run_dir(run_id)` 冲突硬失败
    - run-id 校验防注入 (DEFAULT_RUN_ID_RE)
    - 4 个根目录 (runs/logs/cache/ssh_keys)
  </acceptance_criteria>
  <done>layout paths 就位 (CORE-LAYOUT-01 满足)</done>
</task>

<task type="auto">
  <name>Task 3.4: 写 result/check.py (D-21 — CheckResult schema)</name>
  <files>
    - workspace-core/result/__init__.py
    - workspace-core/result/check.py
  </files>
  <read_first>
    - .planning/phases/02-workspace-core/02-CONTEXT.md (D-21)
    - autoresearch/services/_common.py (phase 1 HealthResult 参考)
  </read_first>
  <action>
写 CheckResult schema (D-21): 统一 8 skill 的返回结构.

```python
# workspace-core/result/__init__.py
from .check import CheckResult, ok, fail, merge, CheckSeverity


# workspace-core/result/check.py
"""统一 skill/check 返回结构 (D-21).

8 skill 全部用 CheckResult; 不是 REQ 衍生但下游都依赖.
"""
from __future__ import annotations

from dataclasses import dataclass, field, asdict
from enum import Enum
from typing import Any, TypedDict


class CheckSeverity(str, Enum):
    OK = "ok"
    WARN = "warn"
    FAIL = "fail"


class CheckResult(TypedDict):
    """单次 check 的返回结构."""
    ok: bool
    severity: CheckSeverity
    data: dict[str, Any]
    message: str
    error: str | None


def ok(data: dict[str, Any] | None = None, message: str = "ok") -> CheckResult:
    return CheckResult(
        ok=True,
        severity=CheckSeverity.OK,
        data=data or {},
        message=message,
        error=None,
    )


def fail(
    error: str,
    data: dict[str, Any] | None = None,
    message: str = "fail",
    severity: CheckSeverity = CheckSeverity.FAIL,
) -> CheckResult:
    return CheckResult(
        ok=False,
        severity=severity,
        data=data or {},
        message=message,
        error=error,
    )


def merge(results: list[CheckResult]) -> CheckResult:
    """合并多个 check 结果. 任何 fail → 整体 fail; 否则看 warn."""
    if not results:
        return ok(message="no checks run")
    any_fail = any(not r["ok"] for r in results)
    any_warn = any(r["severity"] == CheckSeverity.WARN for r in results)
    severity = CheckSeverity.FAIL if any_fail else (CheckSeverity.WARN if any_warn else CheckSeverity.OK)
    return CheckResult(
        ok=not any_fail,
        severity=severity,
        data={"count": len(results), "failed": sum(1 for r in results if not r["ok"])},
        message=f"merged {len(results)} checks",
        error="; ".join(r["error"] for r in results if r["error"]) if any_fail else None,
    )
```

注意:
- TypedDict 而不是 dataclass: 跟 phase 1 _common.py HealthResult 一致 (跟 dict 兼容, 易序列化)
- `ok` / `fail` 工厂函数: 简化调用
- `merge` 用于并发 check 后合并 (02-04 ar-ping 也会用)
  </action>
  <verify>
```bash
cd /Users/Zhuanz/Documents/autoresearch
test -f workspace-core/result/check.py
test -f workspace-core/result/__init__.py
grep -q "class CheckResult" workspace-core/result/check.py
grep -q "def ok" workspace-core/result/check.py
grep -q "def fail" workspace-core/result/check.py
grep -q "def merge" workspace-core/result/check.py
uv run python -c "from workspace_core.result import ok, fail, merge, CheckResult; print('OK')"
```
  </verify>
  <acceptance_criteria>
    - 2 文件存在
    - `CheckResult` TypedDict
    - `ok` / `fail` / `merge` 3 个工厂
    - `CheckSeverity` 3 档 (ok/warn/fail)
  </acceptance_criteria>
  <done>result schema 就位 (D-21 满足)</done>
</task>

<task type="auto">
  <name>Task 3.5: 写测试 (progress + log + layout + result)</name>
  <files>
    - tests/workspace-core/test_progress.py
    - tests/workspace-core/test_log.py
    - tests/workspace-core/test_layout.py
    - tests/workspace-core/test_result.py
  </files>
  <read_first>
    - workspace-core/progress/emitter.py
    - workspace-core/log/logger.py
    - workspace-core/log/formatter.py
    - workspace-core/layout/paths.py
    - workspace-core/result/check.py
  </read_first>
  <action>
**tests/workspace-core/test_progress.py**:
```python
"""Tests for progress emitter."""
import json
import sys
import io
import pytest

from workspace_core.progress import emit_progress, PROGRESS_PREFIX


def test_emit_progress_writes_to_stderr(capsys):
    emit_progress("test.stage", data={"k": "v"})
    captured = capsys.readouterr()
    assert captured.err.startswith(PROGRESS_PREFIX)
    payload = json.loads(captured.err[len(PROGRESS_PREFIX):])
    assert payload["stage"] == "test.stage"
    assert payload["data"] == {"k": "v"}
    assert payload["level"] == "info"
    assert "ts" in payload


def test_emit_progress_does_not_pollute_stdout(capsys):
    emit_progress("test")
    captured = capsys.readouterr()
    assert captured.out == ""


def test_emit_progress_with_extra_fields(capsys):
    emit_progress("ssh.connect", host="nvidia-01", latency_ms=42)
    captured = capsys.readouterr()
    payload = json.loads(captured.err[len(PROGRESS_PREFIX):])
    assert payload["host"] == "nvidia-01"
    assert payload["latency_ms"] == 42
```

**tests/workspace-core/test_log.py**:
```python
"""Tests for log formatters."""
import io
import json
import logging
import pytest

from workspace_core.log.formatter import HumanFormatter, JsonFormatter


def test_human_formatter_includes_level_and_msg():
    rec = logging.LogRecord(
        name="test", level=logging.INFO, pathname="", lineno=0,
        msg="hello %s", args=("world",), exc_info=None,
    )
    out = HumanFormatter().format(rec)
    assert "INFO" in out
    assert "test" in out
    assert "hello world" in out


def test_human_formatter_includes_ctx():
    rec = logging.LogRecord(
        name="test", level=logging.INFO, pathname="", lineno=0,
        msg="hello", args=(), exc_info=None,
    )
    rec.ctx = {"a": 1}
    out = HumanFormatter().format(rec)
    assert "ctx" in out
    assert "'a': 1" in out or '"a": 1' in out


def test_json_formatter_includes_required_fields():
    rec = logging.LogRecord(
        name="test", level=logging.WARNING, pathname="", lineno=0,
        msg="warn %d", args=(42,), exc_info=None,
    )
    rec.host = "nvidia-01"
    out = JsonFormatter().format(rec)
    payload = json.loads(out)
    assert payload["level"] == "WARNING"
    assert payload["logger"] == "test"
    assert payload["msg"] == "warn 42"
    assert payload["host"] == "nvidia-01"
    assert "ts" in payload


def test_get_logger_caches():
    from workspace_core.log import get_logger
    a = get_logger("foo")
    b = get_logger("foo")
    assert a is b
    c = get_logger("bar")
    assert a is not c
```

**tests/workspace-core/test_layout.py**:
```python
"""Tests for layout paths."""
import os
import tempfile
from pathlib import Path
import pytest

from workspace_core.layout import ensure_run_dir, run_dir
from workspace_core.layout.paths import (
    RUNS_DIR, LOGS_DIR, CACHE_DIR, SSH_KEYS_DIR, ROOT_DIR,
)
from workspace_core.layout.paths import RunIDError


@pytest.fixture
def fake_home(monkeypatch, tmp_path):
    monkeypatch.setenv("HOME", str(tmp_path))
    # 重新导入以重置 Path.home()
    from workspace_core.layout import paths
    monkeypatch.setattr(paths, "ROOT_DIR", tmp_path / ".autoresearch")
    monkeypatch.setattr(paths, "RUNS_DIR", paths.ROOT_DIR / "runs")
    monkeypatch.setattr(paths, "LOGS_DIR", paths.ROOT_DIR / "logs")
    monkeypatch.setattr(paths, "CACHE_DIR", paths.ROOT_DIR / "cache")
    monkeypatch.setattr(paths, "SSH_KEYS_DIR", paths.ROOT_DIR / "ssh_keys")
    yield tmp_path


def test_ensure_run_dir_creates_subdirs(fake_home):
    p = ensure_run_dir("2026-06-06-test-001")
    assert p.logs.exists()
    assert p.wandb.exists()
    assert p.prom.exists()
    assert not p.manifest.exists()  # 写到 manifest.json 是调用方的事


def test_ensure_run_dir_conflict_raises(fake_home):
    ensure_run_dir("dup")
    with pytest.raises(FileExistsError) as exc:
        ensure_run_dir("dup")
    assert "已存在" in str(exc.value)


def test_run_id_validation_rejects_path_traversal():
    from workspace_core.layout.paths import _validate_run_id
    with pytest.raises(RunIDError):
        _validate_run_id("../etc/passwd")
    with pytest.raises(RunIDError):
        _validate_run_id("")
    with pytest.raises(RunIDError):
        _validate_run_id("a" * 200)  # too long


def test_run_id_validation_accepts_normal():
    from workspace_core.layout.paths import _validate_run_id
    assert _validate_run_id("2026-06-06-smoke-001") == "2026-06-06-smoke-001"
    assert _validate_run_id("my_exp.v2") == "my_exp.v2"
```

**tests/workspace-core/test_result.py**:
```python
"""Tests for CheckResult schema (D-21)."""
import pytest

from workspace_core.result import ok, fail, merge, CheckResult, CheckSeverity


def test_ok_factory():
    r = ok(data={"x": 1}, message="done")
    assert r["ok"] is True
    assert r["severity"] == CheckSeverity.OK
    assert r["data"] == {"x": 1}
    assert r["error"] is None


def test_fail_factory():
    r = fail(error="connection refused", message="ssh connect failed")
    assert r["ok"] is False
    assert r["severity"] == CheckSeverity.FAIL
    assert r["error"] == "connection refused"


def test_merge_all_ok():
    results = [ok(data={"a": 1}), ok(data={"b": 2})]
    merged = merge(results)
    assert merged["ok"] is True
    assert merged["severity"] == CheckSeverity.OK


def test_merge_one_fail():
    results = [ok(), fail("oops"), ok()]
    merged = merge(results)
    assert merged["ok"] is False
    assert merged["severity"] == CheckSeverity.FAIL
    assert "oops" in merged["error"]
    assert merged["data"]["failed"] == 1
    assert merged["data"]["count"] == 3


def test_merge_empty():
    merged = merge([])
    assert merged["ok"] is True
    assert merged["data"]["count"] == 0
```

  </action>
  <verify>
```bash
cd /Users/Zhuanz/Documents/autoresearch
uv run pytest tests/workspace-core/test_progress.py tests/workspace-core/test_log.py tests/workspace-core/test_layout.py tests/workspace-core/test_result.py -v 2>&1 | tail -25
```
  </verify>
  <acceptance_criteria>
    - 4 测试文件存在
    - test_progress: 3+ 测试 (stderr / stdout 不污染 / extra fields)
    - test_log: 4+ 测试 (human / ctx / json / get_logger 缓存)
    - test_layout: 4+ 测试 (subdirs / 冲突 / path traversal / 正常 id)
    - test_result: 5+ 测试 (ok / fail / merge)
    - 全部 PASS
  </acceptance_criteria>
  <done>progress + log + layout + result 单测覆盖</done>
</task>

<task type="auto">
  <name>Task 3.6: git add + commit (progress + log + layout + result)</name>
  <files>
    - workspace-core/progress/__init__.py
    - workspace-core/progress/emitter.py
    - workspace-core/log/__init__.py
    - workspace-core/log/formatter.py
    - workspace-core/log/logger.py
    - workspace-core/layout/__init__.py
    - workspace-core/layout/paths.py
    - workspace-core/result/__init__.py
    - workspace-core/result/check.py
    - tests/workspace-core/test_progress.py
    - tests/workspace-core/test_log.py
    - tests/workspace-core/test_layout.py
    - tests/workspace-core/test_result.py
  </files>
  <read_first>
    - .gitignore
  </read_first>
  <action>
commit:

```bash
cd /Users/Zhuanz/Documents/autoresearch
git add workspace-core/progress/ workspace-core/log/ workspace-core/layout/ workspace-core/result/ tests/workspace-core/test_progress.py tests/workspace-core/test_log.py tests/workspace-core/test_layout.py tests/workspace-core/test_result.py
git status

git commit -m "feat(02): add workspace-core/{progress,log,layout,result}/

- workspace-core/progress/emitter.py: __AR_PROGRESS__=<json> 协议 (D-14, D-15)
- workspace-core/log/logger.py + formatter.py: stdlib + HumanFormatter + JsonFormatter (D-16)
- workspace-core/layout/paths.py: ~/.autoresearch/{runs,logs,cache,ssh_keys}/ + RunPaths (D-17)
- workspace-core/result/check.py: CheckResult TypedDict + ok/fail/merge (D-21)
- tests/workspace-core/: 16+ pytest (progress / log / layout / result)

Closes CORE-PROTO-01, CORE-PROTO-02, CORE-LOG-01, CORE-LAYOUT-01"
```
  </action>
  <verify>
```bash
cd /Users/Zhuanz/Documents/autoresearch
git log -1 --format="%h %s" | grep -q "feat(02): add workspace-core"
git show --stat HEAD | grep -E "workspace-core/(progress|log|layout|result)" | wc -l
uv run pytest tests/workspace-core/ 2>&1 | tail -3
```
  </verify>
  <acceptance_criteria>
    - 13+ 文件被 commit
    - `uv run pytest tests/workspace-core/` 全部 PASS (含本批 + 02-01/02-02)
  </acceptance_criteria>
  <done>progress + log + layout + result commit 完成</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] workspace-core/progress/, log/, layout/, result/ 9 文件就位
- [ ] emit_progress 写 stderr __AR_PROGRESS__=<json>
- [ ] get_logger + HumanFormatter + JsonFormatter 落文件
- [ ] ensure_run_dir 创建 logs/wandb/prom 3 子目录
- [ ] run-id 冲突硬失败 + 路径注入防护
- [ ] CheckResult TypedDict + ok/fail/merge 工厂
- [ ] 16+ pytest 全部 PASS
- [ ] CORE-PROTO-01..02 + CORE-LOG-01 + CORE-LAYOUT-01 全部满足
</verification>

<success_criteria>
- All 6 tasks completed
- 8 skill 可直接 `from workspace_core.progress import emit_progress`
- 8 skill 可直接 `from workspace_core.log import get_logger, configure_root`
- 8 skill 可直接 `from workspace_core.layout import ensure_run_dir, run_dir`
- 8 skill 可直接 `from workspace_core.result import ok, fail, merge, CheckResult`
- 进度协议 __AR_PROGRESS__= 与 phase 1 AGENTS.md 锁定一致
- 目录约定 ~/.autoresearch/ 跨平台 (Mac/Linux)
</success_criteria>

<output>
After completion, create `.planning/phases/02-workspace-core/02-03-SUMMARY.md`
</output>
